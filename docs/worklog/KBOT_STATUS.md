# K-BOT — STATUS (single source of truth)

This file is the one place that says what is done, what is in progress, and what is next.
Three people rely on it: the developer, Claude (chat), and Code (Claude Code / Codex).
Keep it current. If reality and this file disagree, fix this file in the same commit.

**Rule:** every task also produces a worklog in `docs/worklog/` (see
`docs/worklog/CODE_WORKFLOW.md`). This STATUS file is the summary; the worklog is the detail.

---

## Slice registry

Numbers are permanent once assigned. A slice done in several passes keeps one number and
gets sub-numbered worklogs (`SLICE-0007-01-…`, `SLICE-0007-02-…`). The **next free slice
number** is recorded at the bottom of this section — bump it when you assign a new one.

> The numbers below are a starting proposal. Confirm or renumber them once, then treat
> them as fixed. Felia 1 (auth) is the only one anchored by history; the rest are assigned
> here for the first time.

| Slice | Name | Status | Worklog(s) | Notes |
|------:|------|--------|-----------|-------|
| 0001 | Auth — bearer tokens, session store (Felia 1) | DONE | (pre-worklog-rule) | Static API key eliminated for K-BOT; legacy FOREXE still uses X-Api-Key |
| 0002 | Split-brain 401 fix + reason codes | DONE | (pre-worklog-rule) | login mints via STORE; every 401 carries a reason code |
| 0003 | Redis session backend | DONE (code) / config PENDING on VPS | — | `SESSION_BACKEND="redis"`, `SESSION_KEY_PREFIX`, `REDIS_DB=2` must be set in host config.py; verify DB 2 is free |
| 0004 | Tier 1 hardening | IN PROGRESS | — | rate limiter DONE+pushed; remaining: ApiOptions address, retire AppConfig, verify gunicorn guard. Plan: `KBOT_Tier1_Plan.md` |
| 0005 | Phase A cleanup (lying tests, https guard) | DONE | — | Python 75 passed / 7 skipped, 0 fail/error; .NET 80 green |
| 0006 | MainForm scaffolding | DONE | (pre-worklog-rule) | Plan: `PLAN_MainForm_Scaffolding.md`. Built against its own 11-item checklist; `WithReauth(Of T)` + ListaAngajamente vertical preserved. Real DI signature is 5 params (`forexeRunner, session, apiClient, authApi, loginFactory`), not the 4 the plan expected |
| 0007 | AngajamentTreeInfo POCO correction | SUPERSEDED by 0008 | `SLICE-0008-tree-data-api.md` | Was done WRONG: built against `qFX_MAIN_TREE` alone, so `Salarii` was dropped and `IDORD` kept. 0008 rewrote the POCO against the real contract (row-source `_DESCRIERE` + flags `qFX_MAIN_TREE`): `Salarii` restored, `IDORD` dropped |
| 0008 | Tree data API + `MainForm.LoadTree` | DONE (code) / UNVERIFIED on a live DB | `SLICE-0008-tree-data-api.md`, `SLICE-0009-maintree-loadtree.md` (Part A amendment) | Plan: `PLAN_TreeDataApi.md`. `GET /api/forexe/tree` (an/ss/include_hidden, base from session), nine `EXISTS` flags, POCO rewrite, tree load + nav gating. **Amended by 0009:** the SS filter now has an orphan escape (`EXISTS SS OR NOT EXISTS any indicators`) so zero-indicator angajamente stay visible. **No part of it has touched a real database** — all route tests are host-only and skip off-host |
| 0009 | `MainForm.LoadTree` (client half) + tree orphan escape | DONE (code) / UNVERIFIED on a live DB | `SLICE-0009-maintree-loadtree.md` | The brief's Parts B/C/D (DTOs, `GetTreeAsync`, `LoadTreeAsync` + gating) were **already shipped by 0008**; the real deltas are Part A (orphan escape on the server, see 0008 row) + its 2 host-only tests, and the 4 `GetTreeAsync` client tests 0008 never added (Api 26 → 30). Kept 0008's choices: mapping in the client (no `BuildTreeInfo`), token from session (no param), `IDDF As Long?` throughout. LoadTree is period-driven (runs on load + every An/SS change = the `SetPeriod` precondition) |
| 0010 | `KBotDataView` — owner-drawn unbound grid (Access continuous-form) | DONE (code) / **NO VISUAL VERDICT YET** | `SLICE-0010-01-…skeleton.md`, `-02-…virtualizare.md`, `-03-…tipuri-coloana.md`, `-04-…formatare-disable.md`, `-05-…input-selectie.md`, `-06-…editare.md`, `-07-…scroll-by-column.md` | Plan: pasted in-session (continuation plan supersedes the original where they disagree). Multi-pass. **0010-01 (skeleton):** models + enum, double-buffered `Control` implementing `IThemedControl`, theming cache + `ApplyTheme`, header + empty body, 4 child controls in Designer. **0010-02 (render+virtualize):** split into partials (`.Theming`/`.Layout`/`.Painting`), frozen + scrolling column bands, integer virtualization math, two-pass scrollbar sizing, Text + CheckBox cell painting, `CellFormatting`/`RowFormatting` plumbing with **reused** args, full palette→role mapping. **Decision:** kept in `KBot.Controls` + a `KBot.Theming` ProjectReference (no cycle) so it self-themes — the plan's "`KBotTheme` constants" don't exist (that's a ~9-slot Forexe façade). **0010-03 (column types):** Combo / OptionButton / Button / ProgressBar painting + `OptionGroup` exclusivity via `SetOptionValue`. **0010-04 (formatting+disable):** three-level effective-enabled (`IsCellEnabled`/`IsRowEnabled`, separate "probe" args so queries can't clobber an in-flight paint), disabled rendering across all six types, conditional formatting. **0010-05 (input+selection):** current cell + `SelectionChanged`, Access-style keyboard nav, click/double-click, toggle + button activation gated on `IsCellEnabled`, header-edge column resize. **0010-06 (editing):** floating Text/Combo editor, `CellValidating` (veto **and** value coercion), Esc discard, auto-commit on move/scroll, and the corrected `IsDirty` contract — API writes are *loading*, only operator edits/toggles dirty a row (`ClearDirty` added; 2 pass-01 tests deliberately rewritten). Virtualization proven **headlessly** (same painted-row count at 5,000 and 50,000 rows). 158 tests green. **Editing caught 2 real VB case-insensitivity bugs** where a parameter shadowed a same-named property and an unqualified assignment was a silent no-op: `ProposedValue` (commit always wrote Nothing) and — live since 0010-01 — `HeaderText`, meaning **every column header was Nothing**; both fixed with `Me.` + regression tests. ⚠️ **NOBODY HAS RUN THE VISUAL HARNESS for any of the six passes.** Scroll smoothness, the WinForms key→handler path, resize drag, floating-editor placement/focus and actual colours are all unverified — the blank-header bug shows exactly why that matters. Run: DevHarness → Controls/UI → «KBotDataView — virtualizare + temă (5.000 × 20)». **0010-07 (later add-on):** `ScrollByColumn` property — horizontal scroll snaps to column edges (direction-aware: a small step still advances a full column), covering thumb/arrows/wheel via one snap in the ValueChanged path; harness got a «Derulare pe coloană» checkbox. 7 tests. Next: Sumar slice consumes it read-only |

| 0011 | Sumar — endpoint + `SumarView` (prima vedere reală) | DONE (code) / **partially run on a live DB (0011-03)** / **NO VISUAL VERDICT** | `SLICE-0011-01-sumar-endpoint.md`, `SLICE-0011-02-sumar-view.md`, `SLICE-0011-03-sumar-join-clasificatii.md` | Plan: pasted in-session. Port of `qFX_MAIN_SUMAR` v1 → `GET /api/forexe/sumar?cod=…` (header hoisted once + one row per indicator), plus `SumarView` replacing `PlaceholderView` for the `sumar` key — **the first of the nine views that is real**, and the first consumer of `KBotDataView` (read-only). **The `Clsf` blocker was resolved by the operator supplying the real DDL, not by guessing:** `Clasificatii.Clsf` EXISTS as a STORED generated column `concat_ws('.',Capitol,Subcapitol,Articol,Alineat)` (indexed), so Branch A — select it directly. `Titlu = left(Articol,2)` explains Access's `Mid(Clsf,13,2)`. Port decisions: no SS filter, `ClasificatiiG`→`Clasificatii` / `ParteneriG`→`Parteneri`, all `IdUnitate` join predicates dropped, **`LEFT JOIN Clasificatii`** (was INNER — an indicator with no classification must still appear), `TotalReceptii = SUM(DIF)` not `Valoare`, `'Angajament nou.'` with the trailing period. Deliberate deviations: `COALESCE(...,0)` on the five totals, `cod` pushed into every aggregate (Access full-scanned per aggregate), deterministic `ORDER BY` added, `ROUND(,2)` kept on revizii/ordonanțări only. Client: `WithReauth` passed in specialized on `SumarInfo` so the 401 policy stays in the shell; snake_case stops at the wire DTOs; **stale-response guard** discards a superseded `cod`. 164 tests green, 0 warnings. **0011-03 (after the operator's live run) fixed three stacked defects in the query:** the join key WAS wrong (`FX_Indicatori.IdClsf` holds the Access id → matches `C.IdClsfAcc`, giving 0 rows against `C.IDClsf`); `IdUnitate` had been dropped from a SHARED nomenclator (67-row fan-out); and `Clasificatii` has real duplicates on `(IdClsfAcc, IdUnitate)` (still 50 rows with both predicates). Fix: `LEFT JOIN Clasificatii` → **scalar subquery with `LIMIT 1`**, `ORDER BY` moved to the output alias. **The same defect was found and fixed in `aggRev`/`Parteneri`, which the brief did not ask for** — there the join sat BEFORE the `GROUP BY`, so a duplicate partener multiplied `SUM(SA.ValCur)` and inflated `TotalRevizii` (a wrong money figure, not a blank column). 3 new tests incl. a general `len(rows) == indicator count` sentinel. ⚠️ **Still open:** (a) nobody has run `SumarView` on screen, and `KBotDataView`'s visual harness is STILL unrun; (b) the `Clasificatii` duplicates need triage — if the rows differ by `Sursa`/`Sector` then a join dimension is MISSING and `LIMIT 1` picks arbitrarily between two different classifications; if identical, they are data to clean |
| 0012 | Migrare Access → MariaDB — introspecție coloane pentru seed | 0012-01 DONE (code) / **NEVER RUN ON A LIVE DB** | `SLICE-0012-01-seed-columns.md` | `GET /api/forexe/seed/columns?db_name=&table=` în `routes/forexe/seed.py`, al treilea endpoint al fișierului. Gardă **`@require_api_key` (X-Api-Key, NU bearer)** ca celelalte două — seed-ul e condus de VBA/FOREXE legacy. `db_name` prin `_DBNAME_RE`, `table` prin aceeași `ALLOWED_TABLES`; niciun identificator din client nu ajunge în SQL fără allow-list. Întoarce **doar numele coloanelor, în ordinea din tabel** (`SHOW COLUMNS`), ca apelantul să construiască INSERT-uri pe poziție. **Tabel inexistent → `200` cu `columns: []`, NU 404** — apelantul trebuie să distingă «zero coloane» (migrarea nu a rulat) de o eroare de rețea/cheie. Ca să nu se adulmece errno 1146 dintr-o excepție, existența se testează întâi cu `SHOW TABLES LIKE %s` (parametrizat, nu aruncă) și abia apoi `SHOW COLUMNS`. Strict read-only: nicio scriere, niciun DDL, `conn.close()` în `finally`. 11 teste host-only. **Decizie blocată (varianta A, nedistructivă): `seed/schema` rămâne în cod dar utilitarul de migrare NU îl apelează** — tabelele `FX_` există deja în MariaDB cu DDL curat, iar `/schema` face `DROP TABLE IF EXISTS` înainte de `CREATE` din tipurile DAO (`LONGTEXT` pentru orice necunoscut), deci le-ar recrea mai slabe peste date reale. `/columns` există exact ca să nu fie nevoie de `/schema`. ⚠️ Neverificat: nimic nu a atins o bază reală; presupunerea că `FX_Rezervarii_IMG` NU e migrat pe `000_DEMO` (folosit ca tabel-lipsă în test) e a mea — testul se sare explicit dacă apare; contul de seed are nevoie de privilegiul `SHOW` pe baza țintă |

| 0013 | `KBotDataView` — auto-sizing coloane + moduri de umplere | DONE / **VISUALLY ACCEPTED** in harness playground | `SLICE-0013-column-sizing.md` | Plan: pasted in-session. Two grid-wide knobs run as ONE pass in `UpdateLayout` (before offsets/scrollbars): `AutoSizeColumnsMode` (`None`/`ToContent`, default `ToContent`) measures each visible column to `max(header, sampled cells) + padding` clamped to `[MinWidth, MaxWidth]`; `ColumnFillMode` (`None`/`FirstColumn`/`LastColumn`/`Proportional`, default `None`) then spends the leftover or absorbs the overflow so a fill mode never shows a horizontal scrollbar (except the honest `sum(MinWidth) > available` fallback). Model gains `MaxWidth` (default uncapped) + `Width` clamped `[Min,Max]` on every write + internal `UserSized` (drag pins a column; `ToContent` skips it, fill/shrink still applies; `ResetColumnSizing()` clears it). **New hard rule: all code comments added/touched this slice are in ENGLISH** (marked «English (slice 0013)»); the rest of `KBot.Controls` stays Romanian — no mass conversion. Measuring uses `TextRenderer.MeasureText` with the painter's fonts and the FORMATTED value (so `N2` money measures wide). Available width mirrors `UpdateScrollBars` exactly, vScroll visibility decided first (row-count only) → no circular dependency; `_inAutoLayout` re-entrancy guard. Limitations (in worklog): sampling (`AutoSizeSampleRows`, default 200, 0=all — a wider value further down ellipsizes), `CellFormatting` NOT raised while measuring (a handler that widens `Text` ellipsizes), the `MinWidth`-overflow fallback, and no column→grid back-reference (post-load column-property edits need an explicit `AutoSizeColumns()`; `AddColumn`/drag/resize/theme/`EndUpdate` cover the rest). 93 `KBot.Controls.Tests` green (15 new auto-size + 4 model), full solution green, 0 warnings. ⚠️ **Still unrun on screen** — like all of 0010/0011. Follow-up (separate): `SumarView` should adopt `ToContent` + `LastColumn`/`Proportional` and drop its hardcoded widths (Partener column removed separately) |

| 0014 | Rezervări — endpoint + `RezervariView` (a doua vedere reală) | DONE (code) / **NEVER RUN ON A LIVE DB** / **NO VISUAL VERDICT** | `SLICE-0014-rezervari-view.md` | Plan: pasted in-session. `GET /api/forexe/rezervari?cod=` (cititor brut, un rând per `FX_Rezervari`) + `RezervariView` (master/detail: arbore lună→(dată,tip) la stânga, grilă Clsf/CreditBug/Inițiale/Curentă/Definitive la dreapta) înlocuind `PlaceholderView` pentru `rezervari`. **Întrebarea deschisă a planului (§7.1, totalul lunar) e REZOLVATĂ din sursa Access, nu ghicită:** `qFX_REZERVARI_TREE` calculează `TOTALL = SUM(R_Valoare)` corelat pe (CodAngajament, lună, an) — candidatul (a). Valoarea frunzei = `SUM(IIf(EInitiala,R_Initiala,R_Valoare))` (Suma din `QFX_DDF_REZERVARI`); tipul derivat client-side (Inițială>Mărire>Micșorare). Clasificația: aceleași decizii ca Sumar 0011-03 — prin `FX_Indicatori` (join CodAI, `IdClsf`=id Access), subinterogare scalară `LIMIT 1` + predicat `IdUnitate` păstrat la nomenclator. `LEFT JOIN FX_Indicatori` (o rezervare nu dispare fără indicator). Iconițe GDI tematizabile (`RezervariIcons`), nu resurse binare. «+» = display-only (ridică `AdaugaDdfCerut`, fără abonat) — workflow-ul DDF (migration-plan item 7) e felie ulterioară. Plasa 401 + stale-guard ca la Sumar. Api 36→42 (+6 GetRezervari), App 22→30 (+8 view), 16 teste Python host-only (skip off-host). ⚠️ **Trei lucruri deschise:** (a) nimeni nu a văzut `RezervariView` pe ecran (+ harness-ul `KBotDataView` STILL unrun); (b) `test_forexe_rezervari.py` nerulat pe host — confirmă că cele opt tabele/coloane există și că join-ul prin `FX_Indicatori` dă Clsf populat; (c) cifrele exacte din screenshot (Ian 1.091.940 / …) NU au fost reproduse numeric (formula e din sursă, dar datele angajamentului lipsesc) |

| 0015 | Recepții — endpoint + `ReceptiiView` (a treia vedere reală) | 0015-01/02/03 DONE (code) / **NEVER RUN ON A LIVE DB** / **NO VISUAL VERDICT** | `SLICE-0015-01-receptii-view.md`, `SLICE-0015-02-receptii-root-tooltip.md`, `SLICE-0015-03-receptii-month-tree-revision.md` | Plan: `PLAN_ReceptiiView.md`. **0015-03 = revizuire operator (2026-07-22):** arbore pe **3 niveluri** (folder lună/an → recepție → antet, grupat pe DataR); LISTA **agregată la orice nivel** (click pe lună/recepție/antet → total Sum(DIF) + rând per clsf Sum(Valoare)); coloana „Descriere" = `Clasificatii.Denumire` (nou pe endpoint, drumul 0011-03) fiindcă agregă peste anteturi; **tooltip pe lună ȘI recepție** cu fereastra de plăți extinsă până la **prima recepție a lunii următoare** (nu ≤ MaxDataH). Api 48, App 39. `GET /api/forexe/receptii?cod=` (cititor brut: un rând per linie `FX_Receptii`, cu antet `H` + recepție `R` părinte, + array `plati` în același răspuns) + `ReceptiiView` (master/detail: arbore **2 niveluri** recepție→antet la stânga, grilă LISTA NrCrt/Descriere/Clsf/Valoare la dreapta) înlocuind `PlaceholderView` pentru `receptii`. **Cele trei furci ale planului sunt blocate din sursa Access, nu ghicite:** (1) arbore 2 niveluri (frunzele pe clsf din `Show_Receptii` sunt cod comentat — `AggregateRow`/`valAsoc` dormante); (2) LISTA condusă doar de antet (rând-total `Sum(DIF)` + rânduri per clsf `Sum(Valoare)`, din `qFX_MAIN_REC_LISTA_IND`); (3) un singur endpoint (nu tree/lista/tooltip separate). Clasificația: același drum ca Sumar 0011-03 (`FX_Indicatori`, `IdClsf`=id Access, subinterogare scalară `LIMIT 1` + `IdUnitate` păstrat la nomenclator, `LEFT JOIN` ca o linie să nu dispară). Iconițe GDI de stare (sus/jos/neutru, finding 5) tematizabile. Plasa 401 + stale-guard ca la Sumar/Rezervări. Api 42→48 (+6 GetReceptii), App 30→37 (+7 view), 15 teste Python host-only (skip off-host). **Devieri documentate:** chei de fir snake_case (nu PascalCase-ul schiței); coloana grilei „Descriere" = Descrierea ANTETULUI (`FX_Receptii_H.Descriere`, din `Nz([HH]![Descriere],…)`), nu a indicatorului; `sters_h` adăugat în rând pentru cumulul DIFH din tooltip (0015-02). ⚠️ **Deschis:** (a) `test_forexe_receptii.py` nerulat pe host; (b) `ReceptiiView` niciodată pe ecran; (c) **pass 0015-02 (tooltip de recepție `NewRootPlatiTooltip`) nefăcut** |

| 0016 | `KBotDataView` — coloane auto-hide (responsive) | DONE (code) / **NO VISUAL VERDICT** | `SLICE-0016-kbotdataview-auto-hide-columns.md` | New per-column `Column.AutoHide`: a column may disappear when the grid would otherwise need a horizontal scrollbar. The fit pass (in `PerformAutoSize`, after measure, before fill) hides auto-hideable columns **rightmost-first** until the rest fit or none remain; if none remain, the scrollbar appears normally (with auto-hide engaged, unresolved overflow does **not** shrink the survivors — that overrides slice-0013 `ShrinkToFit`). Auto-hidden state is a separate `Friend AutoHidden` flag (never touches the caller's `Visible`), recomputed every layout so a widened grid brings columns back; `IsEffectivelyVisible = Visible AndAlso Not AutoHidden` replaces the two column-visibility reads (`RecalcColumnLayout`, `VisibleColumns`) so paint/hit-test/nav/auto-size all inherit it. **Interaction with the stretch column:** the `ColumnFillMode` First/Last target is the "expanding column" — it's **never** auto-hidden (a column that is both `AutoHide` and the fill target stays and expands: stretching wins), and once a column disappears the expander absorbs the freed gap (the leftover branch). `Proportional`/`None` have no single protected target. **Interpretation flagged:** there is no per-column stretch flag in the codebase — "the property to stretch a column" was read as the existing grid-wide `ColumnFillMode`; if the operator meant a per-column `Stretch`, only the target designation changes. Controls 120 tests (+9), full solution 243 green, 0 warnings. DevHarness got a «Ascunde coloane la nevoie» checkbox. ⚠️ **Unrun on screen** like all of 0010/0013; minor pre-existing edge (editor left floating if a resize hides the edited column) noted in the worklog |

| 0017 | Plăți — endpoint + `PlatiView` (a patra vedere reală) + `KBotDataView` totals row | 0017-01..04 DONE (code) / **0017-04 VĂZUT PE ECRAN ȘI ACCEPTAT 2026-08-13** (prima acceptare vizuală a vederii Plăți) / **NEVER RUN ON A LIVE DB** | `SLICE-0017-01-kbotdataview-totals-row.md`, `SLICE-0017-02-plati-endpoint.md`, `SLICE-0017-03-plati-view.md`, `SLICE-0017-04-plati-tree-doua-niveluri.md` | Plan: pasted in-session. Four passes: **01** `KBotDataView` pinned totals row; **02** server `GET /api/forexe/plati?cod=`; **03** `PlatiView`; **04** fix Rezervări «+» (one leaf, not all). **0017-03 (DONE):** a patra vedere reală (master/detail: arbore 3 niveluri `« TOATE PLĂȚILE »`/lună/zi/plată la stânga, grilă LISTA sus-dreapta cu rând de totaluri, panou extras bancar jos-dreapta) înlocuind `PlaceholderView` pentru `plati`. LISTA e **filtru, nu agregat** (click pe nod → exact rândurile lui). Panoul de detaliu e condus de selecția din grilă, din datele deja pe rând (fără al doilea apel). «+» pe EXACT o zi (cea mai veche cu `AreOrd=False`) + luna ei + nodurile de nivel 2 ne-ordonantate; nimic altundeva. Reînvie codul dormant Level=2 din Access. **Asumpții notate (fără sursă Access pentru lună/zi merjate):** iconița stării = orice `Incarcat`→sus / orice `Preluat`→jos / neutru; verde INCASARE doar când TOATE rândurile zilei/lunii sunt INCASARE (per-rând la nivel 2). «+» single-tint (Access nu are variantă verde pentru plăți). `PlatiInfo` (`PlataRow`/`ExtrasBancar`) + `GetPlatiAsync` + `PlatiIcons` (up/down/neutru/«+»/«toate») + events dormante `AdaugaOrdonantari(Cerut)`. Api 48→53 (+5), App 39→50 (+11), soluție 0 warnings. **0017-02 (DONE):** `GET /api/forexe/plati?cod=` (cititor brut, un rând per `FX_Plati`, cu extrasul bancar `FX_Extrase` purtat pe rând + `are_ord`). **Deviere de la SCHIȚA planului:** clasificația prin `FX_Indicatori` (join CodAI, PK→1:1) + subinterogare scalară `Clasificatii` pe `IdClsfAcc=I.IdClsf` `LIMIT 1` (drumul 0011-03), NU `p.IdClsf` — rezolvat de nota Step 0 a planului («match receptii.py»); se întorc și `clsf`/`denumire` (nomenclator) și `clsf_plata` (brut, fallback). `LEFT JOIN FX_Extrase` (nu INNER pe H ca Access) — o plată fără extras nu dispare. `are_ord` contra unui derivat `DISTINCT` (o plată pe mai multe linii de ordonantare nu duplică rândul). `ORDER BY Data_plata, IdPlataFX`. `data_doc` e TEXT (string brut, nu ISO). 15 teste Python host-only (skip off-host), inclusiv garda anti-fan-out `count(plati)==count(FX_Plati)`. Suite offline 75 passed / 13 skipped, py_compile curat. **0017-01 (DONE):** reusable totals band in `KBot.Controls`, NOT special-cased for Plăți. New `KBotAggregate {None,Sum,Count,Average}`; `KBotDataColumn.Aggregate`/`AggregateFormatString`; `KBotDataView.ShowTotalsRow`/`TotalsRowHeight`. Pinned band between body and h-scroll, header band styling (reused `_bHeaderBack`/`_pHeaderSep`/`_pHeaderBaseline`/`HeaderFont`), frozen + h-scroll layering mirrored from `DrawHeader`. Aggregates over ALL rows: Sum/Average skip Nothing+non-numeric, Average of nothing = empty (not 0/NaN), Count = rows with `HasValue` (not non-empty cells). NOT a row (excluded from RowCount/nav/hit-test/dirty). `ViewportHeight`+`UpdateScrollBars`+`WillVScrollBeVisible` all subtract the band; totals text participates in auto-size measurement (chosen, so a wide total can't ellipsize). **Deviation:** `AddRow` recompute is one step behind (cells set after AddRow returns), so the control's per-cell `Item` setter recomputes too; bulk loads via `KBotDataRow` inside Begin/EndUpdate recompute once at EndUpdate. Controls 120→134 tests (+14), 0 warnings. DevHarness got a «Rând de totaluri» checkbox. ⚠️ **Band never rendered on screen; `KBotDataView` visual harness STILL unrun** (open since 0010) — frozen alignment only smoke-tested headless. **0017-04 (DONE, VĂZUT PE ECRAN):** activarea vederii arunca `ArgumentException: Cheie de coloană duplicată: 'clsf'` — o trecere prin designer autorase toate cele cinci coloane, iar `BuildColumns()` le adăuga a doua oară; `BuildColumns` șters, designerul deține coloanele. Arborele a coborât de la patru straturi la **DOUĂ**: rădăcină lună → frunză ZI (toate plățile zilei într-un nod); `« TOATE PLĂȚILE »` și nodul per `IdPlataFX` au dispărut, LISTA rămâne filtru. Iconițele vin din `image_list` (`month` pe lună, `up`/`down` pe starea merjată a zilei); luna nu mai poartă stare și nu mai e verde (ca în Access: folder, nu stare). Lunile pornesc strânse **în afară de cea cu «+»** (abatere deliberată de la Access, cerință operator). ⚠️ **Defect găsit pe drum:** o regenerare a lui `InitializeComponent` prin designer ștersese cele zece apeluri `InitDetailPair`, deci panoul de detaliu ieșea GOL, tăcut — cablarea s-a mutat în `BuildDetailRows()` din constructor. `image_list` n-are încă cheile `neutru`/`plus` (cad pe GDI); `nrdoc`/`data` sunt `ValueType.DateTime` în designer dar `FillGrid` scrie șiruri în ele (probabil scăpare, lăsat neatins) |

| 0018 | `KBotNavList` — separatori + orientare + aliniere Near/Far + vizibilitate; gating MainForm prin ascundere | DONE (code) / **NO VISUAL VERDICT** | `SLICE-0018-navlist-separators-orientation-visibility.md` | Four capabilities added to `KBotNavList` (`KBot.Theming`): (1) **separatori** — `AddSeparator(Optional align)`, oricâți, desenați ca linie fină (`Palette.BorderColor`), neselectabili, săriți de mouse/tastatură; cheie internă auto (`__sep_N`) ca să nu colideze cu `FindIndex`; (2) **orientare** — `Orientation` (`KBotNavOrientation.Vertical` implicit / `Horizontal`); orizontal măsoară lățimea butonului din text, centrează textul și remapează navigarea pe Stânga/Dreapta; (3) **aliniere Near/Far** — enum nou `KBotNavAlign` pe `AddItem(key,text,align)` și `AddSeparator(align)`; `Far` ancorează grupul **jos** (vertical) / **dreapta** (orizontal); (4) **vizibilitate** — `SetItemVisible(key,visible)`, un buton ascuns nu ocupă spațiu, nu se pictează, nu se selectează, e sărit de tastatură (rezolvă firul deschis „`KBotNavList` has no `SetItemVisible`"). Layout-ul e acum un singur `RecalcLayout` (invalidat la add/vizibilitate/orientare/resize/font), nu vechiul `index * height`; grupul Far se așază de la `capăt - extindereTotală`. **`MainForm`:** DDF/ORD mutate în grupul `Far` cu `AddSeparator(Far)` deasupra (desprinse la baza barei); **`ApplyViewGating` trecut de la `SetItemEnabled` la `SetItemVisible`** — un flag `Are*=FALSE` acum **ASCUNDE** butonul, nu doar îl dezactivează (cererea operatorului). Fallback-ul la `sumar` când vederea activă dispare rămâne. `KBot.Theming` + `KBot.App` compilează curat, 0 warnings. ⚠️ **Fără verdict vizual** — verificat doar prin compilare; harness-ul controalelor UI rămâne nerulat (ca 0010+); ~~**fără teste xUnit pentru `KBotNavList`** (nu existau înainte) — separator/vizibilitate/aliniere/orientare neacoperite de teste~~ **REZOLVAT în felia 0025** (`KBotNavListTests.vb`, 20 de teste: separatori + chei `__sep_N`, ordinea Near/Far verificată geometric, «ascuns ⇒ `Rectangle.Empty`, sărit de `IndexAt` și de navigarea cu tastatura»). Rămâne fără verdict vizual |

| 0019 | XFA_WRITTER → `KBot.Xfa` (librărie în proces, nu exe separat) | DONE (code) / **NEVER RUN LIVE** / **NO VISUAL/PDF VERDICT** | `SLICE-0019-xfa-writter-library.md` | Portat exe-ul `XFA_WRITTER` din `Surse/SURSA_XFA_WRITTER` în `src/KBot.Xfa` ca **librărie apelată în proces** — K-BOT nu mai lansează un `.exe` separat cum făcea Access prin `WScript.Shell.Run` + cod de ieșire. API public = **`XfaWriter`** (fațadă ce înlocuiește `Program.Main`): `Genereaza` / `GenereazaSiSemneaza` / `Semneaza` / `ExtrageAtasamente`, întorcând un **`XfaResult`** (`Reusit`, `Semnat`, `Masca`, `SemnatAB/CD/Ordonator`, `CaleOutputPdf`, + `CodIesireLegacy` 0/2/11..17 pentru compatibilitate). `docType` ∈ {DDF, ORD}. Motorul `AdobeUtils` (iTextSharp 5.5.13.3 `PdfReader`/`PdfStamper`/`XfaForm`): completează XFA din XML, embedează atașamente base64, deschide Adobe și **așteaptă** semnarea (fără timeout, `WaitForExit`), calculează masca semnatarilor din handler-ul `.pdf` din registry (+ validare că e Adobe). `TemplateDownloader` = cache DDF/ORD de pe API-ul **legacy** (`Configs`: adcredit.avatarsoft.ro:5008 + `X-API-KEY`, `c:\avacont\cache`) — separat de API-ul K-BOT bearer. **Adaptări la regulile casei:** fără bloc `Namespace` (`AdobeUtilsNS` scos); `Logger`→modul `XfaLog` (fără `AllocConsole`, trasare în `C:\AVACONT\logs`); granițele loghează în `GlobalErrorLog` + rearuncă, deci `KBot.Xfa` referă acum `KBot.Common` (fără ciclu — Common referă doar Domain); comentarii/șiruri românești. **Două capcane rezolvate:** (1) `Option Strict On` rupe `PdfStamper(…, "\0", True)` original → `ChrW(0)` (sentinela iTextSharp „păstrează versiunea machetei"; exe-ul pe Option Strict Off trimitea de fapt un backslash); (2) shadowing VB case-insensitive — Sub `DeschidePdf` vs param `deschidePdf` → Sub redenumit (aceeași capcană ca la 0010/`ProposedValue`/`HeaderText`). **Modurile cu semnare BLOCHEAZĂ** (deschid Adobe) → din UI rulează-le pe thread de fundal. iTextSharp/BouncyCastle sunt pachete .NET Framework pe net8.0-windows → NU1701 e așteptat, rulează corect (aceeași versiune ca exe-ul). `FileVersion` 1.0.0.0→1.1.0.0. **Cum se rulează din K-BOT:** DevHarness → categoria **XFA** (`XfaWriterHarnessTest` în `KBot.App/HarnessTests`, doar Debug, descoperit prin scanarea assembly-ului de intrare) — DOAR logica pură offline (separare atașamente base64 + clasificare mască ORD/DDF). Proiect `tests/KBot.Xfa.Tests` xUnit adăugat (**39 teste**: `ExtrageAtasamente` + `MascaSemnatari` ORD/DDF + `XfaResult` coduri 0/2/11..17), adăugat în `KBot.sln`; soluție întreagă verde (0 erori). ⚠️ **Deschis:** (a) generarea completă a PDF-ului cere macheta de pe serverul legacy — niciodată rulată live; (b) semnarea cere Adobe ca handler implicit `.pdf` — neverificată; (c) niciun PDF real nu a fost produs/inspectat; (d) testele acoperă DOAR logica pură (separare atașamente + mască) — motorul XFA propriu-zis (`ModifyXfaFromXml`, clonare `SubformInf`, embed) cere o machetă PDF reală, neacoperit; (e) vederile editor DDF/ORD rămân amânate (vor apela `XfaWriter`) |

| 0020-01 | DDF — endpoint `GET /api/forexe/ddf` + client `GetDdfAsync` | DONE (code) / **NEVER RUN LIVE** | `SLICE-0020-01-ddf-endpoint.md` | Pasul 1 din felia 0020 (vederea DDF), după `PLAN_DdfView.md` §3–§4. **Un endpoint, trei liste, un singur drum dus-întors**: `{cod, antet[], revizii[], linii[]}` — clientul filtrează LOCAL (arbore/grilă/combo), deci un clic în arbore nu mai cere nimic de la server (decizia 7). **`antet` e ARRAY, nu obiect:** PK-ul `FX_DDF` e compus `(IDDF, CUAL)` și nu există constrângere unică pe `CodAngajament` (verificat în `000_DEMO.sql:161`); serverul loghează când sunt >1 rânduri, iar `DdfInfo.AntetDeLucru(iddfPreferat)` face alegerea EXPLICITĂ (potrivire pe `AngajamentTreeInfo.IDDF`, altfel primul) — niciodată tacită. **`TotalRevizie` = `SUM(ValCur)` REAL** prin subinterogare scalară: Access aliază `SA.ValCur AS TotalRevizie` (valoarea UNEI linii purtând numele unui total) și `Show_Revizii` reia aceeași frunză `"RC_" & IDREV` o dată per linie, afișând una arbitrară — aceeași familie de defect ca fan-out-ul `aggRev` din 0011-03; `COALESCE(…,0)` păstrează vizibilă revizia fără linii de secțiune A (Access o șterge prin `INNER JOIN`). **`Clsf` NU are nevoie de tratamentul `IdClsfAcc`+`IdUnitate` din 0011-03:** confirmat din `routes/ddf/sync_acc_mdb.py:7-8` (`Access.IdClsfPY → MariaDB.IdClsf`) + constrângerea `FX_DDF_REV_SA_ibfk_4 FOREIGN KEY (IdClsf) REFERENCES Clasificatii (IDClsf)` — pe ACEASTĂ tabelă `IdClsf` ține deja CHEIA PRIMARĂ a nomenclatorului (invers față de `FX_Indicatori`), deci unicitate prin definiție, fără fan-out și **fără predicat `IdUnitate`**; se preferă coloana denormalizată `FX_DDF_REV_SA.Clsf`, cu cădere pe nomenclator doar când e goală. **ABATERE DE LA PLAN:** §3 cerea `revizii` ca `FX_DDF_REV` *joined to its* `FX_DDF` header — NU s-a făcut: cu PK-ul compus, un `INNER JOIN … ON IDDF` multiplică fiecare revizie cu numărul de rânduri `CUAL`; `revizii` și `linii` filtrează prin `IN (SELECT …)`, deci „un rând per `IDREV`" rămâne o proprietate a FORMEI interogării, nu a unui `GROUP BY` editabil ulterior (pin de regresie: `test_second_cual_row_does_not_fan_out_revisions`). `ParametriiFund` e pe `linii` de la început (cerut de §8 bis pentru `Cell4` în pasul 05), deși grila nu-l arată (decizia 4). POCO-uri noi în `KBot.Domain` (`DdfInfo.vb`): `DdfAntet.FolderPdf`/`NormalizeazaNume` (convenția partener vs `GENERAL`, `\W+`→`_`), `RevizieRow.EtichetaRevizie` (§2.6: `Format(NumarRev,"@@@")` e format TEXT → `PadLeft(3)` cu SPAȚII, niciodată `D3`/`000`), `AntetDeLucru`. Cele patru `FakeApiClient` din `tests/KBot.App.Tests` au primit `GetDdfAsync`. **Teste:** .NET **334 passed / 0 failed** (Api 61, App 50, Controls 134, Theming 27, Xfa 39, Domain 17, Common 7, LocalStore 1), build 0 erori (16 warning-uri NU1701 preexistente); Python offline **75 passed / 14 skipped / 0 failed**. ⚠️ **Deschis:** (a) endpoint-ul **nu a rulat niciodată pe o bază reală** — toate cele 17 teste DDF sunt host-only și au fost văzute DOAR sărind; (b) **§3 punctul 5 — numărul de `CodAngajament` cu mai multe antete `FX_DDF` este ÎNCĂ NECUNOSCUT** (`test_report_duplicate_cod_angajament_count` e o CONSTATARE care nu pică niciodată; până rulează pe host, §2.7 rămâne teoretic-sau-real nedecis); (c) `data_creare`/`data_def` sunt trunchiate la zi; (d) ambiguitatea `Program` (§2.9) rămâne o întrebare de pasul 05; (e) `Tip`/`Semnatura` se poartă dar nu se afișează |

| 0020-02 | DDF — `DdfView` (arbore + sub-navigare orizontală + grilă + combo clasificații) | DONE (code) / **NO VISUAL VERDICT** / **NEVER RUN LIVE** | `SLICE-0020-02-ddf-view.md` | Pasul 2 din felia 0020, după `PLAN_DdfView.md` §5. **A CINCEA vedere reală** (după Sumar 0011, Rezervări 0014, Recepții 0015, Plăți 0017) — `MainForm.CreateView("ddf")` nu mai întoarce `PlaceholderView`. Arbore de revizii pe DOUĂ niveluri la stânga; la dreapta o `KBotNavList` **ORIZONTALĂ** (decizia 8, folosind `Orientation = KBotNavOrientation.Horizontal` adăugat în 0018) peste trei pagini afișate lazy prin `Visible` (**NU un `TabControl`**): «Valori» / «Vizualizare» (felia 03) / «Fișiere» (felia 04). Un SINGUR `GetDdfAsync` per `SetContext`, prin `WithReauth(Of DdfInfo)` + stale-guard `_requestedCod`; **fiecare click în arbore și fiecare schimbare de combo filtrează date deja în memorie — ZERO cereri de rețea** (decizia 7). **Arborele:** rădăcini de lună (`LA_{yyyy}_{M}`, grupate pe an+lună din `DataRev`, cronologic, EXPANDATE) -> frunze de revizie (`RC_{IDREV}`, caption `{NumarRev.PadLeft(3)} - {dd.MM.yyyy}`); iconița frunzei `Incarcat`->sus / `Preluat`->jos / altfel neutru (`REV_SUS`/`REV_JOS`/`REV_NOT`); tooltip = `Desc_Scurta`; `Tag` = `DdfNodeRows` (liniile nodului + dacă e rădăcină + `RevizieRow`-ul frunzei, de care feliile 03/04 au nevoie pentru calea PDF). **Grila:** `Clsf`, `ElementFund` (SINGURA coloană cu `AutoHide`), `Data reviziei` (**doar la nivel de rădăcină**), `ValPrec`, `ValCur`, `ValTot`; rând de totaluri cu **Sum DOAR pe `ValCur`** (decizia 5); fără `Clasificatii.Denumire` (decizia 4); ordonare rădăcină `Clsf, DataRev` / frunză `Clsf`. **Combo-ul de clasificații** se reconstruiește din valorile distincte ale nodului, cu `< Arată toate clasificațiile >` prima și selectată — **NECONDIȚIONAT la fiecare click** (decizia 6). **ABATERI DELIBERATE de la Access (fiecare cu test):** (1) valoarea rădăcinii de lună = suma reală a frunzelor, Access trimite literalul `0` în `AddTree_Root`; (2) o rădăcină e roșie DOAR când propriul total e negativ — Access face `cRoot.foreColor = cNode.foreColor`, deci culoarea depinde de ultima frunză procesată (accidental); (3) o revizie fără linii de secțiune A RĂMÂNE în arbore cu total `0,00` (Access o șterge prin `INNER JOIN`). **DOUĂ INEXACTITĂȚI ALE PLANULUI, găsite citind fișierele reale:** (a) §5 spune că iconițele frunzelor vin „prin `FxIcons`" — IMPOSIBIL: `FxIcons` încarcă resurse EMBEDDED cheiate pe `Stare`a unui angajament, fără desen GDI tintat din paletă; tiparul descris efectiv (GDI, tintat, fără resurse binare) trăiește în `RezervariIcons`/`PlatiIcons`/`ReceptiiIcons`, câte o clasă per vedere -> s-a adăugat `DdfIcons`; (b) **§8 punctul 5 se ÎNCHIDE**: `AutoHide` pe o coloană care NU e ultima funcționează — `PerformAutoHide` (`KBotDataView.AutoSize.vb:176`) iterează dreapta->stânga dar sare coloanele fără `AutoHide`, iar singura protejată e ținta de umplere, care sub `ColumnFillMode.LastColumn` e `ValTot`, nu `ElementFund`; coloana NU trebuie mutată. **Teste:** .NET **355 passed / 0 failed** (App 69, Api 61, Controls 134, Theming 27, Xfa 39, Domain 17, Common 7, LocalStore 1), build **0 erori, 0 warning-uri BC**. 19 teste headless STA noi. **Bug de harness prins de teste:** `FindControl(Of ComboBox)` (depth-first) găsea ComboBox-ul flotant de EDITARE al lui `KBotDataView` în locul filtrului `cboClsf` -> căutare pe NUME (`FindByName`, helper-ul pe care `PlatiViewTests` îl are deja exact pentru asta); codul de produs nu era vinovat. ⚠️ **Deschis:** (a) **FĂRĂ VERDICT VIZUAL** — vederea nu a fost VĂZUTĂ niciodată pe ecran (layout, lățimile măsurate ale navigării orizontale, auto-ascunderea la lățime reală = neobservate; `AutoHide` e argumentat din citirea codului, nu din a vedea o coloană dispărând); (b) niciodată rulată pe bază reală — a văzut doar obiecte `DdfInfo` construite de mână; (c) `_antet` se încarcă și se ține dar NU e încă folosit (poartă `CUAL`/`PartAng`/`NumePartener` pentru feliile 03/04 — intenționat, nu cod mort); (d) calea cu MAI MULTE antete (log + alegere pe `_preferredIddf`) nu are test, fiindcă §3 punctul 5 încă nu ne-a spus dacă acest caz există în date reale; (e) `pnlPreview`/`pnlFisiere` sunt coji goale — textul „va fi disponibilă într-o etapă următoare" din Fișiere TREBUIE înlocuit în felia 04, nu livrat așa; (f) nimic nu dezactivează Vizualizare/Fișiere când nu există document |

| 0020-03 | DDF — două suprafețe de previzualizare (`XfaXmlPreview` implicit + `ReaderHostPreview` rezervă) | DONE (code) / **NO VISUAL VERDICT** / **ReaderHost NERULAT** | `SLICE-0020-03-ddf-preview.md` | Pasul 3 din felia 0020, după `PLAN_DdfView.md` §6 și §11. **AMBELE** implementări construite; niciuna cod mort. Selecția = o singură constantă la COMPILARE în `DdfPreviewFactory` (`Private Const Mode = DdfPreviewMode.XfaXml`) + un `Select Case` — deliberat FĂRĂ comutator la rulare. **`IDdfPreview`** (contract): `Surface`, `ShowDocument(pdfPath, exists)`, `Clear()`, `GenerateRequested` — `DdfView` nu știe nimic despre Adobe/XFA/handle-uri. **`XfaXmlPreview`** (implicit, opțiunea C): redă din XML-ul XFA — siblingul `.xml` întâi (`Path.ChangeExtension(pdf, ".xml")`), altfel `PdfXmlExtractor.TryExtract` pe PDF; trei stări (conținut = antet + `KBotDataView` linii secțiune A + notă / «document lipsă» / mesaj); NU pixel-identic cu PDF-ul tipărit (compromisul acceptat §6). **`DdfXfaModel` + `DdfXfaParser`** (pure, testabile): parsează arborele `form1` pe LOCAL-NAME (merge și pe `form1` brut și pe cel embedat `xdp:xdp/xfa:data/form1`), sărind primul `Row1` fictiv exact ca JS-ul machetei — SINGURA parte testabilă a pasului. **`ReaderHostPreview`** (rezervă, opțiunea A): `Process.Start` pe PDF, `EnumWindows` după o fereastră Adobe (clasă cu `Acrobat`) al cărei titlu conține numele fișierului, timeout 8 s, apoi `SetParent` în panoul-gazdă cu `WS_CAPTION`/`WS_THICKFRAME`/`WS_POPUP` curățate + `WS_CHILD`, re-așezată la resize; la dispose și la fiecare document restaurează părintele/stilul și închide procesul pornit (fără orfani); dacă nu găsește fereastra, cade pe `Process.Start` simplu și o spune; `Get/SetWindowLongPtr` dispecerizate 32/64-bit. **`DdfView`** montează `DdfPreviewFactory.Create()` în `pnlPreview`, se abonează la `GenerateRequested` (stub ce loghează `NotImplementedException` — felia 05), cascadează tema, `Clear()` la click pe rădăcină. **CONTRAZICE PLANUL:** §0/§8bis/DoD presupun o machetă DDF `.xml` reală în repo pentru randare/validare — **`ddf_demo.xml` și `ord_demo.xml` sunt IDENTICE la byte (același MD5 `9e5fe957…`)**: fișierul numit `ddf_demo.xml` e o COPIE a machetei ORD, nu un DDF real. Setul de elemente DDF a fost derivat din `mdl_FX_DDF_PDF.GenereazaXML_PentruPython` (sursa autoritativă, ce va fi portată în felia 05). **Teste:** .NET **371 passed / 0 failed** (App 85, Api 61, Controls 134, Theming 27, Xfa 39, Domain 17, Common 7, LocalStore 1), build 0 erori / 0 warning-uri BC. 9 teste pure de parser + 7 teste headless STA de stare (inclusiv calea reală de randare din siblingul `.xml` prin fișier temporar). ⚠️ **Deschis:** (a) **FĂRĂ VERDICT VIZUAL** — iar pentru `ReaderHostPreview` niciunul NU e posibil aici: calea de găzduire Win32 (`EnumWindows`/`SetParent`/curățare stiluri/resize/detach) NU a rulat NICIODATĂ și nu se poate exercita headless sau fără Adobe; cea mai puțin verificată bucată din toată felia; (b) **niciun DDF sample real în repo** — vezi firul deschis nou; DoD «se potrivește structural cu un `.xml` Access cunoscut» e IMPOSIBIL de îndeplinit din repo, cere un artefact capturat live; (c) legarea încrucișată (frunză -> cale PDF -> `ShowDocument`) e felia 04 (cere `KBotPaths.DdfPdfRoot` + `CUAL`/`PartAng`); previzualizarea e inertă până atunci; (d) `GenerateRequested` e stub — felia 05 pune apelul `XfaWriter.Genereaza` pe thread de fundal; (e) focus/tastatura peste granița de proces la `ReaderHostPreview` = documentat ca ne-nativ, nereparat |

| 0020-04 | DDF — `KBotPaths` + browser de fișiere + legare încrucișată frunză↔previzualizare | DONE (code) / **NO VISUAL VERDICT** | `SLICE-0020-04-ddf-file-browser.md` | Pasul 4 din felia 0020, după `PLAN_DdfView.md` §7. **`KBotPaths`** (`KBot.Common`): configurație JSON lângă executabil (`<AppDir>\kbot_paths.json`), o singură proprietate — `DdfPdfRoot`, implicit `C:\AVACONT\FOREXE\PDF\DDF\` (decizia 13); lipsă/gol/stricat -> implicit + log, **niciodată nu aruncă la pornire**; `Load(dir)` testabil (ocolește singleton-ul `Current`). **NU e o reînviere a lui `AppConfig`** (acela ținea adresa serverului, care rămâne hardcodată în `ApiOptions`; `KBotPaths` ține doar locații de fișiere); formularul de config ce le va edita e o felie ulterioară. `KBot.Common` `FileVersion` 1.0.0.0->1.1.0.0. **`DdfPdfLocator`** (pur, testabil): convenția §2.5 (`<root>\<partener|GENERAL>\DDF_NR_{CUAL}_REV_{NumarRev}_{Cod}.PDF`) + enumerarea recursivă `*.pdf` filtrată pe `_{CodAngajament}.PDF` — prinde `GENERAL\` ȘI fiecare folder de partener fără a hardcoda vreunul, exclude siblingii `.xml` prin construcție (globează doar `*.pdf`), nu atinge `PDF\ORD\` (în afara rădăcinii); sortat descrescător după Modificat; rădăcină lipsă -> listă goală (fără throw, fără a crea folderul). **`DdfFileBrowser`** (`KBotDataView`): coloane Folder/Nume/CUAL/Rev./Dimensiune/Modificat; rădăcină lipsă -> mesaj care NUMEȘTE calea configurată; selecția unui rând ridică `FileActivated(fullPath)`. **Legare încrucișată în `DdfView`** (§7, o singură suprafață `IDdfPreview`, două puncte de intrare): frunză din arbore -> `DdfPdfLocator.ExpectedPath(KBotPaths.Current.DdfPdfRoot, _antet, NumarRev)` (aici `_antet` — încărcat în pasul 02 — e în sfârșit folosit) + `File.Exists` ca flag; rădăcină -> golește previzualizarea; rând din browser -> aceeași previzualizare + comută pe pagina «Vizualizare». Șirul-placeholder «va fi disponibilă într-o etapă următoare» de pe `pnlFisiere` (firul deschis din 0020-02) e ELIMINAT. **Teste:** .NET **391 passed / 0 failed** (App 103, Api 61, Controls 134, Theming 27, Xfa 39, Domain 17, Common 14, LocalStore 1), build 0 erori / 0 warning-uri BC. 7 teste `KBotPaths` + 10 `DdfPdfLocator` + 5 headless `DdfFileBrowser` + 2 de legare în `DdfViewTests`. Un test a prins o subtilitate WinForms reală (`Control.Visible` propagă False de la un ancestor ascuns) -> testul încarcă date întâi; produsul nu era vinovat. ⚠️ **Deschis:** (a) **FĂRĂ VERDICT VIZUAL** — coloane/mesaje/comutare pagină verificate doar headless; (b) `KBotPaths.Current` (singleton) NU e testat direct, doar `Load(dir)`; (c) legarea încrucișată e testată pe arbori temporari, dar niciun layout real `C:\AVACONT\FOREXE\PDF\DDF\` nu a fost citit și nicio previzualizare dintr-un PDF real generat nu a fost văzută (așteaptă pasul 05); (d) `GenerateRequested` rămâne stub (pasul 05) |

| 0020-05 | DDF — generarea PDF (părțile ne-blocate) + `DdfXmlBuilder` + pasul 05-00 (client) | DONE (code) / **NEVER RUN LIVE** / **NO PDF VERDICT** | `SLICE-0020-05-ddf-generate.md` | Pasul 5 din felia 0020, după `PLAN_DdfView.md` §8 bis. **Decizie operator:** se construiesc părțile care NU depind de descărcarea live a machetei; verificarea live a pasului 05-00 + verdictul PDF-ului real rămân pentru o rulare pe host. **Pasul 06 (semnare) AMÂNAT la felia 0021.** **05-00 (client):** `Config.BASE_URL` `http://adcredit.avatarsoft.ro:5008/api/` -> `https://kbot.avatarsoft.ro/api/`; comentariul învechit corectat (`mfp_bp` e în ACEEAȘI aplicație Flask — `main.py:52` — nu pe un „server vechi"); `KBot.Xfa` `FileVersion` 1.1.0.0->1.2.0.0; `X-API-KEY` RĂMÂNE (amânare deliberată, fir deschis). **Server confirmat offline:** `routes/mfp.py:177` servește `/api/mfp/template_ddf` pe **GET și HEAD** sub `@require_api_key`; GET/HEAD live peste 443 (+ ruta nginx `/api/mfp/*` pe vhost-ul 443) = NEVERIFICAT (rularea host a pasului 05-00). **Endpoint extins:** `linii` primește `ss` (`COALESCE(NULLIF(SA.SS,''), Clasificatii.SS by IDClsf)` — MariaDB `Clasificatii` NU are `CodSSI`, are `SS` generat); `sectiuneb`+`atasamente` OPT-IN printr-un SINGUR flag `?pentru_generare=1` (§2.8 — ABATERE de la planul care cerea `?atasamente=1` doar pentru atașamente). **`DdfXmlBuilder`** (pur, testabil — nestemata pasului): port FIDEL al `GenereazaXML_PentruPython` + `GenereazaXML_NOTAFD` + `InsereazaAtasamente`, per revizie. Fidelități portate + testate: `ToXmlNum` (virgulă->punct, fără zerouri), omiterea nodului-text gol, rândul fictiv sărit, `Int()`=floor, lățimile `Left(…,N)`, §2.9 (program din globalul de sesiune). **DOUĂ ABATERI DELIBERATE (documentate):** (1) elementele NOTAFD sunt CALIFICATE cu namespace — VBA folosea `createElement` fără namespace, INVALID față de `notafd_v0.xsd` care e `elementFormDefault="qualified"` (confirmat din XSD); (2) base64-ul NOTAFD e UTF-8, nu ANSI (VBA folosea `StrConv vbFromUnicode`) — fără machetă DDF reală de comparat (firul deschis), UTF-8 e fără pierderi, ANSI ar tăia diacritice; RISC DE FIDELITATE NEVERIFICAT. **Flux `DdfView.OnGenerateRequested`** (`Async Sub`, graniță UI): fetch `pentru_generare=1` -> filtrează pe revizia-țintă -> `BuildComplete` -> scrie siblingul `.xml` sub folderul partener/GENERAL -> `Await Task.Run(XfaWriter.Genereaza)` pe thread de fundal -> reîmprospătează browser+preview; FĂRĂ scriere înapoi în bază (§2.4). `DdfView` primește un `SessionContext` opțional (globalii unității); `MainForm` îl pasează. **Teste:** .NET **415 passed / 0 failed** (App 120, Api 63, Controls 134, Theming 27, Xfa 39, Domain 17, Common 14, LocalStore 1), build 0 erori / 0 warning-uri BC; Python **75 passed / 14 skipped / 0 failed**. 20 teste structurale `DdfXmlBuilder` + 2 client (flag) + 5 Python (ss/flag/sectiuneb/atasamente). ⚠️ **Deschis:** (a) **05-00 live NEVERIFICAT** (GET/HEAD peste 443 + machetă în cache-ul serverului) — de rulat pe host înainte de a te încrede în generare; (b) **niciun PDF real produs/deschis** — verdictul „Adobe deschide corect PDF-ul" (0019 a/c + DoD-ul feliei) rămâne deschis; (c) **niciun diff structural față de un `.xml` Access real** — imposibil din repo (firul deschis); constructorul e prins de 20 teste față de SURSA modulului, nu de un artefact real; (d) riscul base64 ANSI-vs-UTF-8; (e) fără bară de progres la generare (doar gardă anti-reintrare); (f) pasul 06 (semnare) amânat la 0021 |

| 0022 | Istoric — endpoint + `IstoricView` (a cincea vedere reală găzduită) | 0022-01/02 DONE (code) / **NEVER RUN ON A LIVE DB** / **NO VISUAL VERDICT** | `SLICE-0022-01-istoric-endpoint.md`, `SLICE-0022-02-istoric-view.md` | Plan: `PLAN_IstoricView.md`. `GET /api/forexe/istoric?cod=` (cititor brut, **două** liste: `randuri` = un rând per `FX_Istoric` cu ordinea Access `DataFX, Clsf`; `clasificatii` = ierarhia de filtrare deduplicată) + `IstoricView` înlocuind `PlaceholderView` pentru `istoric`. Access era un **dialog popup** (`frmFX_ISTORIC`), aici o vedere găzduită. **Trei decizii-cheie confirmate din sursă, nu ghicite:** (1) `data_fx` **păstrează ora** (`_iso_dt` dedicat, NU `_iso`-ul din DDF care trunchiază la zi — randurile de istoric sunt evenimente cu ora, §2.3); (2) `Clsf` e coloană TEXT denormalizată, **citită direct, fără join** (§2.4 — vederea nu e expusă duplicatelor din nomenclator pentru afișare); (3) cheia de clasificație e **INVERS față de DDF** — `FX_Istoric.IdClsf` ține id-ul **Access** (`= Clasificatii.IdClsfAcc`, NU `IDClsf`), deci predicatul `IdUnitate` vine prin `FX_Indicatori` pe `CodAngajament`, exact ca filtrul Access `bFilter_Click`; cheia inversată dă meniu GOL, nu eroare (pin de regresie). `clasificatii` **deduplicat pe `IdClsfAcc`** (`GROUP BY`, o listă nu un `LIMIT 1` scalar). **`IstoricFilter`** = clasă PURĂ (port fidel `mdl_FX_Popups.ApplyColumnFilter`): trei segmente independente (Clsf/TipRand/DataFx), setarea înlocuiește segmentul, «TOATE» golește doar unul, filtrul efectiv = AND. Trei `ContextMenuStrip` tematizate (înlocuiesc `bFilter`-ul care urmărea mouse-ul): Clasificații ierarhic (Subcapitol→Articol→Alineat, «TOATE» per nivel doar când >1 copil), TipRand (grupuri `Rez_`/`Plata_` case-insensitive), DataFX (lună/an→zi). Grilă cu **12 coloane** în ordinea Access, **rând de totaluri pe EXACT cele trei coloane** pe care Access le totaliza (`Val_Rezervare_Dif`/`Val_Receptie`/`Val_Plata`), `Descriere`/`Observatii` auto-hide (0016) + panou de detaliu. `RandSchimbat` ridicat, **fără abonat, deliberat** (ca `AdaugaDdfCerut`). Plasa 401 `WithReauth(Of IstoricInfo)` + stale-guard + reset necondiționat al filtrului la context nou. **Nouă devieri documentate** (§9): meniuri scopate la angajament (Access era NEscopat — bug), TipRand case-insensitive, `cod` exact (Access `Like`), rând pe două benzi aplatizat, caption Alineat = `Clasificatii.Denumire` (decizie operator), `bFilter`→bandă cu 3 butoane, 6 coloane deliberat absente de pe fir, dedup pe `IdClsfAcc`, `data_fx` cu oră. **Două bug-uri Access NEportate** (`SELECT` fără `FROM` în `bFilter_Click`; DataFx aplicat pe alt obiect form). `FileVersion`: Domain 1.0.1→1.0.2, Api 1.0.1→1.0.2, App 1.0.2→1.0.3. **Teste:** .NET **436 passed / 0 failed** (App 120→136 = +8 filter +8 view, Api 63→68 = +5 GetIstoric, Controls 134, Theming 27, Xfa 39, Domain 17, Common 14, LocalStore 1), build 0 erori / 0 warning-uri BC (16 NU1701 preexistente); Python **75 passed / 15 skipped / 0 failed** offline (`test_forexe_istoric.py` = +1 skip). ⚠️ **RISC DE VERIFICARE PE HOST #1:** captiunile meniului de clasificație vin din `AVACONT_COMUN.DefaClsfF.Denumire` / `DefaArticol.Denumire` (planul §2.2), dar singura sursă din repo (`qFX_DDF_SA_CLSF`) folosește `DefaClsfF.Explicatie` și `DefaTitlu2` — schema MariaDB reală a acestor nomenclatoare NU e în repo; implementat pe plan cu `LEFT JOIN` (captiune lipsă → NULL, tabelă lipsă → 500 „fail loudly", cum cere §2.2); dacă numele reale diferă, e o singură linie SQL de schimbat. **Alte deschise:** (a) endpoint-ul **nu a rulat niciodată live** (12 teste host-only, văzute doar sărind); (b) `IstoricView` **niciodată pe ecran** (meniuri, temarea celor 3 `ContextMenuStrip`, aplatizarea, auto-hide la lățime reală = neverificate); (c) testul de constatare (§8.12) închide `Status_migrare_5 §9` abia la rularea pe host |

| 0023 | Harness Adobe RHP — patru pârghii contra panoului din dreapta (+ scenarii, layout, veridicitate) | DONE (code) / **rulările din 04.08 sunt INVALIDE (bază contaminată)** | `SLICE-0023-adobe-rhp-harness.md`, `SLICE-0023-adobe-harness-config-layout.md`, `SLICE-0023-adobe-harness-fixes.md`, `SLICE-0023-adobe-harness-truthful-panel.md`, `SLICE-0023-adobe-harness-move-window.md` | Plan: pasted in-session. Panoul Tools din dreapta („Export PDF" / „Start Free Trial") e chrome de APLICAȚIE — parametrii `/A` (chrome de document) nu-l ating și nu există open parameter pentru el. Toate cele PATRU pârghii candidate construite în `AdobeReaderHarnessForm` (DevHarness → «Adobe/PDF»), operatorul judecă pe ecran, pe UI clasic ȘI modern: **§3.A** probă ferestre copil (recursie GW_CHILD adâncime ≤4, log per nod: clasă/text/rect în coordonate gazdă/stil; candidat RHP = EURISTIC — vizibil, lipit de marginea dreaptă ±8px, < 1/2 lățime; decide totul: HWND discret ⇒ §3.C, altfel ⇒ §3.B); **§3.B** decupare geometrică LIVE fără relansare (`HostedBounds()` unic pentru layout + `NudgeRedraw`; „Măsoară din probă" preia lățimea candidatului); **§3.C** ascundere directă de copil (`ShowWindow`, restaurare exactă, gardă `IsWindow` — handle-urile moarte se aruncă cu log, nu throw); **§3.D** Shift+F4/F4 EXPERIMENTAL (tastatura peste granița de proces nu e nativă — „nimic" e rezultat valid); **§3.E** HKCU AVGeneral (`bExpandRHPInViewer=0`/`bRHPSticky=1`/`aDefaultRHPViewMode_L=Collapsed` + `bEnableAv2=0`, hive auto-detectat Reader-vs-Acrobat cu override) cu snapshot O DATĂ pe sesiune și restaurare EXACTĂ — **absent se restaurează prin ȘTERGERE, nu prin 0** (capcana clasică); kill-ul nostru pe PID + consimțământ românesc înainte de a închide Adobe străin; **§3.F** HKLM FeatureLockDown (`bAcroSuppressUpsell` + `cServices\bToggleAdobeDocumentServices`) prin `.reg` generat (apply + revert din citiri pre-apply, linii de ștergere pentru absente) importat cu `reg.exe` elevat (`runas`; K-BOT NU se elevă; UAC anulat = 1223 tratat explicit); **§3.G** bloc de stare permanent (hive/decupare/copii ascunși/HKCU/HKLM). Logica pură în spatele seamului `IRegistryAccess` (`Internal/Adobe/`): `RegistrySnapshotSet`, `RegFileBuilder`, `AdobeHiveResolver`, `AdobeRegistryConstants` (numele §2 TRANSCRISE, nu din memorie). **Proiect de teste NOU `tests/KBot.DevHarness.Tests`** (+22: snapshot 6, .reg 8, resolver 8; `FakeRegistryAccess` — zero registry real). DevHarness FileVersion 1.0.2.0→1.0.3.0. Teste: .NET **458 passed / 0 failed** (+22), build 0 erori / 0 warning-uri BC. ⚠️ **Deschis:** (a) NIMIC din §2 nu e verificat pe o mașină reală — absența unei chei pe build-ul operatorului e REZULTATUL de notat (tabelul gol §5 e în worklog); (b) fără verdict vizual; (c) `AdobeUtils.GetAdobeReaderPath`/`IsAdobeViewer` NEreutilizate (sunt `Private` în KBot.Xfa — în afara scopului; harness-ul își ține rezolvatorul App-Paths propriu); (d) deviere documentată: restaurarea la închidere NU omoară Adobe străin (fără dialog de consimțământ la închidere — regula „niciodată silențios" câștigă), doar loghează. **PASUL 2 (scenarii + layout, `SLICE-0023-adobe-harness-config-layout.md`): NICIO pârghie nouă**, doar (1) **fișiere de scenariu** JSON (`System.Text.Json`, fără pachet nou; comentarii + virgule finale permise fiindcă se scriu de mână) în `<AppDir>\Config\` — încarcă/rulează/salvează, zece pași care mapează FIECARE pe calea de cod deja din spatele butonului corespunzător. **Un scenariu e o REȚETĂ DE SETĂRI, niciodată un document** (clarificare operator, aplicată după prima variantă): încărcarea **pune valorile în panoul din stânga**, iar PDF-ul se alege separat cu «Deschide PDF…» și se deschide cu acele setări; secțiunea `document` a fost ELIMINATĂ din schemă, salvarea nu mai scrie nicio cale, iar din momentul încărcării **panoul e singura sursă a adevărului** (scenariul nu mai e consultat la lansare — ce e bifat e ce rulează, și operatorul poate ajusta înainte de a alege fișierul). Fișierele scrise pe schema veche încă se încarcă (`document` ajunge la proprietăți necunoscute = avertisment). Comutatorul `chkApplyOnLoad` a dispărut odată cu asta (încărcarea aplică mereu, altfel n-ar face nimic). Alegerea unui fișier lansează cu switch-urile din panou și reaplică ascunderile; restul pașilor rulează la «Rulează scenariul» (`RelaunchAsync` a fost SPART în `StartFreshLaunch` + `CompleteEmbedAsync`, nu reimplementat; contorul de generație, `NudgeRedraw` și kill-ul pe PID neatinse). **Ferestrele copil se adresează DOAR după TEXT, niciodată după HWND** — handle-urile se schimbă la fiecare lansare (`0x5083E`→`0x20B66`), textul `AVTaskPaneHostView` nu, iar clasele sunt inutile (aproape tot e `AVL_AVView`); după fiecare încorporare se re-sondează și se re-ascunde, cu reîncercări (Adobe creează task pane-ul DUPĂ vederea principală), fiecare încercare logând «n din m găsite». **Absent ≠ dezactivat**: fiecare secțiune e tip referință (Nothing la absență) și fiecare scalar `Nullable`, deci «fără `clip`» (lasă în pace) rămâne distinct de `"clip": {"enabled": false}` (oprește). Pas necunoscut = **abandonează rularea** cu mesaj care numește pasul greșit + lista celor valizi; proprietate necunoscută = doar avertisment (`JsonExtensionData`). Siguranță: `machinePolicy.apply` implicit **false** (pasul se sare chiar dacă apare în listă), o singură confirmare românească ÎNAINTE de rulare care listează **fiecare cale și valoare, curentă lângă nouă** (Cancel = abandonează toată rularea), încărcarea nu scrie NIMIC. Salvarea scrie starea curentă a tuturor controalelor + **textele ferestrelor ascunse acum** (rotunda care contează), mereu cu `apply=false`. (2) **Layout redimensionabil**: `SplitContainer` vertical (tras la rulare) + `TableLayoutPanel` cu o secțiune `GroupBox` per rând AutoSize la stânga, `pnlHost` 100% peste `lblStatus` AutoSize la dreapta; `lblCmd`→`txtCmd` multiline; decuparea se **reaplică pe `pnlHost.Resize` ȘI `SplitterMoved`**, debounce 150 ms (Adobe repictează târziu în furtuna de resize), cu gardă pe fereastră găzduită. Temarea containerelor noi (SplitContainer/GroupBox/TableLayoutPanel/TextBox) e deja acoperită de `ThemeManager` — verificat prin citire, fără modificări. Scenariu-exemplu livrat (`Config/rhp_collapse_then_hide.json` = combinația Shift+F4 apoi ascunde `AVTaskPaneHostView`). **DEFECT LIVRAT ȘI REPARAT (găsit de operator pe ecran):** panoul de opțiuni era `TableLayoutPanel` cu `AutoScroll` + un rând final `Percent 100`; rândul procentual absoarbe spațiul rămas, deci tabelul raporta MEREU că încape, **nu afișa nicio bară de derulare și tăia tot ce era sub prag** — cele două secțiuni de registry («Preferințe Adobe HKCU» și «Politici Adobe HKLM», exact motivul feliei) erau IMPOSIBIL de atins; în plus `SplitterDistance=320` trunchia captiunile (fișierul regenerat de designer la DPI-ul operatorului măsura `chkNewInstance`=427). Nici build-ul, nici cele 475 de teste nu au observat, fiindcă nimic nu atingea layout-ul. Reparat: `tlpOptions`→**`flowOptions`** (`FlowLayoutPanel` TopDown + AutoScroll — containerul care derula deja acest conținut înainte de rework), rândul-umplutură dispare odată cu tabelul, `SplitterDistance` 320→470, `Panel1MinSize` 260→300, iar lățimea secțiunilor se fixează prin `MinimumSize`/`MaximumSize` în `SizeSections` (a seta `.Width` pe un control `AutoSize` e inutil — motorul o recalculează din conținut și secțiunea iese lateral din panou; min=max lasă AutoSize să guverneze DOAR înălțimea). **Verificat prin randarea formularului real**: panou 453×740, bară verticală prezentă (`Maximum` 1467), fără bară orizontală, 11 secțiuni la 426px, `grpUser` la top=979 și `grpMachine` la top=1241 — ambele sub prag și ambele complet accesibile prin derulare. Teste: .NET **491 passed / 0 failed** (+19 model scenariu, **+8 regresie de layout** `AdobeHarnessLayoutTests` STA: panoul TREBUIE să deruleze, ambele secțiuni de registry trebuie aduse în vizor, cele 13 controale de registry trebuie să existe cu dimensiune nenulă; **+6 legare scenariu→panou** `AdobeHarnessScenarioBindingTests` STA: încărcarea setează comutatoarele/spinnerele, bifează DOAR valorile numite, **NU atinge documentul**, iar ajustarea operatorului de după încărcare învinge fișierul; DevHarness.Tests 22→55), 0 erori / 0 warning-uri BC. DevHarness FileVersion 1.0.3.0→1.0.4.0. ⚠️ **Lecție de proces consemnată:** pasul a fost raportat „livrat" de DOUĂ ori fără ca layout-ul să fi fost vreodată pe ecran; «compilează curat și testele sunt verzi» nu spunea nimic despre el. ⚠️ Deschis: runner-ul de scenarii nu a rulat niciodată contra unui Adobe real; layout-ul e randat, dar nu testat prin click/la DPI diferit. **PASUL 4 (veridicitatea bancului, `SLICE-0023-adobe-harness-fixes.md`): patru scenarii rulate pe 04.08 au raportat SUCCES fără să testeze nimic** — `AVTaskPaneHostView` era **0×0 și deja invizibil ÎNAINTE** de rulare (pe 03.08 era 346×1440 vizibil), deci `hideChildren` ascundea o fereastră deja ascunsă și raporta «1 din 1 texte găsite». Trei defecte închise: (1) **`hideChildren` clasifică acum fiecare potrivire ÎNAINTE de a o atinge** — `ASCUNS`/`DEJA ASCUNS`/`DIMENSIUNE ZERO`/`NEGĂSIT`, sumar «0 ascunse, 1 deja ascuns/zero. ATENȚIE: nicio schimbare reală», oprire devreme DOAR la o ascundere reală, iar o rulare care vede numai «deja ascuns» arde toate încercările și o spune; (2) **`userPrefs` se scriu LITERAL din scenariu** (întreg→REG_DWORD, șir→REG_SZ, **`null`→ștergere**; numele sunt DESCHISE, orice valoare din hive fără schimbare de cod) — bifele rămân doar scurtături pentru numele nemenționate; scenariul 02 cerea `bEnableAv2=1`, iar bifa îl strivea la 0 (log: `0 (DWord) -> 0 (DWord)`); **verificare prin RE-CITIRE după fiecare scriere**, nepotrivire ⇒ `EȘEC: … cerut X, citit Y` + oprirea scenariului + dialog; tabel nou `gridPrefs` **Valoare·Cerut·Curent·Tip** cu rândurile nepotrivite colorate; bifa nu mai minte (se bifează doar dacă scenariul cere exact ce înseamnă ea); (3) **mașina era CONTAMINATĂ și nimic nu o spunea** — pe 03.08 12:09 s-a aplicat politica HKLM «Adobe Acrobat» FĂRĂ revocare (citit live: `bAcroSuppressUpsell=1`, `cServices\bToggleAdobeDocumentServices=1`, încă active), cel mai probabil motivul pentru care panoul e acum de dimensiune zero. Adăugat: buton **«Starea mașinii (Adobe + registry)»** (citire pură, fără elevare), **verificare automată a bazei ÎNAINTE de fiecare scenariu** (`── Bază de pornire ──` + dialog Continuă/Anulează, sau REFUZ dacă `requireCleanBaseline`), **marcaj persistent** `Config\harness_machine_state.json` (supraviețuiește închiderii; fișier corupt = «necunoscut, avertizează», niciodată «curat»), **`chkRevertPolicyOnClose`** implicit bifat cu revocare verificată, iar la eșec/UAC anulat marcajul RĂMÂNE și operatorul e anunțat cu cheile exacte. Probă: linie dedicată `PANOU: <text> — 346x1440, vis=1` / `0x0 (posibil învechit)` / `NEGĂSIT` per țintă, dreptunghiurile ferestrelor invizibile marcate ca posibil învechite (log-ul arăta `27x913` într-o gazdă de 587). Schemă (tot v1): `userPrefs.values` acceptă `null`, `requireCleanBaseline` (root, implicit false), `machinePolicy.revertOnClose` (implicit true). Cele patru scenarii ale operatorului **aduse în repo** (existau doar în `bin\`) și re-emise cu `requireCleanBaseline: true`. Teste: .NET **527 passed / 0 failed** (+36; DevHarness.Tests 55→91), 0 erori / 0 warning-uri BC. **Verificat pe ecran** cu `rhp_02` încărcat: `gridPrefs` arată `bEnableAv2 | cerut 1 | curent 0` în roșu, bifa «=0» nebifată. ⚠️ **MAȘINA E ÎNCĂ CONTAMINATĂ** — nu am revocat politica (scriere elevată pe mașina operatorului); de făcut din banc («Revocă (cere elevare)») ÎNAINTE de orice concluzie; **nicio concluzie din rulările 04.08 nu e valabilă**; cele patru scenarii re-emise NU au fost re-rulate. **PASUL 5 (panoul încetează și el să mintă, `SLICE-0023-adobe-harness-truthful-panel.md`): NICIO pârghie nouă.** Prima bază curată (04.08, 10:04 — 0 politici, 0 procese Adobe) a dus la prima scriere verificată din toată seria: `bEnableAv2: 0 -> 1 (DWord) (verificat)`. Din acea rulare au ieșit trei lucruri: (1) **panoul tot nu putea exprima 1** — pasul 4 a făcut literală calea SCENARIULUI, dar calea MANUALĂ rămăsese pe bife: «bEnableAv2 = 0» bifat pe o mașină deja pe 0 loga `0 -> 0`, corect și inutil, fără nicio cale de a cere 1 din panou; (2) **`restoreOnClose` anula experimentul** — scenariul 02 avea `true`, deci închiderea bancului readucea `bEnableAv2` la 0 și a doua zi mașina era iar pe clasic; (3) **baza „curată" nu era curată** — aceeași citire de la 10:04 arată `bRHPSticky = 1` și `aDefaultRHPViewMode_L = Collapsed` sub `HKCU\…\Adobe Acrobat\DC\AVGeneral`, pe care NICIUN log din felie nu le scrie (rămase din altceva, plauzibil Adobe salvându-și starea după Shift+F4 pe 03.08); verificarea bazei privea doar HKLM, deci mașina primea undă verde deși își amintea deja că vrea panoul colapsat. Închise: **(§1) un RÂND per preferință, nu o bifă** — `PrefRowSelection`/`PrefRowParse` (pur) traduc rândul în intenție: «nu atinge» (implicit) = NICIO intenție, «șterge» = ștergere, orice întreg = `REG_DWORD` literal (nu doar 0/1), orice alt text pe rândul de șir = `REG_SZ` literal, orice altceva pe un rând DWORD = **respins**, numit, iar aplicarea REFUZĂ (a sări peste el ar readuce panoul și registry-ul în dezacord). Cele patru bife → `cboExpandRhp`/`cboRhpSticky`/`cboRhpViewMode`/`cboEnableAv2` într-un `tlpPrefRows` pe două coloane (nume | combo **editabil**, `DropDown` nu `DropDownList`: intrările listate sunt sugestii, deci o valoare neanticipată apare CA EA ÎNSĂȘI, nu rotunjită la cel mai apropiat comutator); `aDefaultRHPViewMode_L` = text liber cu «Collapsed»/«Expanded» oferite. «nu atinge» e cu adevărat distinct de «0» — distincția absent-vs-zero din schemă există acum și în UI. Încărcarea unui scenariu scrie valoarea din fișier VERBATIM în rând (`1` rămâne `1`, `null` devine «șterge», `"Docked"` rămâne `"Docked"`), iar un scenariu tăcut despre HKCU RESETEAZĂ rândurile la «nu atinge» în loc să le moștenească de la precedentul. **(§2) `chkRestoreOnClose` DEBIFAT implicit**; când e debifat și s-au aplicat valori, log-ul o spune pe nume («Restaurare la închidere DEZACTIVATĂ — valorile HKCU rămân aplicate: … Ele vor apărea ca stare de pornire la următoarea rulare»); `rhp_02_interfata_moderna.json` trece pe `restoreOnClose: false` din același motiv, cu antetul explicând cum se revine la clasic (rândul pe `0` sau «șterge» + aplică). **(§3) bază curată = nicio politică HKLM ȘI nicio preferință RHP rămasă în HKCU** — `BaselineOrigin` (`MachinePolicy`/`UserPreference`) pe `PolicyReading` + `ReadUserRhpState()` care citește `bExpandRHPInViewer`/`bRHPSticky`/`aDefaultRHPViewMode_L` din AMBELE hive-uri (mașină neutră = toate trei **absente**); `ReadBaselineState()` = politici + preferințe pentru verificarea dinaintea scenariului, iar `ReadPolicyState()` rămâne DOAR HKLM fiindcă revocarea îl folosește ca să verifice importul elevat și o valoare HKCU nu trebuie să facă acea verificare să pice. **`bEnableAv2` e DELIBERAT în afara bazei** — el alege interfața clasică/modernă, adică exact obiectul probei; contorizat ca murdărie, orice scenariu care îl setează s-ar bloca singur la rularea următoare. Textele de avertisment/refuz sunt conștiente de origine (fiecare valoare etichetată `[HKLM politică]`/`[HKCU preferință]`, fiecare consecință spusă doar când se aplică, fiecare rută de curățare numită pentru originea ei: revocare elevată pentru politică, «șterge» + «Aplică și repornește Adobe» pentru preferințe); sumarul stării numără ambele. Teste: .NET **552 passed / 0 failed** (+25; DevHarness.Tests 91→116: 15 `PrefRowSelectionTests`, +6 bază HKCU, legarea scenariu→rânduri rescrisă +4), 0 erori / 0 warning-uri BC. **Verificat pe ecran** cu `rhp_02` încărcat: trei rânduri pe «nu atinge», `bEnableAv2` pe `1`, restaurarea debifată, `gridPrefs` roșu pe `bEnableAv2 | 1 | 0 | DWord`. ⚠️ **Deschis:** (a) **lipsește arborele de probă de pe interfața modernă** — «nu mai merge nimic» pe `bEnableAv2 = 1` e răspunsul căutat, dar dacă `AVTaskPaneHostView` e absent, redenumit, sau prezent-dar-nu-mai-e-panoul decide ce implementare primește `ReaderHostPreview`, și doar blocul `── Probă ──` poate spune care; NU s-a ghicit aici; (b) cele două valori HKCU rămase sunt **încă setate** — pasul le face vizibile și dă bancului o cale de a le șterge, nu le șterge pe la spatele operatorului; (c) `machinePolicy.values` rămâne pe bife. **PASUL 6 (poziția ferestrei găzduite, `SLICE-0023-adobe-harness-move-window.md`): A CINCEA PÂRGHIE.** ⚠️ **CORECȚIE consemnată:** pasul a fost construit ÎNTÂI ca «Mută ferestre copil» — `SetWindowPos` pe ferestre copil ale lui Adobe (`AVSplitterView`) adresate după text, cu magazie de dreptunghiuri originale și cronometru de reaplicare fiindcă Adobe își reface aranjarea. **Mecanism GREȘIT.** Corecția operatorului: poziția se aplică **ferestrei PDF principale** — cea găzduită de banc — **exact ca proprietățile dreapta/sus**, nu unor ferestre copil anume. Refăcut ca atare: motorul de mutare a copiilor, magazia de originale, cronometrul de reaplicare și pasul `moveChildren` au fost ELIMINATE, nu stratificate; reworkul e și net mai mic, semnul că asta era forma corectă de la început (un singur dreptunghi, fără sondare, fără cronometru, fără evidență de coordonate per copil). **CE ESTE:** `dx`/`dy`/`dw`/`dh` pe fereastra GĂZDUITĂ, aplicate în `HostedBounds()` — exact funcția prin care trec deja decuparea dreapta/sus. Deplasările sunt forma generală a decupării (decupare dreapta 200 = `dw=200`; decupare sus 60 = `dy=-60` + `dh=60`) și **se COMPUN** cu ea (decuparea produce dreptunghiul de bază, deplasările îl mută). `dx`/`dy` negative trag fereastra spre stânga/sus, deci banda de la marginea aceea iese din zona vizibilă; `dw`/`dh` o cresc înapoi peste marginea opusă ca să nu rămână o fâșie goală; lățimea/înălțimea au prag inferior 1 (o fereastră de dimensiune zero ar dispărea fără cale de întoarcere în afară de relansare). **Fiindcă totul trece prin `HostedBounds()`, o deplasare se re-aplică GRATIS** la `LayoutHostedWindow`, `NudgeRedraw`, debounce-ul de resize/splitter și la fiecare relansare — de aceea varianta pe ferestre copil avea nevoie de cronometru, iar aceasta nu are; deplasările supraviețuiesc deliberat lui `ClearHostState` (descriu unde trebuie să meargă URMĂTOAREA fereastră găzduită, ca setările de decupare). **SECȚIUNEA** «Poziția ferestrei Adobe», pusă direct sub «Decupare» fiindcă amândouă conduc aceeași fereastră: patru spinnere (−2000..2000, implicit 0), «Readu la zero» și o linie de indicație care spune explicit că mută FEREASTRA ADOBE, nu ceva din interiorul ei. Spinnerele se aplică LIVE, ca cele de decupare (fără buton «Aplică»); readucerea la zero e exactă prin construcție (deplasările SUNT toată deplasarea, deci zerourile restaurează geometria neatinsă — nu trebuie reținut nimic dinainte). Fiecare plasare loghează ce s-a întâmplat cu adevărat, cu dreptunghiul cerut lângă cel obținut (`Poziție: cerut -120,-90 1028x736 — MUTAT 0,0 908x646 -> -120,-90 1028x736`), iar când diferă: «ATENȚIE: fereastra nu a ajuns la dreptunghiul cerut (Adobe a refuzat sau a limitat)» — Adobe poate limita o dimensiune, iar o cerere ignorată NU are voie să arate ca un succes (lecția `hideChildren` din pasul 4, aplicată acestei pârghii înainte ca aceeași greșeală să poată fi făcută a doua oară). `ChildRectInParent` convertește o singură dată coordonatele ECRAN ale lui `GetWindowRect` în coordonate client `pnlHost`, prin `MapWindowPoints(0, parent, rect, 2)` — amestecul celor două e singurul mod în care asta ar produce numere plauzibile fără sens. **Schemă (tot v1):** `move: { dx, dy, dw, dh }` + pas nou `applyMove` (frate cu `applyClip`); secțiune absentă = `Nothing` («lasă fereastra unde o pune panoul»), distinctă de una prezentă plină de zerouri (avertisment); `moveChildren`/`moveOptions` și pasul `moveChildren` sunt ELIMINATE — un fișier scris pe ele eșuează zgomotos, nu tăcut. Scenariu livrat `Config/rhp_05_muta_fereastra.json` cu `clip.enabled` **explicit false** (se compun, deci cu amândouă active nimic de pe ecran n-ar mai fi atribuibil unui singur mecanism). Teste: .NET **579 passed / 0 failed** (552 înainte de pas; DevHarness.Tests 116→143, `MoveWindowTests` 20), 0 erori / 0 warning-uri BC. DevHarness FileVersion 1.0.5.0→1.0.6.0. **Verificat pe ecran** cu `rhp_05` încărcat: secțiunea stă sub «Decupare», arată −120/−90/120/90, iar blocul de stare scrie `Poziție: dx=-120 dy=-90 dw=120 dh=90`. **DEFECT PRINS DE RANDARE:** «Readu la zero» rămânea dezactivat după încărcarea unui scenariu cu deplasări nenule — `ApplyScenarioToControls` nu chema `UpdateActionStates`, deci o deplasare venită din FIȘIER nu activa butonul pe care una tastată îl activa; reparat, ambele sensuri prinse acum de teste. ⚠️ **Deschis:** (a) **nimic de aici n-a fost aplicat unui Adobe pornit** — dacă tragerea ferestrei spre stânga scoate într-adevăr jgheabul din vizor și dacă Adobe limitează dimensiunea sunt deschise; (b) **limitarea de zoom e comună cu decuparea și netestată**: fereastra găzduită chiar E mai mare decât panoul, deci un zoom «fit width»/«fit page» rescalează pagina la lățimea umflată — mutarea NU poate evita asta, iar afirmația anterioară că ar putea era legată de mutarea unei ferestre COPIL, ceea ce nu mai face; (c) compunerea `clip`+`move` e acoperită aritmetic de un test, dar nu a fost privită niciodată pe ecran |

| 0024 | DDF, fila «Document» — profiluri Adobe cu comutare la rulare (+ gazdă Adobe partajată + captură/desprindere) | 0024-01/02/03 DONE (code) / **NEVER RUN AGAINST A REAL ADOBE** / **NO VISUAL VERDICT** | `SLICE-0024-01-adobe-shared-host.md`, `SLICE-0024-02-adobe-profiles-runtime.md`, `SLICE-0024-03-adobe-host-teardown.md` | Plan: pasted in-session. Cele două configurații găsite pe banc în felia 0023 ies din banc și intră în `ReaderHostPreview`, în spatele unei setări pe care operatorul o schimbă **cât timp aplicația rulează**. **0024-01 (gazda partajată):** clip, deplasare, ascunderea copiilor, proba și plumbăria Win32 existau în DOUĂ copii (`AdobeReaderHarnessForm` în KBot.DevHarness și `ReaderHostPreview` în KBot.App) și **DEJA divergeau** — bancul curăța `WS_SYSMENU`/`WS_MINIMIZEBOX`/`WS_MAXIMIZEBOX` și forța redesenarea, aplicația nu, deci același PDF se comporta diferit în cele două locuri, iar copia aplicației o purta pe cea mai proastă (fără `NudgeRedraw` fereastra reparentată poate rămâne **nevăzută**). Extras în `src\KBot.Controls\Adobe\` (12 fișiere): `AdobeNativeMethods` (toate P/Invoke-urile, o dată, `Friend`), `AdobeHostGeometry` (= corpul lui `HostedBounds()` + `HostedWindowGeometry`/`MoveOutcome` **mutate**), `AdobeHideOutcome` (**mutat**), `AdobeWindowProbe`, `AdobeUiDetector`, `AdobeViewerProfile`, `AdobeViewerSettings`, `AdobePopupFilter`, `AdobePopupWatcher`, `AdobeWindowHosting`, `AdobeReaderHost`, `AdobeHostLog`. Bancul a fost **refăcut pe codul partajat în ACEEAȘI felie** (`WalkChildren`, `ChildWindowItem`, `FindReaderWindow`, `ResolveAdobePath`, `ReadAppPath`, `KillPid` și **întregul bloc de 145 de linii de interop** ELIMINATE; `MoveOutcome.vb`/`HideOutcome.vb` șterse din DevHarness); NU folosește `AdobeReaderHost` (orchestratorul) — decizie deliberată: bancul conduce decuparea/poziția live din spinnere, are pași de scenariu `launch`/`waitForEmbed` separați, omoară Adobe pe PID și ține un contor de generație; primitivele sunt partajate, orchestrarea nu, și nu mai există nicio primitivă duplicată care să dividă. Singura schimbare de comportament a bancului: `ProbeChildren` loghează în plus `Mod detectat: …`. **Profilurile sunt TRANSCRIERI, nu propuneri** — verificate contra stărilor salvate care mai erau pe disc (`bin\Debug\…\Config\scenariu_adobe_modern_full.json` 04.08.2026 20:06:04 și `scenariu_adobe_oldschool.json` 20:10:29), acum păstrate **byte-verbatim** ca fixtures de test: MODERN (`/n` nu, `/s` nu, **niciun** parametru `/A`, decupare dreapta 230 sus 152, `dx=-130`), CLASIC (`/n` da, `/s` da, `toolbar=0&navpanes=0`, fără decupare, fără deplasare). **Detecția citește ARBORELE DE FERESTRE, nu registry-ul** (`AVTaskPaneHostView`→clasic; `AV2DocumentTabView`/`AV2DockableTabStripView`→modern; niciunul→arborele complet în jurnal + cădere pe clasic, alegerea conservatoare fiindcă profilul clasic nici nu decupează nici nu mută); marcajele se potrivesc pe TEXT **sau** clasă; un arbore cu marcaje din ambele generații rezolvă prin prima regulă dar e marcat `Ambiguous` și spus în jurnal. **`bEnableAv2` NU se scrie niciodată de codul livrat** — ar schimba Adobe-ul operatorului pentru orice PDF, oriunde; bancul îl scrie fiindcă bancul e banc. **Setarea:** `KBotPaths` (`<AppDir>\kbot_paths.json`, KBot.Common) primește `AdobeViewerMode` + `AdobeNewInstance` și un `Save()`; ales în locul lui `ThemeStore` (`%AppData%\AVACONT\theme.json`, per utilizator) fiindcă modul descrie **ce Adobe e instalat pe mașină**, nu o preferință a persoanei — un magazin roaming ar duce valoarea greșită pe altă mașină. Valorile se păstrează ca TEXT (enum-urile sunt în KBot.Controls, care referă KBot.Common; invers ar fi ciclu), iar `Save(dir)` cu director EXPLICIT nu atinge singleton-ul `Current`. **0024-02 (runtime):** `ReaderHostPreview` rescris (325→205 linii, zero interop), încorporare **asincronă** (înainte bloca firul UI până la 8 s pe `EnumWindows`) cu stale-guard, `lblNote` = nota discretă doar când versiunea Adobe nu e recunoscută, și **procesul NU se mai închide necondiționat** — sub profilul modern (`/n` oprit) Adobe poate preda documentul instanței operatorului, iar închiderea ei i-ar lua și celelalte documente; se închide doar ce a pornit K-BOT ȘI deține fereastra găzduită. Supraveghetorul de `AVL_AVPopup` e pornit **doar aici** (`PopupWatchEnabled`), ca bancul să rămână identic. UI: `pnlAdobe` (bandă `Top` în `pnlPdf`, totul în `.Designer.vb`) cu «Mod vizualizator Adobe» (Automat/Modern/Clasic) și «Instanță nouă Adobe» (Automat/Da/Nu), pe elemente tipizate (`AdobeModeItem`) ca citirea selecției să nu însemne comparare de text de interfață; o schimbare persistă AMBELE setări și reaplică profilul pe documentul afișat ACUM — **geometria imediat, parametrii de lansare abia la documentul următor** (nu se poate schimba linia de comandă a unui proces pornit). `_suppressAdobeComboEvent` împiedică popularea combo-urilor să suprascrie setarea (test dedicat). Jurnal nou `<AppDir>\Logs\adobe_preview.log` (decizii, nu excepții). **Document livrabil NOU: `docs\SETARI_UTILIZATOR.md`** — nu exista niciun document de setări pentru operator; scris în română cu diacritice, cu toate cele 6 puncte cerute per setare (nume în UI + unde, cheie/tip/valori, implicit + comportament la valoare lipsă/invalidă, ce face în termeni concreți cu cifrele măsurate, cablajul prin cod cu fișierele numite și apelurile Windows rezultate, limitări + dependența de versiune). **CORECȚIE LA PLAN consemnată:** §2 afirmă că supraveghetorul de popup «există deja în banc» și §8 cere «reutilizarea testelor lui» — **nu există**; `AVL_AVPopup` nu apare nicăieri în repo (nici în banc, nici în scenarii, nici în cei 75 KB de `test_adobe_rhp.log`, nici în worklog-urile 0023), deci a fost SCRIS DE LA ZERO iar testele lui sunt noi; numele clasei și cele patru criterii vin din plan (observație de operator), nimic din repo nu le coroborează. `FileVersion`: Common 1.1.0.0→1.2.0.0, Controls 1.0.0.0→1.1.0.0, DevHarness 1.0.6.0→1.0.7.0, App 1.0.3.0→1.0.4.0. **Teste:** .NET **663 passed / 0 failed / 0 skipped** (580 înainte; Controls 134→201, DevHarness 144→153, App 136→143), build 0 erori / 0 warning-uri BC (16 NU1701 preexistente). Regresia cerută de plan trece: ambele stări salvate se încarcă și dau **același dreptunghi** prin `AdobeHostGeometry.Compute` (`(-130,-152) 1230×952` modern, `(0,0) 1000×800` clasic pe gazdă 1000×800), iar profilurile livrate produc exact acele dreptunghiuri. ⚠️ **Deschis:** (a) **NIMIC nu a rulat contra unui Adobe real și nimic nu a fost văzut pe ecran** — `SetParent`/`AttachAsChild`/`NudgeRedraw`/mătura de popup nu s-au executat niciodată; dacă −130/230/152 scot într-adevăr chrome-ul **în panoul aplicației** (măsurate în panoul bancului, de altă dimensiune) e neverificat; (b) **supraveghetorul de popup n-a ascuns niciodată un popup** — dacă numele clasei e greșit, filtrul respinge tot și o spune în jurnal, dar asta e o ipoteză; (c) bancul **nu a fost re-rulat pe ecran** după refactor: «verifică re-rulând stările salvate» s-a făcut ARITMETIC (testul de regresie), nu încărcând cele două fișiere cu Adobe deschis; (d) calea «relansează o dată» (pe `Auto`, primul document al sesiunii pornește cu parametrii clasici, detectează, și relansează dacă profilul detectat cere alți parametri de lansare) e **netestată la rulare** — primul document modern al unei sesiuni poate porni Adobe de **două** ori; e mărginită (o dată per document) și logată, dar dacă arată acceptabil operatorului e necunoscut; (e) **`newInstance = false` pe profilul modern rămâne un risc viu** (§1 al planului): implementat ca măsurat, logat zgomotos, procesul nu se închide când nu e al nostru, operatorul poate forța «Da» — **se datorează încă o decizie dacă implicitul livrat ar trebui să forțeze `/n`**; (f) setarea e per **instalare**, nu per utilizator: doi utilizatori Windows pe aceeași instalare împart o valoare. **PASUL 03 (captura și desprinderea, `SLICE-0024-03-adobe-host-teardown.md`): cele două defecte pe care operatorul le vedea pe ecran.** Briefingul a venit scris ca «felia 0023, pasul 3» și cerea un `AdobeHostController` NOU în `KBot.Common` — **ambele erau depășite**, fiindcă briefingul precedă feliile 0024-01/02; reconcilierea e consemnată în §0 al worklog-ului, nu ocolită în tăcere (CODE_WORKFLOW §4): §2/§3 (mută motorul în Common) **SUPERSEDAT** — motorul există deja ca `AdobeReaderHost` în `KBot.Controls`, scopul real (o singură implementare) fiind atins de 0024-01; decizie de operator: rămâne în `KBot.Controls`, `KBot.Common` NEATINS, tot pe `net8.0`. §5 (aplicația scrie `bEnableAv2 = 0`) **CONTRAZICE** decizia din 0024-01; decizie de operator: se construiește, dar INERT în spatele unei constante. §7 («bancul trece pe controller») **NEURMAT deliberat** — 0024-01 §3 a decis motivat că bancul își ține orchestrarea și partajează doar primitivele; pasul ăsta îi partajează primitivele NOI. **DEFECTUL 1 — fereastra rămasă cu buton în bara de activități:** `Detach` chema `RestoreStandalone`, care repunea stilul original (`WS_CAPTION`/`WS_POPUP`) și re-parenta fereastra pe desktop — adică exact rețeta de CREARE a unei ferestre top-level, iar o fereastră top-level e un buton în taskbar; închiderea care ar fi trebuit să urmeze era un `CloseMainWindow()` pe un `Process` care, de la al doilea document încolo, IEȘISE deja (Adobe e practic mono-instanță). Reparat: `RestoreStandalone` **ȘTERS din cod**, nu doar nefolosit; ambele moduri de desprindere ABANDONEAZĂ fereastra în loc să o predea. `AdobeWindowTeardown` nou, cu `AdobeDetachMode`: **A (`KillProcess`, implicitul livrat)** omoară procesul, dar DOAR un PID din `_launchedPids` — mulțimea proceselor pornite chiar de gazdă; un PID din afara ei nu e omorât NICIODATĂ și apelul coboară la modul B cu refuz logat, fiindcă sub profilul modern fereastra poate fi a Adobe-ului operatorului; **B (`CloseWindow`)** postează `WM_CLOSE` (postat, nu trimis: un fir UI străin ocupat nu are voie să blocheze firul nostru), așteaptă `CloseGraceMs`, apoi cade pe A doar pentru un proces pornit de noi. B e singura cale care așteaptă, deci implicitul livrat nu blochează niciodată UI-ul la schimbarea documentului. **DEFECTUL 2 — fulgerarea ferestrei:** `FindReaderWindow` respingea orice candidat care pica `IsWindowVisible`, deci prin construcție putea prinde doar o fereastră **DEJA desenată pe ecran**, cu tot cu bara de titlu. `AdobeWindowCapture` nou: vizibilitatea nu se testează NICIODATĂ, identitatea vine din PID, iar fereastra e ascunsă **în interiorul buclei de căutare**, în clipa potrivirii, înainte ca apelantul să afle că există; pas de sondare 150 ms → 30 ms. **CORECȚIE la brief pe care el nu avea cum să o anticipeze:** §3.3 cere «sari peste orice PID care nu e al nostru», dar profilul MODERN lansează FĂRĂ `/n` (valoare măsurată, transcrisă în 0024-01), deci Adobe predă documentul unei instanțe existente și procesul nostru nu deține nicio fereastră — un filtru strict pe PID ar întoarce `WindowNotFound` pentru FIECARE document modern; implementat ca **PID întâi, cu cădere pe titlu logată, permisă doar când profilul a omis `/n`**, iar rezultatul poartă `ByPid`/`ByTitle`, o potrivire `ByTitle` fiind exact ce interzice apoi omorârea acelui proces. **Seamuri noi** (`INativeWindows`/`Win32Windows`, `IAdobeLauncher`/`ProcessAdobeLauncher`, `IHostSurface`/`ControlHostSurface`) — captura, desprinderea și toată orchestrarea rulează acum fără Adobe, fără fereastră și fără pompă de mesaje; `AdobeNativeMethods` rămâne unicul set de P/Invoke. `AdobeCreationHook` (`SetWinEventHook`) e construit dar **OPRIT implicit**: fiind WinEvent out-of-context, îngustează fereastra în care Adobe poate fi văzut, dar **nu se poate promite că o închide**. `ReaderHostPreview` primește starea a patra `pnlLoading` («Se încarcă documentul…», declarată în Designer) plus constanta de compilare `DetachMode`. `AdobeUiPreference` (KBot.App) = mecanismul §5 cu **`ENFORCE_LEGACY_UI = False`** — scris, testabil, dar nu citește și nu scrie nimic cât constanta e False. **Mutare:** cele șase fișiere de registry (`IRegistryAccess`, `WinRegistryAccess`, `RegistryValueSnapshot`, `RegistrySnapshotSet`, `AdobeHiveResolver`, `AdobeRegistryConstants`) au trecut din KBot.DevHarness în `KBot.Controls\Adobe\` (290 de linii, `git mv`, corpuri neatinse) — KBot.App nu poate referi KBot.DevHarness, iar duplicarea unui helper de instantaneu e exact greșeala pe care 0024-01 a fost scrisă să o elimine. **Banc:** secțiunea «Închidere / captură» (radiouri A/B, bifă cârlig de creare, spinnere întârziere-captură și răgaz-închidere, plus eticheta cu **milisecundele lansare → încorporare**, numărul care transformă comparația A-vs-B din impresie în măsurătoare) și bifa `bEnableAv2 = 0` care reia calea de aplicare existentă, inclusiv dialogul de consimțământ; secțiune nouă de scenariu `hosting`, cu rotundă completă. **Banc:** secțiunea «ActiveX (AcroPDF)» — CLSID citit LA RULARE din `HKCR\AcroPDF.PDF.1\CLSID` (**niciun GUID hardcodat**), subclasă minimă `AxHost`, `LoadFile` prin reflexie (`Option Strict On` interzice legarea târzie), buton de al doilea document fiindcă SCHIMBAREA de document e toată comparația; fără `aximp`, fără assembly de interop, fără referință COM, și nimic din KBot.App nu depinde de ea. `FileVersion`: Controls 1.1.0.0→1.2.0.0, App 1.0.4.0→1.0.5.0, DevHarness 1.0.7.0→1.0.8.0; **Common NEATINS**. **Teste:** .NET **693 passed / 0 failed / 0 skipped** (663 înainte; Controls 201→224, DevHarness 153→160), **0 erori / 0 warning-uri de orice fel** — cele 16 NU1701 vechi au fost reduse la tăcere în acest pas, la cererea operatorului: `NoWarn="NU1701"` pe `PackageReference`-ul iTextSharp din KBot.Xfa, plus o proprietate `<NoWarn>` în cele patru proiecte care ajung la **BouncyCastle**, care vine TRANSITIV prin iTextSharp și deci nu poate fi acoperit de atributul de pachet (alt id). NU e un `Directory.Build.props` global: celelalte opt proiecte avertizează în continuare normal, iar un pachet .NET Framework adăugat mâine tot va fi raportat. **Ce costă:** nu vom mai fi anunțați că cele două pachete nu sunt native pe .NET 8 — acceptabil fiindcă versiunile sunt FIXATE (aceleași ca exe-ul XFA_WRITTER din care s-a portat motorul, felia 0019) și suprafața folosită e acoperită de KBot.Xfa.Tests; dacă vreo versiune se schimbă, `NoWarn` se șterge ÎNTÂI (nota e în .vbproj, nu doar în worklog). KBot.Xfa NU a primit bump de `FileVersion` — i s-au schimbat doar setările de avertismente, nicio linie de cod. Toate cele nouă teste cerute de §9 există, inclusiv cele două care contează cel mai mult: **captura acceptă o fereastră INVIZIBILĂ** (regresia fulgerării) și **desprinderea nu cheamă niciodată `SetParent` înapoi și nu restaurează niciodată stilul** — verificat pentru AMBELE moduri, fiindcă ăsta e defectul exact. Un test existent a fost ACTUALIZAT, nu ocolit: `AllTwelveSections_ArePresent_InOrder` → `AllFourteenSections_…` (e o gardă reală pe ordinea secțiunilor și a prins schimbarea). ⚠️ **Deschis:** (a) **NIMIC nu a rulat contra unui Adobe real și nimic nu a fost văzut pe ecran** — testele dovedesc ORDINEA și ABSENȚA apelurilor pe un desktop fals; `SetParent`/`ShowWindow`/`PostMessage`/`SetWinEventHook` nu s-au executat nici măcar o dată; (b) **§10 «definiția de terminat» din brief NU poate fi semnată de aici** — «nicio fereastră rămasă», «niciun buton în taskbar», «fereastra nu se vede înainte de încorporare» sunt OBSERVAȚII PE ECRAN; căile de cod care produceau ambele defecte au dispărut și absența lor e fixată de teste, ceea ce e o afirmație diferită și mai slabă decât «defectele sunt observate reparate»; (c) **fulgerarea reziduală e ÎNGUSTATĂ, nu măsurată**; (d) `CaptureDelayMs` e 0 și niciun prag nu a fost găsit — dacă reparentarea unei ferestre Adobe pe jumătate inițializate face rău e întrebarea deschisă pentru care există spinnerul; (e) **modul B nu a închis niciodată o fereastră reală** — dacă Adobe onorează `WM_CLOSE` pe un copil reparentat, și dacă procesul supraviețuiește când era ultima fereastră de document (caz în care B degenerează în A), sunt neverificate, la fel comportamentul pe UI-ul modern; (f) **VERDICTUL AcroPDF E CONSEMNAT (05.08.2026) ȘI E POZITIV** — un DDF XFA real SE RANDEAZĂ în controlul ActiveX, fără substitutul «Please wait…»: controlul e înregistrat (CLSID `{CA8A9780-280D-11CF-A24D-444553540000}`, citit din HKCR la rulare), răspunde la reflexie (versiune = lista de plugin-uri Acrobat **26.1**), două DDF-uri generate reale s-au randat, iar **schimbarea de document funcționează** (al doilea încărcat în ACELAȘI control, fără golire între). Singurul eșec, `ORD_009.pdf` gri, **nu e nici fișierul, nici calea**: era ACELAȘI document deschis simultan în fereastra găzduită — Adobe e mono-instanță, controlul e servit de același motor, iar a doua cerere nu întoarce nimic (`LoadFile` întoarce oricum True). Două teorii plauzibile au fost greșite și au costat timp (calea cu spațiu; forma XFA dinamică) — ambele fapte sunt adevărate, niciunul nu e cauza. **Consecință de proiectare:** găzduirea de ferestre și o suprafață AcroPDF NU pot arăta același document simultan, deci dacă AcroPDF înlocuiește vreodată găzduirea, o înlocuiește COMPLET — nu există migrare graduală cu ambele active. Bancul avertizează acum când cele două se ciocnesc. Confirmat ulterior de operator: `ORD_009.pdf` SE RANDEAZĂ normal când nu e simultan găzduit — coliziunea era tot. **DAR documentul apare cu TOATE barele Adobe vizibile**, adică exact problema pe care felia 0023 a atacat-o cinci pași (decupare, ascundere de copii, HKCU, HKLM, taste) fără s-o rezolve curat. Diferența: controlul ActiveX are **API DOCUMENTAT** pentru asta — `AcroPdfHost.ApplyChrome` cheamă acum prin reflexie `setShowToolbar(False)`, `setShowScrollbars(False)`, `setPageMode("none")`, `setLayoutMode("SinglePage")`, `setView("Fit")`, **raportând FIECARE apel separat** (răspunsul interesant e CARE dintre ele le onorează build-ul ăsta; un membru inexistent se loghează ca «NU EXISTĂ pe acest build», rezultat nu eroare), aplicate ÎNAINTE și din nou DUPĂ `LoadFile` cu cele două treceri logate distinct (documentația și practica nu sunt de acord ce ordine ține pe ce build — bancul e locul unde se măsoară). **MĂSURAT 05.08.2026 — API-UL DE CHROME NU FUNCȚIONEAZĂ:** toate cele cinci apeluri întorc OK la fiecare încărcare, înainte ȘI după `LoadFile`, iar barele rămân. Acceptate și ignorate — **exact aceeași ruptură ca la parametrii `/A` din felia 0023**: metodele adresează chrome de DOCUMENT, iar banda de file și panoul Tools sunt chrome de APLICAȚIE. Ruta ActiveX nu scapă de problemă, o moștenește. **CE FUNCȚIONEAZĂ, în schimb:** starea pe care operatorul o numește perfectă (nimic vizibil până la mișcarea mouse-ului, când apare bara plutitoare) **NU e un mod oferit de Adobe — sunt trei ferestre invizibile**; diferența completă față de o stare proastă, din sonde: `AVDockableTabStripView` 67x297 vis=1 → 0x297 vis=0, `AVExpandCollapseButtonView` 27x297 vis=1 → vis=0, iar vederea documentului `AVSplitationPageView` trece de la x=67 lățime 1805 la **x=0 lățime 1899, adică tot panoul, SINGURĂ** (nimic nu trebuie redimensionat). Deci se reproduce cu `ShowWindow`, adică exact pârghia `hideChildren` din felia 0023, îndreptată spre controlul ActiveX; `btnAcroHideChrome` face asta, clasificând fiecare fereastră ÎNAINTE de a o atinge (lecția pasului 4). **STAREA PANOURILOR NU E ÎN REGISTRY:** 15 instantanee AVGeneral × 106 valori, peste toate manipulările de panouri, iar singurele valori schimbate sunt contoare de sesiune (`iNumAcrobatLaunches`, `iNumOfAVDocsOpened`, `uLastSessionTimeStamp`); nimic legat de panouri nu s-a mișcat. Starea care supraviețuiește de la un document la altul stă deci **în memoria procesului Acrobat viu**, care persistă tocmai fiindcă Adobe e mono-instanță — o presetare de registry NU o poate dirija în timpul sesiunii, iar primul document al unei sesiuni moștenește ce a lăsat sesiunea Acrobat anterioară a operatorului (de asta același fișier se comportă diferit după ce a fost deschis altceva înainte). Enumerarea cheii în loc de ghicirea unui nume a fost decizia corectă: răspunsul e «nicio cheie», ceea ce nicio ghicire nu l-ar fi produs. ⚠️ `btnAcroHideChrome` **nu a rulat încă** — mecanismul e DEDUS dintr-un diff de sonde peste o stare atinsă manual, nu reprodus programatic; și **nu se știe dacă starea ascunsă supraviețuiește documentului următor** (Adobe reconstruiește ferestrele copil la fiecare document, handle-urile se schimbă — de asta felia 0023 le adresează după text și le reaplică după fiecare încorporare; bucla de reîncercare există pentru fereastra găzduită, nu și pe calea ActiveX). Panoul ActiveX era 1899x**298** în toate sondele — prea scund ca documentul să încapă; ridicat la 700; (g) **tabelul §5 despre `bEnableAv2` e GOL** — dacă opt-out-ul mai e onorat, per generație de interfață, nu a fost observat; `AdobeUiPreference` fiind inert, calea lui de instantaneu/restaurare (inclusiv «absent se restaurează prin ștergere») nu s-a executat niciodată în aplicație; (h) bancul **nu a fost re-rulat pe ecran** după acest refactor; (i) purtate mai departe din 0024-02 și neatinse: calea «relansează o dată» e tot netestată la rulare, iar `newInstance = false` pe profilul modern **datorează încă o decizie** — pasul ăsta o face mai SIGURĂ (o fereastră străină nu e omorâtă niciodată), dar nu o ia |

| 0025 | Suprafețe editabile din designer (`KBotNavList`, `KBotDataView`, `KBotCaptionBar`, `KBotBusyBar`) + `MainForm.navViews` autorit în designer | DONE (code, inclusiv pașii 7, 02, 05, 06, 07 și 08) / **VERIFICĂRILE DIN VISUAL STUDIO NU AU RULAT** | `SLICE-0025-designer-surfaces.md`, `SLICE-0025-02-navitem-icon.md`, `SLICE-0025-05-navlist-collapse-padding.md`, `SLICE-0025-06-navlist-collapse-button-icons.md`, `SLICE-0025-07-navlist-collapsed-flyout.md`, `SLICE-0025-08-navlist-corner-radius-gradient.md`, `SLICE-0025-09-navlist-collapse-button-size.md` | Plan: `PLAN_DesignerSurfaces.md` (lipit în sesiune; presupunea felia 0024, care era deja luată). Cele patru controale devin editabile din **grila de proprietăți și dialogul standard de colecție** — NU prin manipulare directă pe suprafața de design (ar cere un `ControlDesigner` propriu, iar pe `net8.0-windows` designer-ul rulează în alt proces, deci ar cere un assembly de design-time contra `Microsoft.WinForms.Designer.SDK`; în afara scopului, deliberat). **`KBotNavList`:** clasa privată imbricată `NavItem` **înlocuită** cu `KBotNavItem` public (constructor fără parametri, cele șapte proprietăți atribuite, `Bounds` lăsat `Friend` ca designer-ul să nu-l serializeze) + `KBotNavItemCollection` care invalidează layout-ul la fiecare mutație; `Items` cu `DesignerSerializationVisibility(Content)`, `ISupportInitialize`, `ToolboxItem(True)`. **UN singur tip de element** cu steag `IsSeparator` (două tipuri într-o colecție ordonată ar cere editor de colecție propriu = assembly de design-time), diferența fiind vizibilă prin `ToString()` în lista dialogului. `AddItem`/`AddSeparator`/`SetBadge`/`SetItemEnabled`/`SetItemVisible` **neschimbate**, inclusiv fiecare `ArgumentException`. `FindIndex` sare peste separatori ȘI respinge cheia vidă — a doua parte nu e cosmetică: `OnKeyDown` cheamă `FindIndex(_selectedKey)` cu `Nothing`, iar `String.Equals(Nothing, Nothing)` = True ar fi potrivit un element autorit în designer cu cheia încă goală. `SelectedKey` între `BeginInit`/`EndInit` **se reține fără validare** (designer-ul nu e obligat să emită `Items` întâi); `EndInit` dă `__sep_N` separatorilor fără cheie (contor care sare peste orice cheie deja folosită, inclusiv una tastată manual), validează cheile și aplică selecția — **validarea se sare în designer**, unde greșeala se arată în schimb cu **chenar roșu de 2px** din `OnPaint`. **`KBotDataView`/`KBotDataColumn`:** constructor fără parametri, `Owner` `Friend`, `Key`/`ColumnType` devin scriibile dar aruncă `InvalidOperationException` **peste o grilă cu rânduri** (valorile celulelor stau pe cheia coloanei în dicționarul lui `KBotDataRow` — o redenumire peste date ar orfelina fiecare celulă și coloana ar picta gol, adică pierdere de date deghizată în no-op); `Columns` trece din `IReadOnlyList(Of KBotDataColumn)` în `KBotDataColumnCollection`, care reconstruiește indexul + totalurile + layout-ul singură — **asta închide jumătate din limitarea consemnată la 0013** (editarea PROPRIETĂȚILOR unei coloane după încărcare tot cere `AutoSizeColumns()` explicit). `AddColumn` neschimbat bit cu bit, dar fără liniile duplicate. Rândurile și starea de selecție sunt `Browsable(False)` + `Hidden` (sunt DATE de rulare; serializate ar îngheța în formular datele de test ale cuiva); `ComboItems`/`Tag` la fel, fiindcă **nu pot face rotunda** prin `InitializeComponent`. **Defect de rotundă prins pe drum, absent din plan:** `TotalsRowHeight` NU putea primi `DefaultValue(0)` — getter-ul întoarce `HeaderHeight` cât banda urmărește antetul, deci designer-ul ar fi serializat numărul REZOLVAT (30) și ar fi fixat banda pentru totdeauna; rezolvat cu `ShouldSerialize`/`Reset`, cu test. **`KBotCaptionBar`/`KBotBusyBar`:** `ToolboxItem(True)` + atribute; `Running` **nu pornește cronometrul în designer** (un timer la 15 ms în `devenv`/`DesignToolsServer` repictează la infinit). **Helper comun `KBotDesignTime`** (Public, nu Friend — `KBot.Controls` e alt assembly): `LicenseManager` + `Site.DesignMode` + urcarea lanțului de părinți + numele procesului, fiindcă `Control.DesignMode` e False în constructor ȘI pentru un control imbricat; **toate cele 11 apeluri `GlobalErrorLog.Write` din pictare/mouse/tastatură se sar la design time** (sink-ul scrie în `AppContext.BaseDirectory\Logs`, adică lângă `devenv.exe`). **PASUL 7 (migrarea `MainForm.navViews`) A RULAT, la cererea explicită a operatorului** — felia se oprise inițial înaintea lui, fiindcă planul îl condiționează de trecerea pașilor 1 și 6, iar pasul 6 cere Visual Studio; consemnat ca livrat pe o rotundă **neprobată în designer**, nu pe una verificată. Cele opt intrări (5 butoane Near, separator Far, DDF/ORD Far) trăiesc acum în `MainForm.Designer.vb` în forma pe care o emite designer-ul (`Dim KBotNavItem1..8` + atribuiri + `Items.Add`), cu perechea `CType(navViews, ISupportInitialize).BeginInit()/.EndInit()` lângă cea a lui `split`; `Align = Near` și celelalte valori implicite NU se emit. **Nu am adăugat `AddRange` pe colecție tocmai ca forma scrisă de mână să fie cea pe care o va regenera VS** (`CollectionCodeDomSerializer` folosește `AddRange` doar dacă există, altfel `Add` per element; `Collection(Of T)` nu are). Blocul `AddItem`/`AddSeparator` a dispărut din `MainForm.vb` (dacă rămânea, lovea aruncarea pe cheie duplicată la prima rulare), dar **`SelectedKey = "sumar"` rămâne în cod** — atribuirea aia ridică `SelectionChanged` și creează prima vedere; în designer ar fi valoare moartă. **Corecție la plan:** §9.3 spune «after `ApplyViewGating(Nothing)` as today» — în cod era ÎNAINTE, și a rămas; ambele merg, dar «as today» descria greșit codul. Diacriticele sunt literale, **verificat pe octeți** (`ă` = `C4 83`, `ț` = `C8 9B`, zero `\u00` în fișier) și, mai important, comparate cu literalele la RULARE de teste. 5 teste noi (`MainFormNavItemsTests.vb`) construiesc `MainForm` pe fir STA cu dependențele pe `Nothing` și fixează ordinea + Align-ul, diacriticele, faptul că separatorul a primit `__sep_1` (adică `BeginInit`/`EndInit` chiar au rulat din `InitializeComponent`), faptul că toate cheile folosite de `ApplyViewGating` se rezolvă, și faptul că re-adăugarea din cod ar arunca. Asta e apărarea contra pericolului real — o regenerare de designer care pierde sau reordonează intrări: `IsViewEnabled` aruncă pe o cheie necunoscută, deci o intrare pierdută nu e pagubă cosmetică. `FileVersion`: Theming 1.0.0.0→1.1.0.0, Controls 1.2.0.0→1.3.0.0, App 1.0.5.0→1.0.6.0; **`AssemblyVersion` stă pe loc peste tot**, deși `Columns` și-a schimbat tipul declarat (rupere la compilare pentru cine îl ținea ca `IReadOnlyList` — nimeni nu îl ținea așa, verificat prin grep; toată soluția se recompilează împreună). **PASUL 02 (pictogramă stânga pe elementele de navigație, `SLICE-0025-02-navitem-icon.md`):** cerere de operator în afara planului. `KBotNavItem.Image` — numele e `Image`, nu `Icon`/`IconImage`, fiindcă cererea a fost «ca orice pictogramă de buton», iar echivalentul exact e `Button.Image`: același nume, același tip, **același editor de imagine stoc** din grila de proprietăți (designer-ul depune imaginea în `.resx`-ul formularului); `KBotCaptionBar.IconImage` rămâne cu numele lui, abaterea e conștientă și cele două nu se întâlnesc niciodată în aceeași grilă. Tipul se scrie CALIFICAT (`System.Drawing.Image`) — în VB o proprietate `Image` ascunde numele de tip `Image` și declarația nu compilează altfel. **Fără `ImageAlign`** (un rând de navigație e «pictogramă apoi text» prin construcție), separatorii o ignoră ca pe `Key`/`Text`, iar elementul **NU deține imaginea** și nu o eliberează (ca `KBotCaptionBar`) — test dedicat, fiindcă o imagine partajată de două elemente nu are voie să moară odată cu primul. `KBotNavList.IconSize` (implicit 20, px logici) e **per bară, nu per element**: într-o bară pictogramele trebuie să fie de aceeași mărime, altfel textul nu se mai aliniază; sursa se scalează în pătrat, deci o probă de 64px nu impune lățimea rândului, iar `IconSize = 0` înseamnă «fără pictograme», nu «pictograme de zero pixeli care lasă o gaură». Geometria stă în TREI funcții folosite și la măsurare și la pictare (`IconSide`/`IconSlotWidth`/`IconRect`), ca cele două să nu se poată despărți; slotul se strânge într-un rând mai scund decât latura nominală. Pe un element **dezactivat pictograma se estompează** (`ImageAttributes` + `ColorMatrix`, luminanță standard + alfa 45%) — altfel un buton stins ar avea text gri lângă o pictogramă în culori vii; matricea în locul lui `ControlPaint.DrawImageDisabled`, care nu scalează. Theming `FileVersion` 1.1.0.0→1.2.0.0. **Corecție adusă și testelor pasului 1:** patru assert-uri comparau cu pixeli logici literali și ar fi picat pe un ecran la 150%; trec acum prin `ThemeShapes.ScaleDpi`. **Teste:** .NET **769 passed / 0 failed / 0 skipped** (715 la începutul feliei, 761 după pasul 7; Theming 31→59, Controls 240→261, App 143→148), build **0 erori / 0 avertismente**. **Cifrele planului erau învechite** (spunea Controls 134 / App 136 / soluție 436 — de pe vremea feliei 0022); la fel afirmația «`KBotNavList` nu are niciun test xUnit»: `KBotNavListSelectionTests.vb` exista (4 teste, din commit-ul `5f94f44`). Firul 0018 «fără teste xUnit pentru `KBotNavList`» **se închide acum** — separatorii, alinierea Near/Far și vizibilitatea (inclusiv «ascuns ⇒ `Rectangle.Empty`, sărit de `IndexAt` și de tastatură») sunt acoperite. ⚠️ **Deschis:** (a) **niciuna dintre cele șase verificări manuale din Visual Studio nu a rulat** — Toolbox, dialogul de colecție, rotunda în `*.Designer.vb`, chenarul roșu, `Running` fără animație; NU sunt revendicate ca verificate (lecția feliei 0023: «compilează curat și testele sunt verzi» nu spune nimic despre ce se vede pe ecran); (b) **nimeni nu a deschis `MainForm` în designer după migrare** — dacă VS reîncarcă cele opt elemente în dialogul de colecție și le rescrie identic la prima regenerare e ÎNTREBAREA DESCHISĂ a feliei; ce se știe: se compilează, se construiește headless, iar starea de rulare e identică cu cea de dinainte (fixată de cele 5 teste), și calea de întoarcere (blocul `AddItem`) e în istoricul git; (c) proba cea mai apropiată care S-A făcut: `TypeDescriptor` întoarce editorul **STOC** `CollectionEditor` pentru ambele colecții **fără niciun atribut `<Editor>`** (deci varianta de rezervă din plan nu e necesară), iar `DdfView.Designer.vb` conține deja un `KBotNavList` autorit de designer — designer-ul VS de pe mașina asta încarcă deja controlul; (d) nemigrate deliberat: nodurile `AdvancedTreeControl` (arborele vine din `GET /api/forexe/tree`, nodurile din designer ar fi aruncate) și coloanele celor șase vederi existente (rămân pe `AddColumn` în cod); (e) `DefaultEvent("SelectionChanged")` numește un eveniment cu semnătură VB, nu `EventHandler` — dacă dublu-click-ul pe control nu generează handler, asta e cauza, neverificat; (f) **pictograma nu a fost văzută pe ecran și editorul ei nu a fost deschis** — cum arată 20px lângă text, dacă estomparea de 45% e destulă pe Dark, și mai ales dacă rotunda unei IMAGINI printr-un element de COLECȚIE funcționează (designer-ul trebuie să scrie în `.resx` și să emită `CType(resources.GetObject("KBotNavItem1.Image"), Image)`) sunt neprobate; e prima proprietate din felie care are nevoie de fișierul de resurse al formularului. Nicio pictogramă reală nu a fost aleasă pentru cele opt intrări din `MainForm` — s-a cerut PROPRIETATEA, nu setul; dacă vor primi, sursa firească sunt generatoarele GDI tematizabile care există deja în `KBot.App` (`FxIcons`/`DdfIcons`/`PlatiIcons`/`RezervariIcons`/`ReceptiiIcons`), prin cod, nu prin `.resx`. **PASUL 05 (bară colapsabilă + `ItemPadding`, `SLICE-0025-05-navlist-collapse-padding.md`):** a doua cerere de operator în afara planului. `KBotNavList.ItemPadding` (px logici, implicit `6,6,6,6`) înlocuiește **marginea fixă de 6** din `RecalcLayout` — implicitul e bit cu bit vechiul comportament, fixat de test; `Control.Padding` moștenit e **ascuns din grilă** (`Shadows` + `Browsable(False)` + `Hidden`) fiindcă pe un layout owner-drawn nu face nimic, iar WinForms i-ar mai aplica ȘI autoscalarea formularului peste `ScaleDpi`-ul propriu al barei (aceeași valoare ajustată de două ori la 150%). Strângerea: `Collapsible` (implicit False) desenează un unghi («chevron») tematizat în colțul `CollapseCorner` (`TopLeft`/`TopRight`/`BottomLeft`/`BottomRight`, implicit `TopRight`), care parcurge ciclic `CollapseState` = `Expanded → Icons → Complete → Expanded`; butonul își **rezervă o bandă** (18+2*6) la capătul axei principale dinspre colț, ca să nu stea peste primul/ultimul buton. `Complete` = `18+2*6` px pe axa care se strânge și **niciun slot** (toate `Bounds` rămân `Rectangle.Empty`, deci nimic nu se pictează și nimic nu se poate apăsa — un buton invizibil dar apăsabil ar fi fost mai rău decât unul tăiat). `Icons` = `pad.Left+pad.Right+IconSize+2*8` (implicit 48) pe bară și `IconSize+2*8` (implicit 36) pe butoane, **peste `AutoSize` și `ItemWidth`**, cu pictograma centrată, textul ascuns, pastila badge-ului redusă la un punct și **inițiala** textului pe butoanele fără pictogramă; disponibilă doar pe verticală, cu `IconSize > 0` și măcar un buton **vizibil** cu `Image` (`IconsCollapseAvailable`). Butonul **sare** starea indisponibilă; setter-ul `CollapseState` **aruncă** pe ea (`InvalidOperationException`) — API-ul respectă regula «fără no-op-uri tăcute», operatorul nu e pedepsit pentru o alegere de autorare. „Mărimea inițială" e ultima mărime avută DESFĂȘURATĂ (reținută în `OnSizeChanged`, cu steag contra propriilor redimensionări), nu cea din constructor: cine lățește bara la 240 primește 240 înapoi, nu 170. `CollapseState` **nu se serializează** (e stare de rulare; serializată s-ar bate cu `Size`-ul scris tot de designer), iar click-ul pe buton e **ignorat la design time** (ar redimensiona controlul și ar murdări formularul). `MainForm.navViews` **nu** a primit `Collapsible = True` — s-a cerut capacitatea, nu activarea. `Theming` `FileVersion` 1.2.0.0→1.3.0.0. 25 de teste noi (`KBotNavListCollapseTests.vb`), soluția la **812 passed / 0 failed / 0 skipped**, build 0 erori / 0 avertismente. ⚠️ **Deschis, în plus față de (a)–(f):** (g) **nimic din pasul 05 nu s-a văzut pe ecran** — chevron-ul, hover-ul, pictograma centrată, punctul în locul pastilei și inițiala sunt probate doar prin `DrawToBitmap` headless («nu aruncă», nu «arată bine»), iar editorul de `Padding` din grilă n-a fost deschis; (h) strângerea schimbă `Width`/`Height`, deci merge cu `Dock.Left/Right/Top/Bottom` sau ancorare, dar **NU cu `Dock.Fill`** (containerul rescrie dimensiunea) — limitare consemnată, fără gardă în cod; (i) fără tooltip în `Icons` și fără acționare de la tastatură a butonului din colț (nu s-au cerut); (j) **pașii 03 (`ItemWidth`) și 04 (`AutoSize` pe element + măsurarea cu fontul semibold) există în cod și în teste, dar NU au worklog** — sunt descriși doar în comentariile din `KBotNavList.vb`/`KBotNavItem.vb`; gaura e din feliile anterioare, nu din 05. **PASUL 06 (pictograme pe butonul de strângere + unghiul care se întoarce, `SLICE-0025-06-navlist-collapse-button-icons.md`):** a treia cerere de operator în afara planului — «the expander button must have an exposed property to allow for puting a expanded icon and an collapsed icon in the designer (and in code); if none is supplied use the current `DrawCollapseButton`, but when collapsed it should show the other arrow `>` instead of the same `<`». Două proprietăți noi pe `KBotNavList`: `CollapseExpandedImage` (bara desfășurată ⇒ butonul strânge) și `CollapseCollapsedImage` (bara strânsă ⇒ butonul desfășoară), tip `Image` și nume cu `Image` ca la `KBotNavItem.Image` — asta e ce aduce **editorul de imagine stoc** în grilă și depune imaginea în `.resx`-ul formularului. Sunt **INDEPENDENTE**: cine dă doar una primește unghiul pe cealaltă stare (s-a cerut «o pictogramă», nu «un set complet»); rezolvarea stă într-o singură funcție (`CollapseButtonImage`) folosită și la pictare și de cârligul de test. **O singură pictogramă pentru amândouă stările strânse** (`Icons` și `Complete`) — din amândouă butonul face același lucru. `ShouldSerialize`/`Reset` private pe amândouă (un tip referință n-are `<DefaultValue>` utilizabil în VB; fără ele designer-ul ar fi scris `CollapseExpandedImage = Nothing` în fiecare formular). Bara **NU deține** imaginile și nu le eliberează (`Dispose` neatins), ca `KBotNavItem.Image`/`KBotCaptionBar.IconImage` — test dedicat. Se desenează prin `DrawItemImage`, scalate în pătratul butonului (18 px logici − 2 aer), cu fundalul de hover păstrat dedesubt (el spune «se poate apăsa», nu depinde de felul glifei). **Defectul de unghi:** `forward` era `(_collapseState = Complete)`, deci pe `Icons` unghiul rămânea identic cu cel din starea desfășurată — bara strânsă la pictograme arăta tot `<` și butonul părea mort; acum `ChevronPointsToExpand()` = `_collapseState <> Expanded`, adică se întoarce pe ORICE stare strânsă. Contează cu atât mai mult fiindcă **ciclul a fost scurtat de operator** (ramura `Icons → Complete` comentată în `CycleCollapse`, necomisă, găsită în arbore: «nu imi place cum arata fara nicio iconita!»): butonul face acum `Expanded ↔ Icons` cât timp `Icons` e disponibil (și `Expanded ↔ Complete` pe o bară fără nicio pictogramă), deci `<` era singurul unghi pe care apuca să-l vadă cineva. Cele **două teste din 0025-05 care fixau ciclul în trei trepte erau deja roșii** din cauza acelei schimbări (înainte de pasul 06) — sunt aliniate acum la ciclul care e în cod, cu motivul scris în test; comportamentul de rulare NU a fost dat înapoi, decizia e a operatorului. `Controls` `FileVersion` 1.4.0.0→**1.5.0.0** (`AssemblyVersion` pe loc: doar adăugiri). 7 teste noi, soluția la **819 passed / 0 failed / 0 skipped**, build 0 erori / 0 avertismente. Nimic în `MainForm`/`DdfView`: s-a cerut proprietatea, nu un set de pictograme. ⚠️ **Deschis, în plus:** (k) **nimic din pasul 06 nu s-a văzut pe ecran** — cum arată o pictogramă de 14 px efectivi în pătratul de 18, dacă întinderea fără păstrarea proporției e destulă pentru pictograme ne-pătrate, și dacă unghiul întors se citește corect din `Icons`, toate doar prin `DrawToBitmap` headless; editorul de imagine din grilă tot n-a fost deschis (aici însă pe o proprietate DIRECTĂ pe control, nu printr-un element de colecție ca la firul (f) — calea simplă a celor două); (l) fără `CollapseButtonSize`, fără păstrarea proporției și fără pictogramă separată de hover/apăsat (nu s-au cerut). **PASUL 07 (eticheta plutitoare a barei strânse, `SLICE-0025-07-navlist-collapsed-flyout.md`):** a patra cerere de operator în afara planului — «when is collapsed and i hover any navbar button (except for the sep) to have a sort of tooltip with the entire button, but it mustn't be a real tooltip (yellow and ugly), but some kind of floating control which takes on all the props of the hovered button and displays it on a small timer from left to right». Fișier nou `NavList/KBotNavFlyout.vb` (fereastra + `KBotNavFlyoutStyle`). **Cheia e de unde PLEACĂ:** eticheta nu se așază lângă buton (aia ar fi tot un tooltip, doar mai frumos) — pleacă **exact din dreptunghiul butonului strâns** și crește doar spre dreapta, cu pictograma centrată în prima bandă, adică fix peste locul unde bara o pictează deja; se vede un buton care se DESFĂȘOARĂ, cu pictograma nemișcată și textul ieșind de sub ea, nu două pictograme alăturate. Asta închide și ambiguitatea cererii («the entire button» vs. «doar ce s-a tăiat»): se desenează butonul întreg, dar suprapus peste el însuși. La progres 0 eticheta e **nedeosebită** de buton (test dedicat). Preia toate proprietățile butonului — pictogramă (estompată prin aceeași `KBotNavList.DrawItemImage`, devenită `Friend`), text, pastilă, fundal de selecție/hover, accent + semibold pe cel selectat, text stins pe cel dezactivat, contur `BorderColor`, raza schemei; regulile de culoare **nu se rescriu** în fereastră, bara le calculează ca în `OnPaint` și i le predă în `KBotNavFlyoutStyle`. Fereastra: `WS_EX_NOACTIVATE` + `ShowWithoutActivation`, `WS_EX_TOOLWINDOW`, `AutoScaleMode.None` (bara dă px deja scalați), `Region` tăiată din calea rotunjită, și **`HTTRANSPARENT` pe `WM_NCHITTEST`** — asta nu e cosmetică: eticheta acoperă chiar butonul de sub cursor, deci fără click-through apariția ei ar fi însemnat «mouse-ul a părăsit bara» ⇒ se ascunde ⇒ mouse-ul e iar pe bară ⇒ apare, la infinit. Trei proprietăți în grilă: `CollapsedFlyout` (**True implicit**, spre deosebire de `Collapsible` — poarta rămâne `Collapsible`, dar o bară STRÂNSĂ fără etichete e nefolosibilă, nu mai scrie nimic pe butoane), `FlyoutDelay` (250 ms) și `FlyoutSlideDuration` (120 ms, pas fix de 15 ms, oprire exact la 1.0); negativele se aduc la 0 ca peste tot. Primesc etichetă doar butoanele **vizibile cu text**; săriți separatorii (cerut), cele ascunse, cele fără text și butonul din colț (prioritar) — dar **butoanele DEZACTIVATE primesc**, spre deosebire de hover-ul obișnuit care le sare: tocmai pe bara strânsă e cel mai greu de ghicit ce e butonul stins. Se retrage la ieșirea de pe bară, la trecerea pe alt buton (repornește acolo) și la **orice invalidare de layout** (ține minte un index și un dreptunghi vechi); mișcarea în ACELAȘI buton nu repornește nimic (altfel temporizarea s-ar reseta la fiecare pixel); se reîmprospătează fără retragere la schimbarea selecției și a temei. **Întrebarea „nu știu încă pentru orizontală" se închide singură:** `Icons` nu e disponibil pe orizontală, iar în `Complete` niciun element nu primește slot — deci pe o bară orizontală nu EXISTĂ stare strânsă cu butoane de survolat, prin construcție, nu prin gardă. `Controls` `FileVersion` 1.5.0.0→**1.6.0.0**. 21 de teste noi (`KBotNavListFlyoutTests.vb`), soluția la **843 passed / 0 failed / 0 skipped**, build 0 erori / 0 avertismente. ⚠️ **Deschis, în plus:** (m) **nimic nu s-a văzut pe ecran** (al patrulea pas la rând) — fereastra NU se afișează headless, deci ce e probat e decizia și geometria, nu pixelii; nedovedite: că apare unde crede bara, că `HTTRANSPARENT` chiar lasă mouse-ul să treacă (dacă nu, simptomul e o etichetă care pâlpâie la infinit), că `WS_EX_NOACTIVATE` nu fură focusul, cum arată desfășurarea de 120 ms, și dacă `Region` taie colțurile curat sau zimțat; (n) **fără clamp la marginea ecranului** — eticheta crește spre dreapta, deci o bară strânsă lipită de marginea dreaptă a ecranului ar împinge-o afară; nu s-a pus gardă fiindcă o bară de navigație strânsă e ancorată la stânga formularului prin construcție (`Dock.Left`); (o) **fereastra nu urmărește formularul** — o mutare programatică a formularului cât timp eticheta e afară o lasă în coordonatele vechi până la următoarea mișcare de mouse; (p) **fără tastatură** (eticheta e legată de hover, nu de selecție, deci Sus/Jos pe o bară strânsă nu scoate nimic) și **fără retragere animată** (dispare instantaneu; animația e într-un singur sens, cum s-a cerut). **PASUL 08 (rază de colț per bară + gradient „modern", `SLICE-0025-08-navlist-corner-radius-gradient.md`):** a cincea cerere de operator — «the appearing button must have rounded corners (also the buttons in the navlist)… that property should be exposed in the designer per navlist (not per button) and also it should have a modern gradient. also it should use the theming system». **Gradientul stă în MOTOR, nu în control:** `ThemeShapes` primește `Lighten`/`Darken`/`FillModern` PUBLICE (`KBot.Theming`) — „also it should use the theming system" s-a citit ca cerință de arhitectură, nu doar de culori. `FillModern` **nu introduce nicio culoare nouă**: primește culoarea de bază din paletă (fundalul de selecție derivat din accent, `ButtonHoverColor`) și îi derivă cele două capete, deci gradientul se schimbă odată cu schema și merge la fel pe scheme deschise și întunecate. Capetele sunt **asimetrice** (jos se închide cu 3/4 din cât se deschide sus) — unul simetric arată a buton Windows XP; iar `strength = 0` ⇒ umplere PLATĂ pe drumul `SolidBrush`, adică bit cu bit ce se picta înainte. Zona se umflă cu 1 px sus/jos fiindcă `LinearGradientBrush` pictează primul/ultimul rând cu culoarea capătului opus. Pe control: `ItemCornerRadius` (implicit **-1 = raza schemei active**, adică `ThemeStyleOptions.CornerRadius` — 0 pe Classic/Dark, 8 pe Modern; `-1` ca sentinel fiindcă pe o rază `0` e o valoare reală, «colțuri drepte chiar și pe o schemă rotunjită») și `ItemGradient` (0..100, implicit 14, 0 = plat), amândouă **pe BARĂ, nu pe element** — cerut explicit și oricum singura variantă corectă (butoanele unei navigații trebuie să aibă aceeași formă, ca la `IconSize`); există un test care CADE dacă cineva adaugă vreodată o rază pe `KBotNavItem`. Raza se rezolvă într-o singură funcție (`ItemRadius()`) folosită de butoane + butonul din colț + etichetă, iar eticheta primește exact raza și gradientul barei (fixat de test): ea trebuie să fie butonul care se desfășoară, deci o formă care nu se potrivește ar strica iluzia mai rău decât lipsa ei. `Theming` `FileVersion` 1.3.0.0→**1.4.0.0**, `Controls` 1.6.0.0→**1.7.0.0**. 11 teste noi (`KBotNavListShapeTests.vb`), soluția la **854 passed / 0 failed / 0 skipped**, build 0 erori / 0 avertismente. **Prima bucată din familia asta de felii care se probează HEADLESS pe bune:** patru teste citesc PIXELI dintr-un bitmap și confirmă că `strength=0` e identic cu umplerea plată, că sus chiar se deschide și jos se închide, că e asimetric, și că merge și pe o culoare întunecată. ⚠️ **Deschis, în plus:** (q) testele de pixeli probează că `FillModern` face ce spune, **nu** că 14 e intensitatea potrivită sau că 8 px de rază arată bine pe un rând de 36 — alegerea implicitelor e o judecată, nu o măsurătoare; (r) `Region`-ul ferestrei etichetei taie colțurile **fără antialiasing**, deci pe o rază mare pot ieși zimțate (alternativa, fereastră stratificată cu alfa, e mult mai scumpă și nu s-a cerut); (s) `FillModern` e disponibilă oricui, dar o folosește **doar `KBotNavList`** — `KBotDataView`/`KBotCaptionBar`/`KBotBusyBar`/`KBotNotice`/`KBotTextField`/`ModernRenderer` rămân pe umplere plată, fiindcă trecerea generală la gradient e o decizie de aspect pe toată aplicația, nu un efect colateral; (t) `ThemeStyleOptions` **nu** a primit flag de gradient — intensitatea stă pe control; dacă gradientul se generalizează, locul lui firesc e o proprietate nouă în schemă (serializată JSON), moment în care `ItemGradient` ar trebui să capete și el un sentinel «ia din schemă», ca `ItemCornerRadius` azi. **PASUL 09 (latura butonului de strângere, `SLICE-0025-09-navlist-collapse-button-size.md`):** a șasea cerere de operator — «i also need an exposed property for the size of the expander button for navlist»; **închide firul (l)**. `CollapseButtonSize` (px logici, implicit **18** = exact constanta fixă de până acum, deci o bară care n-o atinge arată identic). **Nu e doar desen, e layout:** toate cele trei locuri care depindeau de 18 treceau deja prin `CollapseButtonSide()`, deci urmează automat — pătratul butonului, **banda rezervată** pe axa principală (`latură + 2*6`, care mută primul/ultimul element) și **lățimea din `Complete`** (aceeași formulă), plus `Icons` prin `Max(...)`. Din cauza ultimelor două, setter-ul **reaplică dimensiunea pe loc** (`ApplyCollapseExtent`) dacă bara e strânsă chiar atunci — altfel o bară pe `Complete` ar fi rămas la lățimea butonului vechi, adică proprietatea ar fi părut inertă exact în starea unde contează; test dedicat, inclusiv că desfășurarea readuce mărimea OPERATORULUI, nu una derivată din buton. Ce e desenat în buton se scalează cu el: unghiul era deja proporțional (braț = latură/5, grosime = latură/10), dar aerul din jurul pictogramei autorate (0025-06) era **fix 2 px** — devine `latură \ 9` (min 1), adică tot 2 la latura implicită, deci zero schimbare din oficiu, dar o pictogramă care nu mai stă lipită de margini la 44 și nu dispare la 8. **`0` = fără buton**, ca `IconSize = 0`: nu se desenează, `CollapseButtonRect()` e `Empty` (nici hover, nici click) și **nu mai rezervă bandă**, altfel ar fi rămas o gaură de 12 px pentru un buton inexistent. E stare validă (o aplicație care strânge bara din propriul buton de toolbar nu vrea unghiul din colț) **și capcană scrisă ca atare în doc**: cu `Collapsible = True` și latura 0, operatorul nu mai poate strânge/desfășura cu mouse-ul — doar din cod; testul verifică ambele jumătăți. Negativele se limitează la 0. `Controls` `FileVersion` era **deja** pe 1.8.0.0 în arbore (HEAD pe 1.7.0.0), bump-ul necomis acoperă și felia asta. 6 teste noi (filtrul `KBotNavList`: 116 → 122), soluția la **868 passed / 0 failed / 0 skipped**, build 0 erori / 0 avertismente — cifra pe soluție NU e comparabilă cu cele 854 de la 0025-08, fiindcă între timp operatorul a comis feliile anterioare și a adăugat WIP propriu pe `AdvancedTreeControl`. ⚠️ **Deschis, în plus:** (u) **marginea de 6 px din jurul butonului NU e expusă** — intră în bandă și în lățimea strânsă la fel de mult ca latura, deci controlul complet al colțului ar cere o proprietate în plus (`CollapseButtonMargin`); (v) **nicio gardă pentru un buton mai lat decât bara DESFĂȘURATĂ** (dreptunghiul ajunge la coordonate negative și butonul iese pe stânga; în stările strânse nu se poate întâmpla, acolo lățimea barei se calculează DIN latură) — aceeași filozofie ca la `ItemWidth`, valoarea e a operatorului; (w) tot nimic pe ecran, a cincea felie la rând — nu se știe cum arată 30 față de 18 lângă rânduri de 36 px, nici dacă unghiul rămâne lizibil la laturi mici |

| 0026 | Consolidarea controalelor în `KBot.Controls`, grupate pe familii | DONE (code) / **NEDESCHIS ÎN DESIGNER** | `SLICE-0026-controls-consolidation.md` | Cerere de operator: «all custom controls MUST live in kbot.controls… their own separate folder… grouped logically». Felie **pur structurală, zero schimbări de comportament**. Cele cinci controale din `KBot.Theming` (`KBotNavList`+`KBotNavItem`, `KBotCaptionBar`, `KBotBusyBar`, `KBotNotice`, `KBotTextField`) au trecut prin `git mv` în `KBot.Controls`, fiecare în folderul lui, cu enum-urile lor cu tot; ce era deja în `KBot.Controls` s-a regrupat din rădăcină în `Tree/` (15 partial-uri + `ColumnDef`/`TreeLogger`/`TooltipTableModel`/`NodeDebugInfo`/`FrmNodeDebug`), `DataView/` (9 partial-uri + coloană/rând/colecție + cele 4 enum-uri) și `DataView/Events/` (verificat prin grep că `EventArgs`-urile sunt consumate exclusiv de grilă, nu de arbore); `Adobe/` era deja corect. **Perimetru întrebat, nu presupus:** form-urile de bază `KBotThemedForm`/`KBotShellForm` **NU** s-au mutat (nu-s controale — și așa `KBot.Theming` rămâne fără nicio referință la `KBot.Controls`, deci sensul referinței rămâne unic, fără ciclu), iar vederile din `KBot.App\Views\` **NU** s-au mutat (sunt ecrane: implementează `IAngajamentView` și cheamă API-ul; mutate ar fi tras `KBot.Api`/`KBot.Forexe`/`KBot.Xfa` într-o bibliotecă de UI care trebuie să rămână frunză). **Lărgiri minime în motor,** fiindcă membrii `Friend` au devenit invizibili peste granița de assembly: `ThemeShapes` `Friend`→`Public` (nu putea fi MUTAT — îl folosesc și `ModernRenderer` și `KBotShellForm`), iar `NativeMethods` devine modul `Public` **cu o suprafață publică de exact un membru** (`DragMove`, cerut de `KBotCaptionBar`): `SetTitleBarDark`/`SetRoundedCorners`/`ApplyMinMaxInfo`/`ApplyWindowTheme` au fost COBORÂTE `Public`→`Friend`, fiind consumate doar din `KBot.Theming`. **Capcană reală prinsă de compilator:** în fișierele de designer forma scurtă `Controls.KBotNavCorner` NU compilează — `Controls` se leagă întâi de `Control.ControlCollection` moștenit (`BC30456`), coliziune pe care vechiul `Theming.X` nu o avea; de aceea tipurile se scriu CALIFICAT COMPLET. La fel, un `Imports KBot.Controls` în `LoginForm.vb` face `Write` ambiguu între `GlobalErrorLog` și `AdobeHostLog` — s-a calificat `KBot.Controls.NoticeKind.Error` în loc. Cele 49 de teste `KBotNavList*` au trecut în `KBot.Controls.Tests` (fără `DisableTestParallelization`: verificat că nimic de acolo nu atinge starea statică `ThemeManager`/`ThemeStore`). `Controls` `FileVersion` 1.3.0.0→**1.4.0.0**; `Theming` era deja pe 1.3.0.0 din 0025-05 (necomis), acoperă și asta; **`AssemblyVersion` stă pe loc peste tot**, deși mutarea a scos cinci tipuri publice din `KBot.Theming` — toată soluția se recompilează împreună (dacă se dorește altfel, e o decizie separată, cu setul COMPLET livrat). Build **0 erori / 0 avertismente**, **812 passed / 0 failed / 0 skipped** — aceleași 812 ca înainte, redistribuite (Theming 76→27, Controls 286→335). Regula e scrisă acum și în `CLAUDE.md` (regulă de casă) și în `src/KBot.Controls/README.md` (tabelul de foldere). ⚠️ **Deschis:** nimic nu s-a deschis în designer-ul VS după mutare — `MainForm`/`LoginForm`/`DdfView` au acum controale din alt assembly, iar Toolbox-ul și cache-ul de design-time trebuie să le regăsească sub `KBot.Controls.*`; firele (a)–(j) ale feliei 0025 rămân toate deschise |

| 0027 | `AdvancedTreeControl` — banda de căutare, tematizare și suprafață de designer | **DONE — VERIFICAT PE ECRAN 08.08.2026** | `SLICE-0027-tree-designer-surface.md` | Cerere de operator în două runde: (1) «even though i set in the tree to have searchbar visible it's not showing it… the designer should properly show it… build something like the playground for the dgv»; (2) culorile din designer nu se aplică + antet (font/textalign/degrade/imagini) + banda de căutare (padding și imagine pe butonul de golire, ESC, fonturi în loc de bold/italic) + «a collection of keys for nodes and also a collection of images which can be imported through the designer IDE». **Bug-ul 1 — `SearchShow` era proprietate MOARTĂ pe calea designerului:** singurul cod care deschidea banda trăia în `ResolveHeaderIcons`, adică pe calea XML FOREXE (`Tree.Builder`), pe care un formular autorit în designer n-o cheamă niciodată. Acum setterul cheamă `ApplySearchShow()` (bandă permanentă fără iconiță de toggle; iconița prezentă o comută), la fel `ResolveHeaderIcons` și setterul `HeaderSearchIcon`, iar un `OnHandleCreated` re-aplică proprietatea — ordinea în care designerul scrie proprietățile în `InitializeComponent` NU e a controlului. `CloseSearchMode` s-a spart în gardă + `ForceCloseSearchMode`. **La design-time banda se DESENEAZĂ** (`DrawSearchBarPreview`: etichetă, casetă, placeholder, ✕), fără controale copil reale — un `TextBox` viu pe suprafața de design fură click-urile; detecția e `KBotDesignTime.IsDesignTime`, nu `Control.DesignMode` (pe net8.0-windows designerul rulează în ALT PROCES). **Bug-ul 2 — culorile din designer, două cauze structurale:** (a) `ThemeManager.Traverse` RECURGE în copiii oricărui control care nu e `IThemedControl`, deci `TextBox`-ul și eticheta interne ale benzii cădeau pe regulile generice pe tip (`TextBox → InputBackColor`) și `SearchBoxBackColor` era șters la FIECARE aplicare de temă — exact capcana din comentariul lui `IThemedControl`, care lovise deja `KBotTextField`; (b) `MainForm.OnThemeChanged` + cele patru vederi împingeau explicit paleta peste designer. **Arborele implementează acum `IThemedControl`** (retrofitul pe care CLAUDE.md îl amâna), traversarea se oprește la el, împingerile manuale au dispărut din shell și din vederi. **Contract nou de culoare: `Color.Empty` = auto/din temă, orice culoare setată câștigă definitiv**, cu `ShouldSerialize*`/`Reset*` pe fiecare — fără ele VS serializa implicitul REZOLVAT, care devenea «alegere»: așa ajunsese paleta luminoasă înghețată în cinci fișiere de designer (13–14 linii/fișier, scoase; s-a păstrat doar verdele deliberat din `MainForm`). **`BackColor`/`ForeColor` au avut nevoie de un al doilea pas, semnalat de operator la recitire:** ele nu sunt culori proprii ale arborelui, ci `Control.BackColor`/`ForeColor`, unde contractul `_auto*` nu ajunge — iar `Control.ShouldSerializeBackColor` întoarce True de îndată ce proprietatea a fost SCRISĂ vreodată, ceea ce arborele face singur de două ori (constructor + `ApplyTheme`). VS ar fi serializat deci un `tree.BackColor = <culoarea temei>` nealeasă de nimeni, iar la reîncărcare linia ar fi trecut prin setterul public și ar fi FIXAT-o — bug-ul reintrodus pe ușa din dos, și chiar mecanismul prin care `tree.BackColor = Color.White` ajunsese în cele cinci designere. Corectat prin suprascrierea `ShouldSerializeBackColor`/`ShouldSerializeForeColor` (răspund din steagul de fixare, nu din punga de proprietăți) + `ResetBackColor`/`ResetForeColor`. Verificat că restul controalelor tematizate NU au aceeași gaură — niciunul nu scrie `BackColor`/`ForeColor` în `ApplyTheme`, se pictează din paletă în `OnPaint`; arborele era expus fiindcă moștenește o suprafață pictată real (`e.Graphics.Clear(Me.BackColor)`). **Curățenie dusă până la capăt, empiric** («ce scrie designerul pentru un arbore neatins?» — numărând liniile `tree.*` din cele cinci fișiere, șapte proprietăți apăreau în TOATE): `TreeFont` și cele trei `Size` de iconițe n-aveau `ShouldSerialize` (nici `Font`, nici `Size` nu pot purta `<DefaultValue>`); `Font` cădea în aceeași capcană ca `BackColor` (constructorul îl atribuie); iar `AutoScrollMinSize`/`AutoScrollPosition`, fiind `Shadows`, NU moștenesc atributele de serializare ale bazei și au fost ascunse cu `Browsable(False)` + `DesignerSerializationVisibility.Hidden`. Designerele curățate din nou (−4 linii/fișier); ce rămâne e verificat alegere reală. **Limită cunoscută, NEreparată deliberat:** steagul de fixare rezolvă doar serializarea — constructorul atribuie în continuare «Segoe UI, 9» explicit, deci arborele nu moștenește fontul ambiant și `ApplyBaseFont` nu ajunge la el; s-ar repara ștergând o linie din constructor, dar `Me.Font` alimentează înălțimea benzii de căutare și `RecalculateItemHeight`, deci se VEDE — după un verdict vizual dat, nu se schimbă unilateral. `ResetFont()` redă moștenirea, iar un test fixează comportamentul real. Câmpurile `_auto*` pornesc de la culorile hardcodate istorice, ca bancul de probă și calea FOREXE/VBA să arate neschimbat. **Antet:** `HeaderFont`, `HeaderTextAlign` (toate cele nouă poziții), `HeaderBackStyle` (Solid/GradientVertical/GradientHorizontal) + `HeaderGradientEndColor` gol = automat spre alb/negru după **luminanța** bazei (BT.601 — arborele n-are nevoie să știe ce temă rulează), iar cele trei iconițe au devenit `Browsable` + serializabile (selectorul standard de imagini le pune în `.resx`), cheile `*IconKey` rezolvându-se din `NodeImages`. **Căutare:** `SearchClearButtonPadding`, `SearchClearButtonImage`, **ESC** golește (pe casetă goală închide banda, dar e no-op pe banda permanentă), `SearchBarLabelFont` + `SearchBarFont` ÎN LOCUL lui Bold/Italic (șterse la cerere); `SearchBarFontName`/`FontSize` supraviețuiesc ca accesori `Browsable(False)` peste `SearchBarFont` — același tipar ca `FontName`/`FontSize` peste `TreeFont` — ca aplicatorul XML și designerele existente să compileze neatinse. **Colecții:** `NodeImages As ImageList` (pozele se importă prin editorul standard al listei) și `Nodes` cu definiții PLATE (`TreeNodeDefinition`, legătură prin `ParentKey`) materializate în `Items` — plat, nu ierarhic, din exact motivul feliei 0025 pentru `KBotNavItem` (un editor propriu ar cere assembly de design-time contra `Microsoft.WinForms.Designer.SDK`); ordinea din colecție = ordinea între frați, un părinte declarat mai jos funcționează, un `ParentKey` inexistent urcă nodul la rădăcină, iar **definițiile sunt sursa DOAR cât timp colecția e nevidă**, deci vederile care umplu la rulare nu sunt niciodată golite. **Banc:** `TreePlaygroundForm` nou (perechea playground-ului `KBotDataView`: toate proprietățile comutabile la rulare în cinci secțiuni, `ColorDialog`/`FontDialog`, buton care probează `Nodes`+`NodeImages`), iar `TreeVisualForm` pornește acum cu antet + bandă permanentă. **Build 0 erori / 0 avertismente, 884 passed / 0 failed** (arborele n-avea NICIUN test înainte; felia adaugă 23, inclusiv calea de design-time montată sub un `ISite.DesignMode=True`). **Trei defecte reale prinse de teste, nu de compilator:** `Blend` arunca `OverflowException` (în VB `Byte - Byte` rămâne `Byte`, deci tot degradeul pe temă întunecată exploda), ordinea fraților ieșea inversată, iar `SearchClearButtonPadding` nu ajungea la buton; plus, găsit la recitire, constructorul făcea `Me.BackColor = White` și marca suprafața drept «fixată de operator», ținând arborele alb pe tema întunecată (acum `MyBase.BackColor`). **Regresie introdusă ȘI reparată în felie:** `KBot.Controls.Tests` n-avea `DisableTestParallelization` — controale WinForms reale pe fire STA + clase rulate în paralel de xUnit + starea statică ne-thread-safe din `NativeWindow` = `IndexOutOfRangeException` din `Dictionary.TryInsert`; în plus un test chema `ThemeManager.SetScheme`, care difuzează peste `Application.OpenForms`, deci peste formularele altor teste, de pe alt fir (`KBot.Theming.Tests` avea deja aceeași protecție, din același motiv). `Controls` 1.7.0.0→**1.9.0.0**, `DevHarness` 1.0.8.0→**1.0.10.0**, `App` 1.0.6.0→**1.0.7.0**. ⚠️ **Deschis:** `ComboBox` theming retrofit rămâne amânat (CLAUDE.md îl lista împreună cu arborele — jumătatea «arbore» e făcută); cheile de noduri nu se validează (deliberat, ca la `KBotNavItemCollection`); `Escape` pe arborele însuși, nu în casetă, nu golește căutarea |
| 0027-02 | `AdvancedTreeControl` — un singur font, banda de subsol și strângerea | DONE (code) / **strângerea VĂZUTĂ PE ECRAN 09.08.2026 (raport de operator + corectură)**, restul benzii neconfirmat bucată cu bucată | `SLICE-0027-02-tree-footer-collapse.md` | Cerere de operator: «has two properties: TreeFont and Font. Only one remains (Font)» + o secțiune NOUĂ de subsol, soră cu antetul, cu buton de strângere. **(0)** Arborele avea DOUĂ fonturi: `TreeFont` (`Consolas, 9`) desena nodurile, iar `Font` (`Segoe UI, 9`) dădea înălțimea rândului, banda de căutare și implicitul antetului — două surse de adevăr pentru aceeași măsură, deci text care putea fi mai mare decât rândul care-l ține. A rămas `Font`; au plecat și accesoriile ascunse `FontName`/`FontSize`. Cele patru locuri care puneau explicit «Segoe UI, 9» (`Plati`/`Receptii`/`Rezervari` + `DdfView.Designer.vb`) au fost ȘTERSE, nu traduse: valoarea e acum implicitul, iar scrisă explicit ar fi FIXAT fontul (`_fontPinned`) și l-ar fi făcut surd la `ThemeManager.ApplyBaseFont`. **Defect prins pe drum, pe cod nemodificat:** `ResetFont` stingea steagul ÎNAINTE de `MyBase.ResetFont()`, care scrie `Font = Nothing` prin setterul virtual — al nostru — și-l aprindea la loc, deci reset-ul nu avea niciun efect asupra serializării, adică exact capcana pe care 0027 o repara; ordinea e acum «resetează baza, apoi stinge steagul», la fel în `ResetBackColor`/`ResetForeColor`. **(1)** Subsol: `.Footer.vb` + categoria «K-BOT Arbore - Subsol» după tiparul antetului (`Color.Empty` = din temă, pereche `ShouldSerialize*`/`Reset*` peste tot) — înălțime/fundal/degrade, `FooterLeftIcon(Key)` + `FooterIconSize`, caption cu font și culori proprii (`FooterCaptionBackColor` gol = FĂRĂ plajă, nu «din temă» — singura culoare a arborelui cu sensul ăsta), `FooterTextAlign`; buton de strângere cu mărime, două pictograme (lipsă ⇒ unghi desenat) și poziție, **butonul în stânga ignorând `FooterLeftIcon`** cum s-a cerut; `MinimumCollapsedWidth` = 100; `Collapsed`/`ToggleCollapse()`/`CollapsedChanged`, cu lățimea desfășurată reținută la fiecare `OnResize` care nu e al nostru — mecanismul din `KBotNavList`, altfel prima strângere ar deveni noua «lățime desfășurată». Subsolul IESE din zona de noduri (clip + buclă `OnPaint`, `RefreshScrollVisibility`/`UpdateVScrollMaximum`, roata, `EnsureNodeVisible`, `HitTestItem`), iar bara de derulare se oprește deasupra benzii ca săgeata ei de jos să nu cadă peste buton. **Nodul plutitor** (`TreeNodeFlyout`) e sora lui `KBotNavFlyout`, cu aceleași trucuri (`WS_EX_NOACTIVATE`, `WS_EX_TOOLWINDOW`, `HTTRANSPARENT` — fără el hover-ul s-ar pierde sub propria fereastră, la infinit) și cu geometria în funcții pure, calculabile fără ecran; iconița și textul se desenează la ACELAȘI X ca în arbore (`NodeTextStartX`/`NodeIconRect` repetă `DrawContent`), ca desfășurarea să pară a rândului, nu a unei etichete alăturate; timpii sunt `FlyoutDelay`/`FlyoutSlideDuration`, aceleași nume ca la NavList. **Al doilea defect prins de teste:** prima variantă ținea dreptunghiul butonului într-un câmp scris de `DrawFooter`, deci apăsarea depindea de o repictare anterioară — acum desenul, hit-testul și testele citesc aceeași funcție pură. **(2)** Playground: scos `TreeFont`, adăugate `Font`, `LeftIconSize`/`RightIconSize`/`HeaderIconSize`, secțiunea Culori (7 culori), culorile de tooltip, `SearchBarLabelForeColor`, `SearchMode` și cele două secțiuni de subsol; arborele e acum docat `Left` într-o gazdă `Fill`, fiindcă strângerea scrie `Width` și un control docat `Fill` n-are lățime proprie. **426 teste în `KBot.Controls.Tests`** (411 + 15 noi în `AdvancedTreeFooterTests`), 0 erori, 1 avertisment preexistent (`MSB3825` pe `DdfView.resx`). Cele 6 picuri din `Domain`/`Api`/`App` (familia `EtichetaRevizie`) sunt PREEXISTENTE — verificate prin `git stash` + rulare pe arborele curat. `Controls` 1.9.0.0→**1.10.0.0**, `DevHarness` 1.0.11.0→**1.0.12.0**, `App` 1.0.7.0→**1.0.8.0**. **Corectură 09.08.2026, după prima probă pe ecran** («it collapses the tree then imediately it expands it again… in the playground is working»): în `MainForm` arborele e `Dock=Fill` în `split.Panel1`, deci lățimea lui o dă `SplitterDistance` — `Me.Width = 100` era dat înapoi de layout la următoarea trecere. Contractul era scris în worklog, dar nimeni nu-l implementa ȘI controlul se bătea cu layout-ul în loc să tacă. Ambele jumătăți reparate: `HostOwnsWidth`/`ExpandedWidth` pe control (când lățimea nu e a lui, nu mai scrie `Width` deloc — doar schimbă starea și ridică evenimentul) + `MainForm.tree_CollapsedChanged` care mută splitter-ul la `MinimumCollapsedWidth + Panel1.Padding.Left`, coboară temporar `Panel1MinSize` (era 240, adică peste ținta de 111, deci păzea și comanda, nu doar tragerea), fixează splitter-ul cât e strâns și clamp-ează distanța ca o fereastră îngustă să nu transforme apăsarea în `InvalidOperationException`. 428 teste. **A doua corectură, tot 09.08.2026** («showrighticononhover SHOULDN'T reserve the space by default»): `DrawContent` retrăgea marginea dreaptă a textului ori de câte ori nodul avea `RightIcon`, FĂRĂ să se uite dacă iconița e pe ecran — o fâșie goală pe fiecare rând pentru o iconiță pe care n-o vede nimeni până la survolare, adică fix ce anulează rostul lui `ShowRightIconOnHover`. Regula stă acum într-un singur loc (`RightIconGutter`): permanentă ⇒ ia mereu locul; hover-only ⇒ nu ia nimic cât nu e survolat nodul, și îngustează textul abia când apare; `ReserveRightIconSpace = True` ⇒ loc fix și pentru ea, ca alegere explicită. Banda de coloane rămâne DELIBERAT ne-condiționată de hover (`ReservedRightIconWidth`) — o geometrie pe tot controlul care se re-așază la fiecare trecere a cursorului ar fi de nefolosit. Prins alături: zona de clic a iconiței avea marginea scrisă `6`, adică implicitul lui `RightIconRightPadding`, deci se despărțea de zona desenată de îndată ce cineva atingea proprietatea. 434 teste. **A treia corectură, tot 09.08.2026** («when the tree is collapsed and there is a selected node, that node MUST NOT DO THE FLYOUT thing! Only for the selected row»): `FlyoutSelectedNode` (implicit **False**) scutește nodul SELECTAT de etichetă — el e singurul pe care operatorul îl are deja în minte, deci eticheta nu-i spune nimic nou, dar acoperă vederea de alături exact unde se uită. Suprimarea e pe UN rând, nu o stingere a etichetei: vecinii ies în continuare. Trei căi ca eticheta să nu rămână agățată — ținta o sare din start, `EnsureCollapsedFlyoutStillAllowed` o retrage când rândul de sub ea devine selectat (`OnMouseDown`, setterul `SelectedNode`, setterul opțiunii), iar `RenderCollapsedFlyout` re-verifică la fiecare cadru fiindcă selecția se poate muta din tastatură cât desfășurarea e în curs. 438 teste. **Completare, tot 09.08.2026** («i also need a righticon in the footer similar to the right icon in the header. if collapse button is on the right, the right button is not displayed. the same if it is on the left»): `FooterRightIcon`/`FooterRightIconKey` + evenimentul `FooterRightIconClicked`, sora lui `HeaderRightIconClicked` (aceeași semnătură — gazda nu învață un al doilea tipar). Regula de capăt e acum SIMETRICĂ și scrisă o singură dată (`ButonPeLatura`): latura pe care stă butonul de strângere îi aparține, iar iconița de acolo nu se desenează — până acum exista doar jumătatea din stânga, împrăștiată în `DrawFooter`. `ShowFooterLeftIcon`/`ShowFooterRightIcon` sunt publice, ca playground-ul (și orice gazdă) să-și stingă comutatoarele fără să reproducă regula. Dreptunghiul vine din funcție pură (`ComputeFooterRightIconRect`, expus prin `FooterRightIconRect`) — a doua oară în felia asta când se aplică lecția butonului. Iconița suprimată e și INAPĂSABILĂ, nu doar invizibilă: altfel rămânea o zonă de clic fantomă peste buton. 442 teste. ⚠️ **Deschis:** doar strângerea are timp de ecran — degradeul benzii, unghiul desenat, alinierea caption-ului și culorile proprii ale etichetei nu sunt confirmate bucată cu bucată, iar corectura însăși n-a fost re-probată în `MainForm`; `FooterLeftIconKey` n-a fost rulat cu o cheie reală; secțiunea «Coloane» rămâne neacoperită de playground (coloanele vin din XML, nu din grila de proprietăți) |
| 0027-03 | Valorile de designer ale arborelui în cele patru vederi (Rezervări / Recepții / Plăți / DDF) | DONE (code) / **niciodată văzut pe ecran** | `SLICE-0027-03-tree-designer-values-in-views.md` | Cerere de operator: «in all the views with advancedtree, implement the designer values (like in mainform) and remove any code that is setting the tree properties». Cele patru vederi lăsau un arbore GOL (doar dock, `ItemHeight`, mărimile iconițelor, `RightTextWidth`) — benzile de antet și subsol, autorite în designer în 0027/0027-02, existau doar în `MainForm`. Acum fiecare vedere poartă același set ca `MainForm.tree`, cu titlul ei (« REZERVĂRI» / « RECEPȚII» / « PLĂȚI» / « REVIZII DDF»): antet 40px `Tahoma 9 Bold`, degrade orizontal spre `CornflowerBlue`, `folder_open` la stânga, subsol 40px cu DOAR butonul de strângere (stânga, `expand_24`/`collapse_24`), `MinimumCollapsedWidth = 120`, `RootExpander = True`. **Fără iconițe de capăt în subsol și fără iconiță dreaptă în antet:** `MainForm` le are fiindcă are handlere în spate (refresh/setări); o vedere n-are azi ce agăța acolo, iar o iconiță care nu face nimic la clic e exact no-op-ul tăcut interzis de regulile casei. **`tree.Font` ȘTERS din `DdfView.Designer.vb`** — era «Segoe UI, 9», adică implicitul arborelui scris explicit, deci capcana din 0027-02: aprinde `_fontPinned` și face arborele surd la `ApplyBaseFont`. **`ConfigureTree()` a dispărut din toate patru** (nu punea decât `RootExpander`, acum linie de designer); copia comentată din `DdfView` a fost ștearsă, nu lăsată comentariu. **Jumătatea de gazdă a butonului de strângere** a fost adăugată în fiecare vedere (`tree_CollapsedChanged` + `ClampSplitter`, aceeași formă ca în `MainForm`): arborele e `Dock=Fill` în `split.Panel1`, deci `HostOwnsWidth` e True și controlul NU scrie `Width` — fără handler butonul ar fi fost un control mort vizibil în patru vederi. **Cele două noduri-mostră de designer din `DdfView` scoase** (`TreeNodeDefinition1/2`): `RebuildFromDefinitions` le materializează ȘI la rulare, iar constructorul cheamă doar `ShowEmpty` (care nu golește arborele — `ClearAll` o face), deci aplicația ar fi pornit cu două revizii false pe ecran; legătura `NodeImages = tree_image_list` rămâne (sursă de iconițe, nu de noduri). Build 0 erori, 1 avertisment PREEXISTENT (`MSB3825` pe `DdfView.resx`, de la lista de imagini a operatorului). `KBot.App.Tests` **148 passed / 1 failed**, picul fiind PREEXISTENT pe ramură (`Leaf_CaptionPadsRevisionNumberWithSpaces` — `DdfInfo.EtichetaRevizie` are forma cu număr comentată în sursă; familia `EtichetaRevizie` era deja notată ca preexistentă în 0027-02). `App` 1.0.9.0→**1.0.10.0**. ⚠️ **Deschis:** nicio vedere n-a fost redată pe ecran, deci benzile și strângerea sunt verificate doar prin analogie cu `MainForm`; `MinimumCollapsedWidth = 120` și textul titlurilor sunt alegeri ale implementării, nu ale operatorului; `OrdonantareView` rămâne în afara feliei (e încă `PlaceholderView`) |

| 0028 | `KBotDataView` — subsol cu agregate pe tipul valorii, benzi tematizate, etichetă de celulă, buton de strângere | DONE (code) / **niciodată văzut pe ecran** | `SLICE-0028-dataview-footer-collapse-tooltip.md` | Cerere de operator în 5 puncte, pentru grilă. **(1) Agregate deschise după `KBotValueType` (nou: Text/Number/DateTime/Boolean)**, distinct de `KBotColumnType`, care spune doar cum se PICTEAZĂ celula — coloanele de sume din DdfView/PlatiView sunt „Text”, deci nimic nu împiedica până acum un `Sum` pe o coloană de nume (ar fi dat 0, tăcut). `KBotAggregate` a trecut de la 3 la 12 valori (`Min`/`Max`/`CountDistinct`/`CountEmpty`/`CountTrue`/`CountFalse`/`First`/`Last`); tabela e într-un singur loc — `KBotAggregateRules` — citită de setter, de convertorul de designer (`KBotAggregateConverter`, care oferă în grila de proprietăți DOAR agregatele valabile) și de calcul. O pereche nepermisă **ARUNCĂ** pe toate cele trei drumuri (setterul agregatului, setterul tipului, intrarea unei coloane libere în grilă), dar verificarea se AMÂNĂ în `BeginInit`/designer: designerul poate emite `Aggregate` înaintea lui `ValueType`, iar o excepție acolo ar închide formularul cu totul; perechea așezată se verifică la `EndInit`. `Min`/`Max` compară în tipul coloanei (nu alfabetic) și nu se oferă la Text (s-ar confunda cu `First`); `CountDistinct` numără pe TEXTUL AFIȘAT, deci prin `FormatString`. **API vechi SCOS, cum s-a cerut:** `ShowTotalsRow`→`FooterVisible`, `TotalsRowHeight`→`FooterHeight`, `.Totals.vb`→`.Footer.vb`, toate cele 5 locuri de apel migrate — vocabularul e acum cel din 0027-02. **(2) Benzile se tematizează cu adevărat:** fontul antetului era literal în sursă (`New Font("Segoe UI Semibold", …)`), deci schimbarea schemei nu ajungea la el; acum vine din `Style.BaseFontName`/`BaseFontSize` (Modern ⇒ «Segoe UI Variable Text» 9pt), cu semibold căutat în familiile INSTALATE — nu construind fontul și prinzând excepția, fiindcă GDI+ nu aruncă pentru o familie necunoscută, ci cade tăcut pe alta. Subsolul are roluri proprii de culoare (nu mai împrumută sloturile antetului), degradeul benzilor se aprinde din STILUL schemei (`ButtonRender`/`CornerRadius`), nu dintr-un `if Modern`, iar șase proprietăți noi de designer (`Header/FooterBackColor·ForeColor·Font`) respectă „gol = din temă” cu perechile `ShouldSerialize`/`Reset`. **(3) În subsol se despart DOAR coloanele agregate** — regula stă în două funcții pure (`FooterDrawsRight/LeftSeparator`), ca să nu fie verificabilă doar cu ochiul; muchia comună a două agregate vecine se desenează o singură dată. **(4) `CellTooltip`** — obiect de setări public + fereastră `Friend`, sora lui `TreeNodeFlyout` (`WS_EX_NOACTIVATE`/`TOOLWINDOW`/`HTTRANSPARENT` — fără ultimul, eticheta ar acoperi celula, ar pierde hover-ul, s-ar ascunde, ar readuce hover-ul, la infinit). Apare doar când textul CHIAR nu încape: se măsoară textul deja formatat, prin aceeași trecere de `CellFormatting` ca pictarea, minus zona chevronului la Combo. **(5) Buton de strângere** cu aceleași nume și reguli ca la arbore/NavList (`Collapsed` neserializat, `HostOwnsWidth` ⇒ nu scriem dimensiunea, gazda mută splitter-ul), plus ce n-au surorile: **`CollapseDirection`** — pe `Vertical` corpul dispare și rămân cele două benzi, adică agregatele, forma utilă pentru o grilă `Dock=Fill` într-o vedere. **(1b, cerere ulterioară pe aceeași felie) `DecimalPlaces`** pe coloanele numerice: câte zecimale se AFIȘEAZĂ, cu rotunjire NORMALĂ — `MidpointRounding.AwayFromZero`, deci 2,5 ⇒ 3, NU implicitul .NET, unde `Math.Round(2.5)` dă 2 fiindcă rotunjește „la par” (de aceea testul de mijloc de interval e cel mai important din suită: fără parametrul acela, implementarea trece tot restul și cade abia în nota contabilă a operatorului). `Decimal` se rotunjește ca `Decimal`, nu prin `Double`. Fără `FormatString`, numărul de zecimale devine ȘI formatul (`F2` ⇒ «2,50»), altfel rotunjirea ar fi invizibilă. **Subsolul adună ce se VEDE:** trei celule afișate «0,34» dau 1,02, nu 1,01 — un total care nu iese la adunare pe pagină e o greșeală de calcul pentru cine o citește. Valoarea STOCATĂ rămâne neatinsă (regulă de afișare), iar editarea NU rotunjește. Se poate fixa doar pe `ValueType = Number`, cu aceeași amânare a verificării în designer. **Nicio vedere n-a fost trecută pe ea** — e opt-in, iar mutarea DdfView/PlatiView de pe `FormatString = "N2"` ar schimba semantica totalului, deci e decizie de operator. Build 0 erori (`KBot.Controls` singur: 0 avertismente; pe soluție rămâne `MSB3825` pe `DdfView.resx`, PREEXISTENT din 0027-03); `KBot.Controls.Tests` **586 passed** (501 înainte, **+85**). ⚠️ **Deschis:** nimic din felie n-a fost VĂZUT PE ECRAN (harness-ul a primit comutatoarele și o coloană «Denumire» prea îngustă anume pentru etichetă, dar **nu a fost rulat**); **nicio vedere nu folosește încă butonul de strângere al grilei**, deci jumătatea de gazdă (ca `tree_CollapsedChanged`) nu există nicăieri pentru `KBotDataView`; antetul și subsolul nu primesc etichetă când textul LOR se taie (nu s-a cerut) |

| 0028-02 | `KBotDataView` — titlu de subsol, pictograme pe fiecare coloană, `Format` în stil Access | DONE (code) / **niciodată văzut pe ecran** | `SLICE-0028-02-dataview-caption-icons-format.md` | Cerere de operator în 3 puncte, tot pe grilă. **(1) Titlu în subsol** (`FooterCaption` + `FooterLeftIcon`/`FooterIconSize`/`FooterLeftIconHoverColor` + evenimentul `FooterLeftIconClicked`) — același vocabular ca subsolul arborelui, plus ce n-are acela: pictograma se APASĂ și are hover. Regula care contează e **unde se oprește zona: la PRIMA coloană agregată**, niciodată mai departe — un titlu care ar curge pe sub o sumă ar arăta ca eticheta acelei sume, adică ar spune altceva decât spune; fără nicio coloană agregată, zona e toată banda. Colțul butonului de strângere rămâne al lui (zona pornește din `FooterContentRect`), deci cu butonul pe stânga titlul se mută după el, nu sub el. Pictograma se desenează doar dacă încape ÎNTREAGĂ, și tot atunci se și apasă. **(2) Pictogramă stânga + dreapta pe fiecare coloană, în ANTET**, fiecare cu mărimea ei (implicit 16×16); cea din DREAPTA e cea apăsabilă — `HeaderRightIconClicked`, cu cheia coloanei ȘI dreptunghiul pictogramei în `KBotColumnEventArgs`, fiindcă exact acolo trebuie așezat un meniu de filtru/sortare — și tot ea are hover; cea din stânga e un semn, nu un buton (fără eveniment, fără hover: o evidențiere sub ceva inert ar promite o acțiune care nu există). **Ordinea sacrificiului, cerută explicit:** titlul se taie ÎNTÂI (elipsă, până la lățime zero), abia apoi cade pictograma din STÂNGA, iar cea din dreapta nu cade niciodată. De aici **podeaua de lățime**: `EffectiveMinWidth = max(MinWidth, 2×Pad + stânga + Gap + dreapta)`, care limitează orice scriere în `Width` și pe care o folosesc toate trecerile de auto-dimensionare (`MeasureColumnToContent` adună acum și pictogramele, `ShrinkToFit` se oprește la ea). **Podeaua bate `MaxWidth`** — un plafon sub cât cer pictogramele e o cerere imposibilă, iar dacă ar câștiga plafonul, coloana ar picta piese suprapuse; setarea unei pictograme re-limitează lățimea PE LOC, nu la următorul layout. Așezarea celor trei piese e o funcție PURĂ (`ComputeHeaderCellLayout`), chemată și de pictare, și de hit-test, și de teste — o a doua formulă ar însemna o pictogramă desenată unde nu se apasă. **(3) `Format`, proprietatea numită în stil Access** (`KBotFormat`: General Number/Currency/Euro/Fixed/Standard/Percent/Scientific, General/Long/Medium/Short Date, Long/Medium/Short Time, Yes/No, True/False, On/Off), tradusă într-un SINGUR loc — `KBotColumnFormat` — citit de amândoi consumatorii, pictarea celulelor și agregatele din subsol, ca un total să nu se scrie altfel decât coloana de deasupra lui. Formatul numit **CITEȘTE** valoarea, nu doar o formatează (o coloană `ValueType.Text` care poartă numere se scrie „Standard” corect); o valoare care nu se poate citi în tipul cerut cade pe calea obișnuită și tot se VEDE, nu se stinge într-o celulă goală. **`Format` și `FormatString` nu se folosesc împreună** — două fețe ale aceluiași lucru, deci amândouă setate ARUNCĂ, cu aceeași amânare în `BeginInit`/designer ca perechea `ValueType × Aggregate` și verificare la `EndInit` (`ValidateAggregatePair` → `ValidateSettled`, care verifică acum trei perechi); alternativa — una câștigând tăcut — ar lăsa în formular o proprietate care arată ca o setare activă și nu face nimic. Câte zecimale scrie un format vine din `DecimalPlaces` când e fixat, altfel 2 (implicitul Access); `AggregateFormatString` rămâne deasupra tuturor. Numele membrilor sunt vocabularul Access (cel căutat de cine portează un formular), textul scris e românesc («Da»/«Nu», «Adevărat»/«Fals»). Build 0 erori / 0 avertismente pe `KBot.Controls`; `KBot.Controls.Tests` **627 passed** (585 înainte, **+42**). ⚠️ **Deschis:** nimic n-a fost VĂZUT PE ECRAN, iar `DataViewHarnessForm` **nu** a fost extins cu piesele noi (nici titlu de subsol, nici pictograme de coloană, nici lista de formate); mărimile pictogramelor sunt px ne-scalați (ca la arbore), pe când spațiile se scalează, deci la 150% DPI podeaua rămâne cu câțiva pixeli sub necesar — se plătește din text, care oricum se taie primul, dar neverificat pe un ecran real; perechile `ShouldSerialize`/`Reset` există pentru toate proprietățile noi `Image`/`Size`/`Color`, dar **verificarea empirică prin `TypeDescriptor…ShouldSerializeValue`** (cerută de CLAUDE.md) n-a fost rulată pentru ele; **nicio vedere nu folosește încă `Format`** — toate rămân pe `FormatString` |

| 0028-03 | `KBotDataView` — filtru de coloană în stil Access (sortare + condiții + valori bifate) și suprascrierea culorilor pe schema întunecată | DONE (code) / **niciodată văzut pe ecran** | `SLICE-0028-03-dataview-column-filter-darkmode.md` | Cerere de operator în 2 puncte. **(1) `ShowColumnFilter`, PE COLOANĂ** — buton de filtru în antet, hotărât coloană cu coloană din designer (`KBotDataColumn.ShowColumnFilter` + `ColumnFilterIcon`/`ColumnFilterIconSize`/`ColumnFilterHoverColor`, toate cu `ShouldSerialize`/`Reset` verificate **prin `TypeDescriptor`**, calea pe care o ia chiar Visual Studio). **Prima variantă îl avea pe GRILĂ; operatorul a cerut per-coloană** («I will decide in the designer for EACH column»), deci `Filterable`/`FilterIconReserve` au dispărut — coloana știe acum totul singură. **Interzis pe `Button` și `ProgressBar`** (nu poartă o valoare care să se caute sau să se sorteze): aprinderea ARUNCĂ, apărat din amândouă direcțiile (și la schimbarea tipului sub un filtru aprins), cu amânare în `BeginInit`/designer și verificare finală în `ValidateSettled`. Butonul deschide un meniu cu paritate Access: sortare sus, «Șterge filtrul din «X»», submeniu de condiții pe tip (`Filtre text` / `numerice` / `de dată`), apoi lista de valori bifate cu «(Selectează tot)», căutare și OK/Anulează. Trei decizii au fost puse operatorului înainte de cod: **grila filtrează SINGURĂ** (nu ridică doar un eveniment), **paritate completă** cu Access, și **bare native + tema întunecată Windows** în loc de un scrollbar desenat. Motorul e PUR și separat (`KBotFilterEngine` / `KBotColumnFilter` / `KBotFilterOperator` / `KBotSortDirection`): condițiile de TEXT se potrivesc pe textul AFIȘAT (operatorul caută ce vede), cele de mărime pe valoarea BRUTĂ citită în tip — altfel «mai mic decât 10» ar fi alfabetic, unde «9» e mai mare decât «10»; un operand ilizibil face condiția **inertă, nu goală** (o grilă golită arată ca un bug); `SelectedValues = Nothing` («toate») ≠ mulțime goală («niciuna»); coloanele logice n-au submeniu (două căsuțe spun deja tot). **Schimbarea invazivă e harta vedere ↔ model** (`KBotDataView.Filtering.vb`): de acum sunt DOUĂ numerotări și **nu se amestecă** — API-ul public rămâne în indici de MODEL (`Item`, `CurrentRowIndex`, `CellClick`, `EnsureVisible` înseamnă exact ce însemnau, fiindcă vederile existente își țin proprii indici și o renumerotare i-ar corupe tăcut), geometria trece pe poziții de VEDERE (`RowTop`, virtualizare, hit-test), iar `RowAtPoint`/`RowTopForModel` traduc la graniță. Harta se reconstruiește LENEȘ (`_viewDirty`/`EnsureView`), fiindcă o pictare căzută în mijlocul unei încărcări `BeginUpdate` ar indexa în gol; `RecomputeFooter` → **`RecomputeDerived`**, care marchează harta murdară ÎNAINTEA gărzii de lot. Sortarea e stabilă (tiebreak pe indexul de model), pe O SINGURĂ coloană, ca în Access. Au trecut pe rândurile VIZIBILE: agregatele din subsol (un total care nu iese la adunare pe ecran e o greșeală de calcul), înălțimea de derulat, eșantionul de auto-dimensionare, dungile alternante, săgețile sus/jos și `Last`. Selecția pe un rând filtrat afară cade (cu abandonarea editării). Pictograma stă mereu la capătul din dreapta (`HeaderRightIcon` se mută la stânga ei — o pictogramă a grilei care și-ar schimba locul de la o coloană la alta se caută de fiecare dată), nu se sacrifică la îngustare, iar podeaua de lățime o ține grila prin `FilterIconReserve` (coloana n-are de unde ști dacă filtrarea e aprinsă); fără imagine dată se **desenează o pâlnie**, plină și în accent cât timp coloana chiar e filtrată. Meniul e o fereastră desenată de noi (`KBotFilterPopup`), ca `CustomPopup` și din același motiv — un `ContextMenuStrip` cu `CheckedListBox` ar rămâne două dreptunghiuri albe pe întuneric; sortarea se aplică IMEDIAT (e o comandă), filtrul se predă abia la OK (se lucrează pe un `Clone`); lista distinctă respectă filtrele CELORLALTE coloane dar îl ignoră pe al ei (altfel, odată bifată o valoare, restul ar dispărea și nimeni nu s-ar mai putea răzgândi). **(2) Pe schemele `IsDark`, culorile fixate în designer se IGNORĂ** (`DarkOverridesDesignerColors`) și benzile iau paleta — regula casei «cine a pus explicit o culoare câștigă» **rămâne întreagă pe schemele luminoase**; excepția e cerută de ce se întâmplă altfel: o bandă lăsată albă peste un corp aproape negru nu e alegerea operatorului respectată, e o grilă imposibil de citit. Culorile **nu se pierd**, doar se ignoră (ieșirea din schemă le dă înapoi), și e valabil **numai pentru culori** — un font fixat rămâne al operatorului, un font nu devine ilizibil pe fundal închis. Celulele luau deja paleta în orice schemă. Barele de derulare: `SetWindowTheme` «DarkMode_Explorer»/«Explorer», re-aplicat la `OnHandleCreated`. **(3) BUG DE MOTOR găsit pe drum, raportat de operator** («in the SumarView the color scheme doesn't get applied»): nu era al vederii, ci al lui `ThemeManager.Traverse`, care se OPREA la primul `IThemedControl` — iar toate cele ȘASE vederi reale sunt `IThemedControl` ȘI țin un `KBotDataView` direct înăuntru, deci grila din fiecare nu primea schema de la nimeni; pe banc, unde grila stă direct pe formular, se colora corect, ceea ce făcea simptomul să pară al vederii (comentariul din `SumarView.ApplyTheme` chiar afirma premisa pe dos, ceea ce a ținut bug-ul ascuns). Reparat la nivelul potrivit, nu în fiecare vedere: `ThemeManager.ApplyToNestedThemed` coboară prin subarborele unui control auto-tematizat și PREDĂ schema doar altor `IThemedControl`, fără nicio regulă pe tip — deci contractul vechi rămâne întreg (`TextBox`-ul intern al lui `KBotTextField` nu e `IThemedControl`, nu e atins). Patru teste noi (`NestedThemedControlTests`) fixează ambele jumătăți: copilul și nepotul tematizat primesc schema, copilul simplu NU, fiecare exact o dată. Build 0 erori / 0 avertismente noi; `KBot.Controls.Tests` **685 passed** (627 înainte, **+58**), `DevHarness.Tests` 170, `Theming.Tests` **65** (61 înainte). Eșecurile din `Domain`/`Api`/`App.Tests` sunt PREEXISTENTE și verificate ca atare (cele din Domain/Api eșuează identic la HEAD cu felia scoasă prin `git stash`; 6 din cele 8 din `App.Tests` vin din rescrierea ÎN LUCRU a lui `DdfView`, ale cărei teste ajung la handler prin reflecție — `GetMethod("tree_NodeMouseUp")` întoarce `Nothing` fiindcă declarația a dispărut din arborele de lucru). **Un test existent a fost REscris, nu reparat**: `AColourSetByTheOperator_SurvivesAThemeSwitch` demonstra vechea regulă folosind chiar schema Dark — acum e `…SurvivesASwitchBetweenLIGHTSchemes` (pe Modern), iar regula nouă are trei teste proprii. Bancul de probă a primit bifa `ShowColumnFilter`, butonul `ClearAllFilters()` și `Filterable` în inspectorul de coloană. ⚠️ **Deschis:** **nimic n-a fost VĂZUT PE ECRAN** (meniul, pâlnia, bifele, dialogul de condiție, barele întunecate — verde doar la compilare și în teste headless); **barele urmează doar perechea întuneric/lumină**, `SetWindowTheme` aduce griul Windows-ului, nu accentul paletei — varianta completă e un `KBotScrollBar` owner-drawn, nefăcut, la alegerea operatorului; o schemă personalizată cu `IsDark` **ȘI** `PreserveDesignerColors` ar readuce culorile de designer prin `DesignerBaseline.Restore` (niciuna dintre cele patru scheme compilate nu le combină, dar editorul de teme poate); filtrarea n-a fost rulată peste date reale; **`SumarView` e singura vedere cu filtrul aprins** (pe toate cele șapte coloane, mutat din forma veche pe grilă pe care o autorase operatorul în designer) — celelalte cinci rămân fără; sortarea pe mai multe coloane nu e implementată (n-a fost cerută) |

| 0028-04 | `KBotDataView` — `AutoSizeMode` PE COLOANĂ, care bate modul grilei | DONE (code) / **niciodată văzut pe ecran** | `SLICE-0028-04-dataview-column-autosize.md` | Cerere de operator, începută de el în cod și lăsată cu TODO în capul lui `KBotDataView.AutoSize.vb`: «i started a new property per column (AutoSize) which takes precedence over the View's own AutoSize». Exista proprietatea (`KBotDataColumn.AutoSizeMode` + `OnColumnAutoSizeModeChanged`), dar pasul de măsurare n-o citea deloc — un buton nelegat la nimic. **Decizia care trebuia luată înainte de cod:** implicitul ei era `None`, iar «precedență + implicit `None`» ar fi însemnat că nicio coloană nu se mai măsoară niciodată, deși grila e pe `ToContent` din construcție — o regresie cu întârziere pentru cele cinci gazde care setează explicit `ToContent` (`DdfFileBrowser`, `XfaXmlPreview`, `Plati`, `Rezervari`, `Receptii`). De aceea enum-ul a primit **al treilea membru, `Inherit = -1`**, implicitul COLOANEI («n-am părere, hotărăște grila») — același tipar cu `Color.Empty` = «din temă» de la arbore. `Inherit` **ARUNCĂ** pe proprietatea GRILEI (deasupra ei nu e nimeni de la cine să moștenească, deci a o accepta ar fi o traducere tăcută în altceva), la fel ca orice valoare din afara enum-ului, pe amândouă proprietățile. `-1` și nu `0` fiindcă bancul leagă `cboAutoSize.SelectedIndex` direct de valoarea enum-ului; lista de pe coloană, care începe cu `Inherit`, are traducere proprie. În pas: precedența se citește într-un SINGUR loc (`EffectiveAutoSizeMode`), bucla de măsurare întreabă per coloană, iar **poarta de la intrare s-a mutat de pe grilă pe coloane** (`AnyColumnSizesToContent`) — altfel o coloană care cere singură măsurarea pe o grilă manuală n-ar fi fost măsurată niciodată, adică fix aranjamentul pentru care există proprietatea. **Precedența acoperă DOAR măsurarea**: umplerea/strâmtarea (`ColumnFillMode`) trec în continuare peste toate coloanele vizibile, inclusiv peste una fixată pe `None` — aceeași regulă ca la o coloană trasă de operator (`UserSized`), pe care măsurarea o sare dar umplerea o mișcă; o coloană complet nemișcată se prinde cu `MinWidth = MaxWidth`. `UserSized` rămâne deasupra tuturor (precedența e față de grilă, nu față de operator). Bancul a primit lista `AutoSizeMode` în inspectorul de coloană, plus corectura activării lui „Width”, care se lua după steagul GRILEI și de acum ar minți. Build 0 erori / 0 avertismente noi; `Controls.Tests` **694** (685 înainte, **+9**), `DevHarness.Tests` 170, `Theming.Tests` 65. Eșecurile din `Domain`/`Api`/`App.Tests` sunt preexistente, verificate din nou **într-un worktree curat la HEAD** (3 + 1 identice la HEAD; din cele 10 din `App.Tests`, 2 la HEAD, restul din rescrierea ÎN LUCRU a vederilor). ⚠️ **Deschis:** n-a fost văzut pe ecran; **nicio vedere nu folosește încă proprietatea** (care coloane merită fixate e alegerea operatorului); un `FillWeight` pe coloană n-a fost cerut și nu s-a făcut |

| 0028-05 | `KBotDataView` — cine deține lățimea unei coloane (designer vs trecere de layout) | DONE (code) / **niciodată văzut pe ecran** | `SLICE-0028-05-dataview-authored-width.md` | Raport de operator pe o vedere reală: «in IstoricView I tried to make the first three columns NO Autosize and only the last one to strech, but no matter what i do, the first column becomes huge». „No matter what I do” era literal adevărat — **două defecte diferite**, niciunul vizibil citind vederea. Fișierul avea `Width = 747 / 40 / 55 / 144`, adică **exact `986 = grid.Size.Width` de pe suprafața de designer**: nimeni nu tastează patru numere care dau fix lățimea formularului, deci erau **ieșirea unei treceri de umplere**, serializată ca alegere a operatorului. **(1) Trecerea de layout rula ÎN DESIGNER.** `PerformAutoSize` scrie `Width`, iar designerul serializează ce găsește după — deci orice deschidere a formularului putea îngheța în `.Designer.vb` lățimea calculată pentru mărimea de atunci, care apoi supraviețuiește layout-ului care a produs-o. E capcana `ShouldSerialize*` cu un etaj mai jos: acolo o valoare implicită, aici ieșirea unui calcul, iar **un calcul de layout n-are ce căuta autorând formularul**. Reparat: ieșire imediată sub `KBotDesignTime.IsDesignTime(Me)` — în designer se văd lățimile AUTORATE, măsurarea și umplerea se fac la rulare. **(2) Strâmtarea era DISTRUCTIVĂ.** `IstoricView` se construiește într-un `SplitContainer` de ≈690px și abia pe urmă ajunge la mărimea ferestrei; la 690 `ShrinkToFit` scria 200 → 120 peste `Width`, iar la lărgire `DistributeLeftover` putea crește DOAR ținta umplerii — deci coloanele rămâneau strâmtate tot restul sesiunii (operatorul cere 200 și vede 120). Cauza: trecerea își compunea propria ieșire, deci rezultatul depindea de istoricul redimensionărilor. Reparat **în model**: `KBotDataColumn` ține și `_authoredWidth` (scrisă doar de setterul public `Width` — designer, cod, tragere), trecerea scrie prin `SetLayoutWidth` și începe cu `RestoreAuthoredWidths()`, soră cu `ClearAutoHiddenState()`. **O trecere e de acum o funcție de (lățimi cerute, spațiu disponibil)**, de nimic altceva; o coloană trasă de operator e sărită de la restaurare (tragerea E lățimea cerută, până la `ResetColumnSizing`). Vederea: `ColumnFillMode = LastColumn` (LIPSEA — de asta nu se întindea nimic), `MaxWidth = 123456` scos, lățimi **300 / 200 / 200 / 250**. Măsurat headless pe vederea reală: la 690px → 300/120/120/150 (strâmtate, fără bară), la 1600px → **300/200/200/900** (cerute înapoi + ultima umple). Build 0 erori; `Controls.Tests` **699** (694 înainte, **+5**), restul neschimbate; eșecurile din `Domain`/`Api`/`App` sunt aceleași preexistente. ⚠️ **Deschis:** nevăzut pe ecran (numerele vin dintr-un test headless); **celelalte cinci vederi pot avea aceeași murdărie înghețată în `.Designer.vb`** (defectul 1 lucra pentru orice formular deschis în designer) — n-au fost atinse, fiindcă fiecare cere privit ce voia operatorul de la coloanele ei, nu o corectură în masă; strâmtarea rămâne grid-wide (ia din toate coloanele vizibile, inclusiv din cele cu `AutoSizeMode = None` — regula din 0028-04), iar schimbarea ei ar fi o schimbare de contract, cu două teste care o fixează pe cea de azi |

| 0028-06 | Meniul de filtrare, AUTORAT ÎN DESIGNER (nu desenat în cod) | DONE (code) / **niciodată văzut pe ecran** | `SLICE-0028-06-filter-popup-designer.md` | Cerere de operator: «i want the filtering form to be configed in the designer, not in normal code… except the dynamic list of values… the values in it should remain filled at runtime». `KBotFilterPopup` era, din 0028-03, o fereastră desenată integral de noi — ~400 de linii de pictură plus tot atâtea de geometrie și hit-test — iar **motivul de atunci era corect ATUNCI**: «un `ContextMenuStrip` cu un `CheckedListBox` ar rămâne două dreptunghiuri albe pe o schemă întunecată». Motivul **a expirat tot în felia 0028**: `ThemeManager` are acum reguli pe tip pentru `CheckedListBox`/`ListBox`/`CheckBox`/`Button`/`Panel`, inclusiv tema nativă a barelor de derulare, iar `KBotThemedForm` le aplică singur. Deci meniul e rescris ca formular obișnuit al casei: `Inherits KBotThemedForm`, TOATE controalele în `KBotFilterPopup.Designer.vb` (patru butoane docate sus, `CheckBox` cu trei stări, `CheckedListBox`, bară de butoane jos), iar `.Painting.vb` **șters**. Rămâne la rulare exact ce nu poate exista la proiectare: **conținutul listei** (cererea operatorului), textele care depind de tipul coloanei (sortarea se numește «A → Z» pe text și «de la mic la mare» pe numere), activarea lui «Șterge filtrul» și existența butonului de condiții (coloana logică n-are submeniu — butonul se ASCUNDE, nu se stinge, ca să nu rămână un rând care nu duce nicăieri). **Lățimea și toate mărimile sunt de acum ale designerului**; codul mai atinge doar înălțimea ferestrei, ca lista să arate câte rânduri are. Chenarul de 1px nu se mai pictează: e `Padding`-ul formularului peste fundalul lui, cu culoarea din temă (`OnThemeChanged`), deci nicio culoare scrisă nicăieri. **DECIZIILE n-au fost atinse** (filtru predat la OK dintr-o copie, sortare aplicată imediat, «tot bifat» ≠ filtru, «Selectează tot» doar peste rândurile arătate, golul etichetat dar vid în model) — dovada e că vechiul `KBotFilterPopupTests` a trecut NESCHIMBAT. **Defect găsit pe drum:** primul `Handles txtCauta.TextChanged` n-a rulat niciodată — `KBotTextField.Text` e delegat la `TextBox`-ul intern, deci evenimentul CADRULUI nu se ridica; toți consumatorii se legau la `InnerTextBox` și nimeni n-a observat. Pentru un formular autorat în designer e o capcană curată (legi evenimentul și nu se întâmplă nimic, tăcut) — reparat în control. Build 0 erori; `Controls.Tests` **708** (699 înainte, **+9**, toate prin CONTROALE, nu prin porțile `Debug*`). ⚠️ **Deschis:** nevăzut pe ecran; culorile pe schema întunecată (bifele desenate de sistem într-un `CheckedListBox`) se judecă privind, nu citind; **navigarea cu tastatura s-a SCHIMBAT** — nu mai există rândul survolat desenat de noi, ci ordinea de TAB a controalelor (Esc/Enter au rămas, prin `KeyPreview`) |

| 0028-07 | Schema «Modern»: butonul crește cât să încapă și umplutura, și textul | DONE (code) / **niciodată văzut pe ecran** | `SLICE-0028-07-modern-button-height.md` | Raport de operator după ce a autorat singur meniul de filtrare în designer: «on modern theme, it adds a lot of padding to the buttons, so the text is not visible anymore… it should adjust the height of the buttons to fit the new padding AND the text». `Modern` cere `ControlPadding = (12,8,12,8)`, iar `ModernRenderer.ApplyButton` o scrie în `Button.Padding` — care NU se desenează în afara butonului, ci micșorează suprafața textului: pe un buton autorat la 32px rămâneau 16px pentru un rând care cere ~15–20, deci textul se tăia sau dispărea. **Al doilea capăt al aceleiași greșeli, invizibil până acum: umplutura nu se dădea NICIODATĂ înapoi** — `DetachButton` (chemat la ieșirea din schemă din amândouă ramurile lui `ThemeManager`) scotea handler-ele și regiunea rotunjită, dar lăsa `Padding` pe buton, deci schema modernă rescria PERMANENT designul. Reparat în MOTOR, cu aceeași formă ca lățimea autorată a unei coloane (0028-05): `ButtonState` reține `AuthoredPadding`/`AuthoredHeight` înaintea primei scrieri, `FitHeightToPaddingAndText` calculează `Padding.Vertical + un rând de text + chenarele` și ia **maximul cu înălțimea autorată** (tema completează ce lipsește, nu rescrie ce s-a ales; și, baza fiind mereu valoarea autorată, zece comutări dau același rezultat ca prima), iar `DetachButton` dă înapoi și marginea, și înălțimea. Sărite dinadins: `AutoSize = True` (se ocupă WinForms) și `Dock = Left/Right/Fill`, unde înălțimea e a PĂRINTELUI și o scriere ar fi ștearsă de următorul layout (cazul OK/Anulează din bara meniului). Un buton fără text primește totuși loc de un rând («Wg») — o pictogramă pusă mai târziu n-are cum să încapă într-un buton cât umplutura. În meniu, butoanele de comandă sunt andocate SUS, deci creșterea lor ar mușca din lista `Fill`: `KBotFilterPopup.OnThemeChanged` re-măsoară fereastra, ca numărul de rânduri arătate să rămână cel dinainte. Build 0 erori; `Theming.Tests` **71** (65 înainte, **+6**), `Controls.Tests` **709** (+1). ⚠️ **Deschis:** nevăzut pe ecran (măsurătorile sunt headless, cu aceeași funcție folosită de pictare); **regula e în motor, deci ORICE buton scund cu `Dock = Top/Bottom/None` din aplicație va crește acum sub Modern** (LoginForm, MainForm, vederi) — e reparația cerută, dar e o schimbare vizibilă peste tot, iar nimic nu se micșorează, deci nimic nu se poate ascunde din cauza ei; umplutura ORIZONTALĂ n-a fost atinsă (cei 12px pot încă trunchia un text lung pe un buton îngust — o creștere automată de lățime ar strica aranjamente întregi, deci se rezolvă cu `AutoSize` sau cu o lățime autorată) |

| 0028-08 | Trecerea OPERATORULUI peste cele două ferestre de filtrare (+ ce a rupt designerul pe drum) | DONE (code) / partea operatorului **văzută de el în designer**, restul neconfirmat | `SLICE-0028-08-filter-forms-operator-pass.md` | Felie de LIVRARE, nu de proiectare: operatorul a lucrat singur în designerul VS peste ce a lăsat 0028-06/07 și a cerut ca tot ce era necomis să intre, «even the work you didn't do». **(1) Meniul — RÂNDURI, nu butoane:** `AplicaRandDeMeniu` face cele patru comenzi de sus plate, fără chenar, în culoarea suprafeței, scoase în relief doar de hover. Motivul e o interacțiune fină între două lucruri corecte separat: schema **Modern** randează orice `Button` owner-drawn, adică îi taie colțurile cu un `Region` de rază 8 — pe un rând lat cât meniul, prin cele patru decupaje se vedea suprafața de dedesubt, deci meniul arăta ca patru pastile gri lipite pe o foaie albă. `ModernRenderer.DetachButton` scoate `Region`-ul și, pe drumul deschis în 0028-07, redă marginea și înălțimea autorate; apelul e idempotent pe celelalte scheme. Roșul lui «Șterge filtrul» vine acum din **paletă** (`ErrorColor`), nu din `Firebrick`-ul scris o dată în designer. **Două defecte reparate pe același drum**, amândouă la deschiderea submeniului de condiții: `CustomPopup` ridică `ItemClicked` ÎNAINTE de `Close`, iar dialogul e modal — submeniul rămânea pe ecran, viu și inutil, până se închidea dialogul (acum `Hide()` înainte de `AplicaConditia`); și `_suppressDeactivate` se pune ACUM înaintea lui `Hide()`, fiindcă ascunderea ferestrei active ridică `OnDeactivate`, care cu garda pusă după închidea chiar meniul folosit. **(2) Dialogul de condiție, rescris pe `TableLayoutPanel`** (`tlyMAIN`, 2 coloane × 6 rânduri): ascunderea celei de-a doua casete nu se mai face mutând butoanele cu mâna, ci **strângând la zero rândurile** lor, cu fereastra scurtată cu exact atât; `StrangeRandul` **citește** rândul din tabel (`GetRow`) în loc să scrie un indice fix — altfel prima reordonare în designer, adică exact rostul trecerii la tabel, ar fi stricat socoteala tăcut. Constructorul a primit `Try/Catch` cu re-aruncare. **(3) Ce a RUPT designerul pe drum, reparat aici:** deschiderea lui `PlatiView` în VS a ȘTERS bucla care adăuga cele nouă rânduri `AutoSize` din `detailTable` (designerul nu poate reciti cod pe care nu l-a scris el), lăsând o singură linie `RowStyles.Add(Percent 100)` — iar **stilurile sunt POZIȚIONALE**, deci ea nimerea pe rândul 0: «Nr. document» absorbea tot spațiul în locul lui «Explicații» (rândul 9). Rescrise unul câte unul, formă pe care designerul o poate reciti. E același tipar ca defectul din 0028-05 (trecerea de layout care autora `.Designer.vb`), văzut din partea cealaltă: **ce nu poate designerul reciti, dispare la prima deschidere.** **(4)** Au mai intrat: `My Project\Resources.resx` + `.Designer.vb` în `KBot.Controls` (gol, doar șablonul VS) plus legarea din `.vbproj` — fără el `.vbproj` trimite către un fișier inexistent pentru oricine clonează; `PlatiView.resx` și `KBotFilterConditionDialog.resx`; ajustările vizuale ale operatorului în `KBotFilterPopup.Designer.vb` (cursor de mână, «Operatori filtru», înălțimi). Build 0 erori / 0 avertismente; `Controls.Tests` 709, `Theming.Tests` 71, `DevHarness.Tests` 170, restul neschimbate; `App`/`Domain`/`Api` — ACELEAȘI eșecuri preexistente, cu același număr. ⚠️ **Deschis:** partea mea n-a fost probată pe ecran; **`PlatiView` e reparat pe hârtie** (niciun test nu prinde ordinea stilurilor de rând — panoul e `Visible = False` până la o selecție), merită o privire la prima rulare a vederii Plăți; **aceeași ștergere de buclă poate lovi orice `.Designer.vb` deschis în VS** (`InitDetailPair` a supraviețuit aici, dar e noroc, nu regulă); `My Project\Resources.resx` din Controls e gol |

| 0028-09 | Butonul de TEMĂ intră în bara de titlu (selectorul de teme nu mai stă în gazdă) | DONE (code) / **niciodată văzut pe ecran** | `SLICE-0028-09-captionbar-theme-button.md` | Cerere de operator: «add a new button ThemeButton, right-to-left: just after the minimize/maximize buttons (if available, else after the close button) and will be built in the captionbar… there will be only one option (true/false) to enable this button» + «another new option: "Show Theme Editor" which will show / hide the last button in the popup». `KBotCaptionBar` are acum al doilea buton cu pictogramă, în partialul nou `KBotCaptionBar.ThemeButton.vb`, care își face SINGUR meniul de scheme (`CustomPopup`, litere de acces, «Stiluri...» → `ThemeEditorForm`) — cele ~130 de rânduri din `MainForm` au dispărut, iar al doilea formular cu bară de titlu nu mai are ce copia. Un singur comutator îl aprinde (`ShowThemeButton`); pictograma implicită vine din `KBot.Controls\Resources.resx`, nu din gazdă. Al doilea comutator, `ShowThemeEditor` (implicit True), scoate ultimul rând al meniului ȘI separatorul rămas fără rost. Trei lucruri reparate pe drum: **(a)** sloturile se derivă acum dintr-o SINGURĂ numărătoare (ordinea dreapta→stânga: închidere, max, min, temă, opțiuni) — a doua formulă scrisă separat ar fi așezat butonul nou fix peste cel de opțiuni; **(b)** `TitleRightLimit` nu se uita deloc la butoanele cu pictogramă, deci un titlu lung curgea pe sub ele; **(c)** sinkul `IPopupAnchor` primește un singur bit și nu spune CARE buton s-a desfășurat — bara ridică un steag chiar înainte de `ShowBelow`, deci se aprinde butonul corect. Cheia rândului de editor e `@ThemeEditor`, nu «Stiluri»: o schemă de utilizator chiar s-ar putea numi așa. `MainForm` a rămas cu un handler de 9 rânduri pe evenimentul nou `ThemeSchemeChanged` (gazda NU reaplică tema — `SetScheme` difuzează singur). 16 probe noi, toate headless. **`LoginForm` a rămas neatins, deși acolo e un buton de opțiuni cu pictograma de temă și FĂRĂ handler — adică un buton mort; un rând îl repară, dar formularul are lucru necomis.** |

| 0029 | `KBotDataView` — GRUPARE ca la raportul Access (agregate, culori, fonturi pe nivel) | DONE (code) / **niciodată văzut pe ecran** | `SLICE-0029-01-kbotdataview-grupare.md` | Cerere de operator: «i need the dgv to support grouping JUST like the access report does. it must support aggregation, different coloring and fonts. be careful when ordering / filtering a grouped dgv». Trei hotărâri confirmate ÎNAINTE de a scrie cod (grupurile se STRÂNG; agregatele pe amândouă benzile, alese pe nivel; sortarea pe o coloană negrupată ordonează ÎNĂUNTRUL grupurilor). Colecție nouă `Groups` (`KBotGroupLevel`, ordinea = ierarhia), fiecare nivel cu bandă de antet și de subsol, șabloane de titlu (`{0}` coloana / `{1}` valoarea / `{2}` numărul de rânduri), retragere cumulativă, culori și fonturi proprii, strângere. **A treia numerotare — indici de BANDĂ** (rândurile DESENATE, antetele/subsolurile de grup incluse), lângă cele două de la 0028-03; API-ul public a rămas în indici de MODEL, neatins. Virtualizarea a trecut de la înmulțire la **offset-uri cumulate + căutare binară**, ca benzile să poată avea înălțimi proprii — iar **calea negrupată n-a fost atinsă**: fără niciun nivel, tabloul de benzi nu se alocă deloc și geometria e cea de dinainte (tabloul e O(rânduri vizibile), adică exact ce evita virtualizarea, și n-avea voie plătit de cele șase vederi care nu grupează). **Miezul feliei, cerut explicit:** comparatorul compară CHEILE DE GRUPARE primele, deci un click pe antet ordonează în interiorul grupurilor și nu le poate sparge (altfel «Total ianuarie» ar apărea de patru ori, cu patru sume); iar un click pe coloana DE grupare întoarce sensul nivelului în loc să adauge o sortare, și ridică sortarea de rând (un antet n-are voie să poarte două semne). **A doua hotărâre de fond: filtrarea schimbă totalurile, strângerea NU** — un filtru scoate rânduri din pagină (grupul rămas gol dispare cu antet și subsol cu tot), o strângere doar ascunde, deci sumele stau pe loc; altfel operatorul care închide o lună ar vedea totalul general mișcându-se singur. Aceeași distincție în selecție: rândul filtrat afară o pierde, cel închis într-un grup **o păstrează** — fără asta `Ctrl+Dreapta` n-ar mai putea redeschide ce a închis `Ctrl+Stânga`, fiindcă n-ar mai exista rândul despre care să întrebe (`AnchorBandOfRow` îl sprijină pe antetul grupului). Agregatele: `ComputeAggregateText` parametrizată pe un INTERVAL de poziții de vedere — subsolul grilei îl cheamă pe tot, cel de grup pe felia lui, deci **o singură definiție a lui «sumă»**; niciun `GroupAggregate` pe coloană, fiindcă un raport Access scrie `=Sum([x])` o dată (pe nivel se alege DACĂ se arată, nu care e). Cache leneș per nod. Culorile: roluri PROPRII în temă, cu nuanța pe adâncime CALCULATĂ (nivelul 0 cel mai apăsat) ca un nivel nou să nu ceară un slot de paletă; peste ea, nivel → `GroupFormatting` → iar la ÎNTUNERIC culorile fixate se ignoră (fonturile nu). Bancul a primit secțiunea «Grupare». Build 0 erori; `Controls.Tests` **800** (752 înainte, **+48**). ⚠️ **Deschis:** nimic nu s-a văzut pe ecran (nuanța pe niveluri, retragerea, triunghiul, benzile sub Modern/Dark se judecă privind); dus-întorsul prin designerul VS nefăcut; **niciun ecran real nu grupează încă** (`IstoricView`/`PlatiView` sunt candidații, neatinși deliberat); nu se poate NAVIGA prin antete de la tastatură (o bandă n-are index de model — ar cere o noțiune de «bandă curentă»); ~20 octeți/rând vizibil DOAR când grila e grupată; **nu există raport Access de referință — exportul n-are secțiune `REPORTS`**, deci s-a implementat SEMANTICA de grupare a rapoartelor, nu un raport anume |

| 0030 | Meniul de coloană: TREI FILE (Sortare / Filtrare / Grupare), rânduri de tabel pe măsura temei, și bifele care se pierduseră | DONE (code) / **niciodată văzut pe ecran** | `SLICE-0030-filter-popup-file-si-grupare.md` | Patru cereri de operator după ce a rearanjat singur `KBotFilterPopup` cu un `TableLayoutPanel`. **(a) Filtrarea „nu mai mergea”, arăta o grilă goală:** `LstValori_ItemCheck` rămăsese fără clauza `Handles`, deci bifele puse cu mouse-ul nu ajungeau în model — pe drumul obișnuit («Selectează tot» stinge tot, apoi se bifează una) mulțimea predată la OK era GOALĂ; «Șterge filtrul» mergea fiindcă nu trece prin bife. **(b)** `pnlCorp` nu era andocat (Location/Size scrise de designer), deci nu urma `ClientSize`; iar re-măsurarea citea `lstValori.Height` înainte de orice layout. Acum `Dock = Fill` + o singură formulă pentru toate filele (rama corpului + cât cere fila activă), plus o re-verificare a poziției DUPĂ `Show()`, fiindcă tema se aplică abia în `OnLoad`. **(c)** `ThemeTableFit` (nou, în `KBot.Theming`): un rând fix crește cu EXACT surplusul cerut peste măsura AUTORATĂ și se întoarce la ea când surplusul dispare — nu «rândul devine cât conținutul», altfel aerul ales de operator s-ar pierde la prima comutare. Trei lucruri ieșite din implementare: baza e un INSTANTANEU (fără el, a doua comutare măsoară peste rezultatul primeia și rândurile cresc la nesfârșit); se întreabă `GetPreferredSize`, **NU `Height`**, fiindcă `TableLayoutPanel` taie orice control andocat la dreptunghiul celulei — un buton de 56px într-o celulă de 40 raportează 40, deci creșterea lui `ModernRenderer` e invizibilă (prima versiune a testului a picat exact pe asta); și `Control.Visible` **nu poate fi întrebat de nimeni** (răspunde despre lanțul de părinți, deci headless tot raportează False), de unde `SetRowCollapsed` explicit pentru rândul butonului de condiții și fila curentă ținută într-un câmp, nu în `pnlX.Visible`. `KBotTextField` a primit un `GetPreferredSize` cinstit. **(d)** Bară `KBotNavList` orizontală + trei panouri `Fill`; **bara OK/Anulează e a FILTRULUI** și se ascunde pe celelalte file, fiindcă cele trei hotărâri se predau diferit: filtrul la OK (închide), sortarea imediat (închide), **gruparea imediat dar meniul RĂMÂNE deschis** — are șapte opțiuni, o filă care s-ar închide la prima bifă ar trebui redeschisă de șase ori. Fila de grupare arată opțiunile de nivel 0029 pentru COLOANA aceasta + ierarhia grilei cu coloana curentă marcată, toate citite din `Groups` la deschidere (controalele sunt ale designerului, textele nu — aceeași împărțire ca lista de valori de la 0028-06); nivelul EXISTENT se refolosește (culorile/fonturile lui de designer supraviețuiesc) și se înlocuiește pe LOCUL LUI din ierarhie. `KBotDataView.EnableGrouping` (implicit STINS) gatează doar fila, nu `Groups` autorate. Pe drum: `btnSortClear` era un buton MORT (fără handler); liniile despărțitoare erau `Label`-uri, deci deveneau invizibile sub regula generică `Label → Transparent` (sunt `Panel`-uri acum); fonturile «Segoe UI, 9» pinuite pe rândurile de meniu au fost ȘTERSE, nu rescrise (ca la 0027-02). Build 0 erori; `Controls.Tests` **808** (800 înainte, **+8**). ⚠️ **Deschis:** nimic pe ecran, iar tocmai ce s-a cerut se judecă privind (bara de file în cele patru scheme, dacă rândurile crescute chiar rezolvă ce se vedea, saltul ferestrei între file); dus-întorsul prin designerul VS nefăcut |

| 0031-01 | Vizualizator de jurnale — trecerea 1: nucleul din `KBot.Common` (căi, rotație, citire, analiză, filtrare, ceas server) + cablarea celor patru scriitori | DONE (code) / **fără UI în trecerea asta** | `SLICE-0031-01-log-core.md` | Nucleu PUR (net8.0, fără WinForms): `LogPaths` (un singur loc care spune unde stau jurnalele), `LogRotation` (10 MB × 5 generații, **doar redenumiri**, nu aruncă NICIODATĂ — aceeași politică cu `RotatingFileHandler` de pe server, ca să fie o singură regulă peste ambele jumătăți), `LogFileReader` (`FileShare.ReadWrite` — OBLIGATORIU, fiindcă `RunLogger` își ține fișierul deschis cu `AutoFlush`), șase analizoare scrise **după scriitorul real**, `LogFileLoader` (ghicește analizorul, **verifică ghicirea**, coase blocurile, moștenește marcajele), `LogFilter` (pur, ca `IstoricFilter`), `ServerClock`. `TreeLogger` s-a mutat în `Logs\` și cele două `Catch`-uri goale au devenit sinkuri terminale pe `Trace`. Build Debug 0 erori / 4 avertismente **preexistente** (`MSB3825` în `KBot.App`, fișiere neatinse), Release 0/0. `Common.Tests` **58** (14 înainte, **+44**), toate verzi. ⚠️ **Deschis:** (1) suita completă **NU era verde nici înainte** — 11 teste picate preexistente, mulțime IDENTICĂ înainte/după (`diff` gol), lăsate neatinse; (2) `TreeLogger` **nu scrie nimic în aplicația construită** — `Init` e apelat doar din `Surse/SURSA_TREE/Tree.vb`, care nu e în `.sln`; (3) rotația din constructorul `RunLogger` e azi fără efect (nume unic pe rulare + `append:=False`); (4) `ApiServerParser` și `ServerClock` nu au văzut niciodată date reale de server |

| 0031-03 | Vizualizator de jurnale — trecerea 3: `KBotChipBar`, bara de jetoane | DONE (code) / văzută pe ecran DOAR în `LogViewerForm` | `SLICE-0031-03-chipbar.md` | Controlul nou din planul §7, în `src/KBot.Controls/ChipBar/` (`KBotChip` + `KBotChipCollection` + `KBotChipBar`): fratele MULTI-SELECT al lui `KBotNavList`, cu deciziile lui COPIATE, nu reinventate (același `SetStyle`, aceeași `ThemeShapes.FillModern`, același chenar roșu de designer, aceeași suspendare a validării între `BeginInit`/`EndInit`). Jetoanele curg pe orizontală și **trec pe rândul următor** când nu mai încap, dar bara **nu-și scrie `Height`-ul** — gazda o andochează, deci gazda îi deține dimensiunea (aceeași regulă ca `HostOwnsWidth` de la arbore). `AccentOverride` există pentru jetonul roșu de ERORI și cel chihlimbariu de AVERTISMENTE, dar **culoarea o dă APELANTUL** din paletă și i-o RE-dă la comutarea schemei; controlul nu numește nicio culoare. `MinimumRequiredChecked = 1` refuză stingerea ultimului jeton bifat **cu o clipire, nu cu o excepție** — e un gest de mouse, nu un apel de API, deci `SetChecked`/`UncheckAll` NU sunt oprite de prag (fixat cu test în ambele sensuri); pragul contează fiindcă `LogFilter` citește mulțimea goală ca «nimic», nu ca «toate». S-au memorat DOAR pensulele care nu depind de starea jetonului — umplerea trece prin `FillModern`, care ia o culoare, iar `TextRenderer` desenează tot cu o culoare. Build Debug 0 erori / 4 avertismente **preexistente**; `Controls.Tests` **831**, din care **17** ale barei (deci 814 înainte; `SLICE-0030` raporta 808 — diferența de 6 e lucru necomis din afara feliei). ⚠️ **Deschis:** (1) **dus-întorsul prin designerul real VS NEFĂCUT** — dialogul de colecție, liniile din `.Designer.vb`, chenarul roșu pe suprafață; testul `BeginInit`/`EndInit` e cea mai apropiată probă și **nu e același lucru** (zidul feliei 0025); (2) bara nu a fost privită singură, ci doar în fereastra de jurnale; (3) `ApplyTheme` scrie `BackColor`, deci regula `ShouldSerializeBackColor` din `CLAUDE.md` s-ar putea aplica — **nu s-a măsurat**, iar `KBotNavList` are aceeași scriere și aceeași lipsă |

| 0033 | Vederea ORD (Ordonanțări), read-only + ruta nouă `GET /api/forexe/ord` | DONE (code) / **niciodată văzută pe ecran, ruta niciodată pe o bază vie** | `SLICE-0033-ord-view.md` | Ultima dintre cele trei vederi reale rămase — `MainForm.CreateView("ord")` nu mai întoarce `PlaceholderView`. Forma sub-vederilor DDF: arbore lună → ordonanțare în `split.Panel1`, `navSub` orizontal, două sub-pagini leneșe editabile în designer, hrănite de părinte (fără DI ⇒ constructor fără parametri ⇒ se deschid în designerul VS): «Vizualizare» (grilă cu ȘASE coloane autorite) și «Document» (PDF real, montare leneșă cu gardă pe perechea `(cale, existență)`). **Ruta**: un singur drum dus-întors, două tablouri (`ordonantari` + `linii`); clientul filtrează local pe `IDORDP`. Cele trei capcane `FX_ORD` tratate explicit — legăturile merg pe cheile «…P» (nu pe id-urile Access omonime), `ClasificatiiG` nu se atinge, iar inversiunea `IdClsf` (aici `IdClsf` = FK `Clasificatii`, `IdClsfAcc` = id Access — invers față de `FX_Indicatori`) e respectată. Filtrul e DIRECT pe `FX_ORD.CodAngajament` (scris cu `_strict_str_nonempty` de ambele căi de migrare), nu prin ocolul Access `FX_ORD.IDDF = FX_DDF.IDDF`, care ar fi adus și fan-out (PK compus). `total_ord` = subinterogare scalară, deci mai mulți beneficiari nu-l pot umfla (familia `aggOrd`). **Clasificația: AMÂNDOUĂ drumurile într-un `COALESCE`** — planul cerea alegerea după o probă pe date reale, proba n-a fost posibilă, deci se încearcă directul (documentat de sync-ul ORD, identic cu `ddf.py`) și se cade pe cel VERIFICAT în 0011-03 (`FX_Indicatori` → `IdClsfAcc + IdUnitate`); a ghici unul reintra fix în capcana «Clsf gol în producție». **`pdf` — abatere motivată:** coloana se numește `CalePDF` (nu `PDF`) și nu e scrisă de nicio rută de migrare, ca cele patru coloane PDF ale DDF-ului; se PROBEAZĂ o dată prin `information_schema` (memorat per bază) și se trimite doar dacă există — un `SELECT` orb ar fi dat 500 pe toată ruta. E oricum doar un SEMNAL: existența se decide printr-o probă pe discul clientului. Convenția PDF **citită din VBA**, nu ghicită (`mdl_FX_ORD_PDF`): `<root>\<partener\|GENERAL>\ORD_NR_{NrORD}_{Cod}.PDF`, partenerul din `FX_DDF` prin `IDDF` (de unde `part_ang`/`nume_partener` pe fir); ciudățenia Access care produce nume `ORD_NR_0_…` e documentată, dar **nu reprodusă**. Pagina «Document» NU folosește suprafața „document lipsă" a lui `ReaderHostPreview`: aceea poartă un buton «Generează», iar generarea ORD e o felie ulterioară. Build 0 erori; **10 avertismente `MSB3825`** (8 preexistente + 2 de la `OrdView.resx` — aceeași familie structurală ca orice vedere cu `ImageList` autorit), deci **nu «0 avertismente»** cum cerea planul. `pytest` 75 trecute / 15 sărite (neschimbat); ruta se înregistrează (`['/api/forexe/ord']`, cu un `config.py` de umplutură — config-ul real lipsește de pe mașină). **Fără teste noi** (planul §11); cele șapte dubluri de `IApiClient` din `KBot.App.Tests` au primit doar ciotul cerut de contract. ⚠️ **Deschis:** (1) ruta n-a atins niciodată o bază vie — care ramură a `COALESCE`-ului câștige, dacă `CalePDF` mai există, dacă `information_schema` e citibil cu utilizatorul aplicației; (2) nimic randat pe ecran, dus-întorsul prin designerul VS e al operatorului; (3) calea PDF neconfirmată pe un disc real; (4) iconițele pornesc de la setul lui `RezervariView` (`neutral` = glifa «=»), de schimbat din designer; (5) amânate deliberat: generarea, Atașamente/Documente/Fișiere, gruparea pe beneficiar, bifele de selecție multiplă |

| 0033-02 | Arborele `DdfView` adus la paritate cu cel din `RezervariView` (iconițe din `ImageList` + dimensiuni) | DONE (code) / **niciodată văzut pe ecran** | `SLICE-0033-02-ddf-tree-parity.md` | Cerere de operator, rulată în felia 0033. `DdfView` desena TOATE iconițele cu formele GDI din `DdfIcons`, deși `tree_image_list` exista de mult în designer și era legat prin `tree.NodeImages` — pozele erau acolo, nimeni nu le cerea. Acum regula listă-întâi din `RezervariView.TipIconOf`: se caută cheia (`folder_closed`/`folder_open`/`up`/`down`/`neutral`), fallback GDI colorat din paletă doar dacă lipsește. Câștig secundar: luna are în sfârșit două poze distincte (închis/deschis) — înainte ambele stări primeau aceeași imagine. `ItemHeight` 24 → 30, singura dimensiune care mai diferea (fontul și dimensiunile iconițelor erau deja aliniate de 0027-03). Structura, cheile, etichetele, valorile, tooltip-urile și comportamentul arborelui: NEATINSE. ⚠️ **Deschis:** nerandat pe ecran; cheia `neutral` **nu e** încă în `tree_image_list`, deci reviziile neutre rămân pe forma GDI până când operatorul pune poza (nu e regresie — e fallback-ul) |

| 0034 | Descărcarea FOREXE: bandă de subsol + consolă + descărcări din arbore | DONE (code) / **niciodată rulat pe FOREXE viu, nicio suprafață văzută pe ecran** | `SLICE-0034-forexe-footer.md` | Prima suprafață prin care operatorul chiar vede și comandă robotul. `ForexeController` (singleton DI) devine **singurul** care vorbește cu `IForexeRunner`; `MainForm` pierde `EnsureForexeSessionAsync` / `SelectCertificate` / `HasLiveForexeSession` / `UpdateForexeStatus`. Două suprafețe PROASTE legate la el: `ForexeFooterView` (UserControl `IThemedControl`, în `pnlStatus`, înlocuiește `lblProgram` și înghite `lblForexe`) și `ForexeConsoleForm` (`KBotShellForm` redimensionabilă: jurnal complet, progres, stare, Anulează, Arată browserul, Deschide jurnalul). **Descărcările se comandă din ARBORE**, nu din bandă: subsol-stânga = lista, iconița din dreapta unui nod = angajamentul acela. **Tot ce se descarcă rămâne LOCAL** (memorie + JSON cu diacritice literale în `<AppDir>\WorkflowResults\`); singura scriere pe server rămâne vechiul upsert al listei, mutat pe rândul «&Sincronizare (server)» din meniul butonului de opțiuni — `btnSinc` a dispărut din subsol. **Cea mai mare corecție față de plan:** cele două `.wfl` de prelucrare (puse de operator după raportul pasului 0) NU produc un tabel `saveTo`, ci **cinci** (`TabelIndicatori`, `BugetIndicator`, `ListaReceptii`, `Detaliu`, `TabelIstoric`) plus nouă scalari `<Read>` — deci sinkul salvează întreg `JobResult`-ul, cu cheile devenite tabele filtrate din `Data` (altfel fiecare tabel s-ar scrie de două ori, o dată structurat și o dată ca șirul JSON brut). Înainte-vs-înapoi e **citit** din Access `mdl_FX_Tasks_Send.FX_Angajament_InfoComplete` (`DMax("DataFX","FX_Istoric",…)`), inclusiv formatul `dd/MM/yyyy HH:mm:ss` pe care planul îl dădea doar ca fallback — scris cu `InvariantCulture`, fiindcă valoarea intră într-o expresie regulată comparată cu textul din pagină. Echivalentul K-BOT al lui `DMax`: `GetIstoricAsync` → `MAX(DataFx)`, prin `WithReauth`. **`KBot.Controls` a primit `FooterLeftIconClicked`** — planul presupunea că iconița din stânga subsolului e deja un buton; **nu era** (se desena, dar fără dreptunghi, survolare sau eveniment, spre deosebire de sora din dreapta). Adăugat pur aditiv, oglindind exact fratele existent. **Strângerea arborelui a dispărut din shell** (`tree_CollapsedChanged` + `ClampSplitter` șterse; `FooterCollapseButton` nici nu era pornit, deci colțul era deja liber) — arborii din VEDERI o păstrează. Jurnalul: `RichTextBoxLogger` n-are buffer public nici eveniment pe linie, deci consola se construiește **o dată, în `MainForm_Load`**, înaintea logger-ului, și închiderea o ASCUNDE (altfel logger-ul ar rămâne cu un control eliberat). `IForexeRunner` a primit `StatusUpdated`, `HasLiveSession` și `ShowBrowserAsync`. Build Debug **0/0**, Release 0 erori / 5 avertismente `MSB3825` **preexistente**; `Controls.Tests` **831** verzi; `App.Tests` 155/6 — mulțime de eșecuri **IDENTICĂ** cu baza (verificat prin `git stash`). **Fără teste noi.** ⚠️ **Deschis:** (1) nimic n-a atins FOREXE-ul viu — nicio sesiune, niciun certificat, niciun workflow rulat; (2) nicio suprafață randată, dus-întorsul prin designerul VS e al operatorului (riscul standing 0025/0027); (3) JSON-ul nu s-a scris niciodată pe disc real; (4) **nu există mapper de ingestie** pentru «Prelucrare Completa» — rezultatul se ține BRUT, deliberat, până când operatorul vede coloanele reale; (5) calea REVERSE e rar exercitată cât timp descărcările rămân locale (istoricul local reflectă doar ce era deja în bază); (6) `{{DATA_IESIRE}}` nu e documentat în antetul propriu al fișierului Reverse, iar `collectFields` pentru recepții DIFERĂ între cele două `.wfl` (varianta completă omite `TipReceptie`/`CodIndicator`) — nereparat, nu e al feliei; (7) iconița de subsol e `database`, aleasă din ce exista — nu e o glifă de descărcare. **Trecerea operatorului peste bandă** (după prima livrare) a adus controlul nou **`KBotProgressBar`** (`KBot.Controls/Progress/`), fratele DETERMINAT al lui `KBotBusyBar`: `System.Windows.Forms.ProgressBar` e desenat de Windows, deci tema nu-l poate atinge — rămânea verde pe schemă întunecată. Regulile casei cap-coadă (`Color.Empty` = din temă + `ShouldSerialize`/`Reset` pe cele trei culori; `Font` cu perechea lui, fiindcă nu poate purta `<DefaultValue>`), iar `Value` **prinde** valorile din afara intervalului în loc să arunce (sursa e `IProgress(Of Integer)` de pe firul robotului — o excepție acolo ar opri descărcarea din cauza unei bare de progres). **Capcana `IThemedControl` a lovit a treia oară:** banda e ea însăși `IThemedControl`, deci `ThemeManager` NU recurge în copiii ei și bara ar fi rămas pe culorile implicite — `ApplyTheme` o împinge explicit mai departe. În bandă, `pbProgress` se vede doar cât rulează ceva și `lblCert` doar după alegerea unui certificat (andocate la stânga ⇒ ascunse nu lasă gol); steagurile pornesc din CONSTRUCTOR, nu din designer, ca suprafața de proiectare să rămână editabilă. Consola a primit același control (doar tipul), ca să nu existe o bară tematizată și una nu pentru același progres. **Cele două `.Designer.vb` fuseseră între timp deschise în designerul VS și re-serializate — deci dus-întorsul s-a făcut și au supraviețuit.** ⚠️ `KBotProgressBar` **n-a fost privit niciodată pe ecran** și nu are teste |

**Next free slice number: 0035.** (0021 rezervat pentru semnarea DDF — `Status_migrare_6` §4.3.8.)
(0031 e în curs: trecerile 01 și 03 sunt gata; mai vin 02 jurnale de server și 04 `LogViewerForm`.)
(0032 = sub-paginile DDF, în lucru în arborele de lucru la data feliei 0033.)

**Sinkuri terminale ne-rearuncătoare — sunt CINCI, nu trei.** Planul feliei 0031 §1.4 cerea
corectarea, aici, a unei linii care ar fi spus «exact trei». Linia aceea **nu a existat niciodată în
acest fișier** (căutată în tot `docs/`); singura formulare a regulii stă în `CLAUDE.md:43` și nu
numără nimic. Situația reală, ca să nu mai fie nevoie de căutare data viitoare:
`GlobalErrorLog.Write`, `AdobeHostLog.Write`, `DevHarnessForm.HandleUiError` (scrierea în runlog),
plus, din felia 0031-01, `TreeLogger.Write` și `TreeLogger.Init`. Ultimele două **nu pot** rearunca:
`AdvancedTreeControl` le cheamă din căi de desenare, iar o excepție dintr-o scriere de jurnal în
`OnPaint` omoară procesul. Toate cinci scriu motivul pe `Trace` — niciunul nu înghite tăcut.

---

## Current focus

- **Slice 0017-04 (Plăți: arborele pe două niveluri) — DONE și VĂZUT PE ECRAN 2026-08-13.**
  Prima acceptare vizuală a vederii Plăți. Trei lucruri merită scoase din ea. (1) **Designerul
  Visual Studio a devenit a doua sursă de adevăr, iar codul nu știa asta** — trecerea operatorului
  autorase cele cinci coloane ale grilei, `BuildColumns()` le adăuga a doua oară, și regula „fără
  no-op tăcut" a transformat asta într-o excepție care dobora activarea vederii. Când designerul
  autorează ceva, codul trebuie să înceteze să o facă. (2) **Ce scrii de mână în
  `InitializeComponent` moare la primul dute-vino prin designer** — cele zece apeluri
  `InitDetailPair` fuseseră șterse de o regenerare, iar panoul de detaliu ieșea gol fără nicio
  eroare; cablarea stă acum în constructor. Aceeași capcană e de verificat în `IstoricView` și
  `DdfView`, ale căror designere au fost regenerate în aceeași rundă (vezi Open threads).
  (3) **Trei capcane de test, toate generale:** `Type.GetMethod` e sensibil la majuscule deși VB
  nu e (o redenumire de handler a arătat exact ca un defect de vedere); `Throw failure` resetează
  stiva și ascunde locul real al eșecului (`ExceptionDispatchInfo.Capture(...).Throw()` nu);
  `ImageList.Images(i)` întoarce un `Bitmap` NOU la fiecare acces, deci `Assert.Same` nu poate
  trece niciodată pe iconițe — comparația se face pe pixeli.
- **Slice 0030 (meniul de coloană: trei file + rânduri pe măsura temei) — DONE (code); nevăzut pe ecran.**
  Trei lucruri merită scoase din ea. (1) **O clauză `Handles` pierdută la o redenumire nu arată ca
  un defect de cod, arată ca un defect de DATE** — meniul se desena corect, bifele se puneau
  corect, și totuși grila ieșea goală, fiindcă modelul nu afla niciodată de ele. Comentariul de
  deasupra lui `LstValori_ItemCheck` spune acum ce se rupe fără clauză; e singura apărare pe care o
  are o legătură care nu se vede în corpul metodei. (2) **`ModernRenderer` crește butoanele, dar
  într-un `TableLayoutPanel` creșterea aia nu se vede niciodată** — motorul de așezare taie orice
  control andocat la dreptunghiul celulei, deci un buton de 56px într-o celulă de 40 raportează 40.
  De aceea `ThemeTableFit` întreabă `GetPreferredSize`, nu `Height`; și de aceea rândurile autorate
  în designer (care nu știe de teme) trebuiau să capete pe cineva care le crește cu exact surplusul
  cerut, dintr-un instantaneu al valorii AUTORATE — altfel a doua comutare de schemă măsoară peste
  rezultatul primeia. (3) **`Control.Visible` nu poate fi întrebat de nimeni**: getter-ul răspunde
  despre lanțul de părinți, deci pe un formular încă nearătat totul raportează `False`. Capcana a
  mușcat de două ori în aceeași felie (măsurarea rândurilor, apoi fila curentă) și a fost rezolvată
  la fel de amândouă dățile — starea se ține explicit, nu se deduce dintr-un control.
  **Next:** aceeași datorie ca tot 0028–0029 — nimic n-a fost pe ecran, iar aici se judecă privind
  exact lucrurile pentru care a fost cerută felia (bara de file în cele patru scheme, dacă
  rândurile crescute sub Modern chiar rezolvă ce se vedea, saltul ferestrei la schimbarea filei).

- **Slice 0029 (`KBotDataView` — grupare ca la raportul Access) — DONE (code); nevăzut pe ecran.**
  Trei lucruri merită scoase din ea. (1) **Gruparea a cerut o A TREIA numerotare** — indici de
  BANDĂ, adică rândurile desenate, printre care antetele și subsolurile de grup, care nu sunt
  rânduri de model deloc. Cele două de dinainte (MODEL / VEDERE) n-au fost atinse, și tocmai de
  aceea `_view` a rămas exact ce era: lista rândurilor de date care trec de filtre. Consecința
  fericită e că subsolul grilei și măsurarea coloanelor n-au avut nevoie de nicio modificare.
  (2) **Sortarea NU poate rupe gruparea, și asta e o proprietate a comparatorului, nu o
  verificare** — cheile de grupare se compară primele, deci nu există click care să împrăștie
  aceeași cheie prin listă; iar sortarea pe coloana DE grupare întoarce sensul nivelului în loc să
  adauge o a doua cheie. (3) **Filtrarea și strângerea NU sunt același lucru**, deși amândouă
  ascund rânduri: prima schimbă pagina (deci și totalurile, deci și selecția cade), a doua doar ce
  se vede din ea (totalurile stau, selecția rămâne). Dacă s-ar purta la fel, închiderea unei luni
  ar face totalul general să se miște singur — ceea ce se citește ca o greșeală de calcul.
  **Next:** aceeași datorie ca tot 0028 — nimic n-a fost pe ecran, iar aici se judecă privind exact
  lucrurile pentru care a fost cerută felia (nuanța nivelurilor, retragerea, fonturile). Și niciun
  ecran real nu grupează încă: `IstoricView` (clasificații) și `PlatiView` (luni) sunt candidații.

- **Slice 0028-08 (the operator's own pass over the two filter forms) — DONE; a delivery slice.**
  Everything uncommitted went in, including work the operator did himself in the Visual Studio
  designer. Three things are worth carrying out of it. (1) **A wide "menu row" cannot be a Modern
  button**: the scheme rounds every `Button` with a radius-8 `Region`, and on a full-width row the
  four cut corners show the surface underneath — the menu read as four grey pills glued to a white
  sheet. `DetachButton` is what makes a row a row, and since 0028-07 it also hands back the authored
  padding and height. (2) **Two window-order defects**: `CustomPopup` raises `ItemClicked` before
  `Close`, so a modal dialog opened from it leaves the submenu on screen; and setting a
  deactivate-guard AFTER `Hide()` is too late, because hiding the active window is itself what
  raises `OnDeactivate`. (3) **The VS designer deletes code it did not write** — opening `PlatiView`
  ate the loop that added nine `AutoSize` row styles, and since styles are positional the surviving
  `Percent(100)` line landed on row 0, so "Nr. document" absorbed the space meant for "Explicații".
  That is slice 0028-05's lesson from the other side: what the designer cannot read back, disappears.
  **Next:** the same open item as the whole of 0028 — none of it has been verified on screen by me,
  and `PlatiView`'s detail pane is repaired on paper only.

- **Slice 0028-07 (Modern: a button grows to fit its padding AND its text) — DONE (code); not seen
  on screen.** Reported on the freshly designer-authored filter menu: under Modern the buttons got
  so much padding that their captions vanished. `ControlPadding` is written into `Button.Padding`,
  which does not draw outside the button — it shrinks the area the text lives in, so a 32px button
  had 16px left for a line that needs ~15–20. Fixed in the ENGINE, in the same shape as slice
  0028-05's authored column width: the button's authored padding and height are captured before the
  theme's first write, the theme computes `padding + one line of text + borders` and takes the
  **maximum with the authored height**, and leaving the scheme gives both back. Two things worth
  carrying: (1) the padding was **never given back** before — `DetachButton` dropped the handlers
  and the rounded region but left the modern padding on the button, so one pass through Modern
  rewrote the design permanently; (2) height is only ours when the button owns it — `AutoSize` and
  `Dock = Left/Right/Fill` are skipped, because there the parent decides and the next layout would
  erase the write. **Next:** unchanged — nothing in 0028 has been on screen, and this one now
  changes how every short button in the app looks under Modern.

- **Slice 0028-06 (the column filter menu, authored in the DESIGNER) — DONE (code); not seen on
  screen.** The menu was hand-drawn (≈400 lines of painting plus as much geometry and hit-testing)
  for a reason that was true when it was written — stock controls would have stayed white under a
  dark scheme — and that stopped being true inside the same slice: `ThemeManager` now carries
  per-type rules for `CheckedListBox`/`CheckBox`/`Button`/`Panel`, native scrollbar theming
  included, and `KBotThemedForm` applies them. So it is now an ordinary house form: every control in
  `KBotFilterPopup.Designer.vb`, the painting file deleted, and only what cannot exist at design
  time left to runtime — the list's CONTENT, the captions that depend on the column's value type,
  and the enabled/hidden state of two commands. The decisions did not move, and the proof is that
  the old test class passed unchanged. **Worth carrying:** a control that delegates `Text` to an
  inner TextBox must still raise its own `TextChanged` — `KBotTextField` did not, so the first
  `Handles txtCauta.TextChanged` written in the designer silently never ran. **Next:** the same as
  the rest of 0028 — nothing has been on screen, and dark-scheme check marks in a `CheckedListBox`
  are exactly the kind of thing judged by looking.

- **Slice 0028-05 (`KBotDataView` — who owns a column's width) — DONE (code); not seen on screen.**
  An operator report on `IstoricView` ("no matter what I do, the first column becomes huge") turned
  out to be two separate defects, neither visible by reading the view — only by reading the numbers:
  the four widths in the designer file summed to **exactly the design surface's own width**, which
  no human types. (1) The auto-size pass was **running inside the Visual Studio designer**, and the
  designer serialized its output as if the operator had chosen it — the `ShouldSerialize*` trap one
  level down, where the frozen value is not a property default but the result of a layout
  calculation. A layout pass has no business authoring the form; it now returns immediately under
  `KBotDesignTime.IsDesignTime`. (2) **Shrink was destructive**: a view built inside a narrow
  splitter panel shrank its columns and, on widening, only the fill target could grow — so an
  authored 200px column stayed at 120px for the rest of the session. Fixed in the model: the column
  keeps the authored width apart from the painted one, and the pass restores that baseline before
  every run, so **a pass is a function of (authored widths, available space)** and not of how the
  window happened to be resized. **Next:** the other five views may carry the same frozen
  pass-output in their `.Designer.vb` — deliberately not touched in bulk.

- **Slice 0028-04 (`KBotDataView` — per-column `AutoSizeMode`, which beats the grid's) — DONE
  (code); not seen on screen.** The operator had started the property and left the measuring pass
  blind to it. The whole slice turned on one decision: **what the default of a property that "takes
  precedence" may be.** It shipped as `None`, and precedence plus a `None` default means no column
  is ever measured again — the grid ships on `ToContent`, so five existing hosts would have lost
  their sizing the day somebody built a column, silently and much later. The fix is a third enum
  member, `Inherit = -1`, the column's default: "no opinion, the grid decides" — the same sentinel
  shape as the tree's `Color.Empty` = "from the theme". `Inherit` **throws** on the grid property,
  because there is nothing above the grid to inherit from and accepting it would mean silently
  translating it into something else. Two other things worth carrying: the entry gate of
  `PerformAutoSize` had to move from the grid to the columns (otherwise a column asking to be
  measured on a manual grid never is — the one arrangement the property exists for), and
  **precedence covers measuring only**: fill / shrink still moves a `None` column, exactly as it
  already moves one the operator has drag-resized. **Next:** the same as the rest of 0028 — the
  bench (which now has the switch) has still never been run.

- **Slice 0028-02 (`KBotDataView` — footer caption, per-column header icons, Access-style
  `Format`) — DONE (code); nothing in it has been seen on screen either.** Three operator requests,
  and each one turned on a boundary decision worth remembering: (1) **the footer caption stops at
  the first aggregated column**, not at the band's right edge — text that ran under somebody's total
  would read as that total's label, which is a different sentence than the one it says; (2) **when a
  column narrows, the caption is cut first and the LEFT icon is dropped second — the right icon
  never goes**, because it is the one that can be clicked, and a button that vanishes exactly when
  the column is narrow is missing precisely when it is needed. That rule is what forces
  `EffectiveMinWidth` (a floor that beats even `MaxWidth`) and it is why the header cell's layout is
  a pure function shared by painting, hit-testing and the tests — two formulas would mean an icon
  drawn where it is not clicked; (3) **`Format` and `FormatString` are two faces of one thing, so
  both set throws** instead of one silently winning, which would leave a property in the form that
  looks live and does nothing. The named format also READS the value (a `Text` column carrying
  numbers still formats), and it is resolved in one place so the footer total is written exactly
  like the column above it. **Next:** the same one as 0028 — the DevHarness bench has still not been
  run, and it does not yet have switches for any of these three, so a visual verdict needs that
  first.

- **Slice 0028 (`KBotDataView` — footer aggregates gated by value type, themed bands, cell
  tooltip, collapse button) — DONE (code); NOTHING in it has been seen on screen.** The grid now
  carries the tree's footer vocabulary (`FooterVisible`/`Footer*`, the old `ShowTotalsRow` is gone
  and its five call sites migrated) and a collapse button with the same contract as
  `AdvancedTreeControl`/`KBotNavList` — plus one axis the siblings don't have, because a grid docked
  `Fill` in a view gains nothing from a 100px vertical strip: on `Vertical` the body disappears and
  the two bands remain, which is exactly the aggregates. Three lessons worth carrying: (1) **the
  render type is not the data type** — `KBotColumnType.Text` columns carry the money in DdfView and
  PlatiView, so the aggregate offer had to be gated by a new `KBotValueType`, and the pairing throws
  rather than quietly summing a column of names to 0; (2) **a designer emits properties in ITS
  order**, so a loud setter has to defer its check inside `BeginInit`/design time or the host form
  stops opening at all — the pair is validated at `EndInit` instead; (3) **GDI+ does not throw for an
  unknown font family, it silently falls back**, so «is Segoe UI Semibold installed?» must be asked
  of the installed-families list — the old `Try New Font(…) Catch` was a check that could never fire,
  which is how the header font stayed literal (and theme-deaf) since 0010. **Next:** run the
  DevHarness bench (it has the new switches and a deliberately-too-narrow «Denumire» column, the only
  way the tooltip can be seen), then give one real view the host half of the collapse button — today
  no view uses it, so that path is verified by tests only.

- **Slice 0027-02 (`AdvancedTreeControl` — one font, footer band, collapse) — DONE (code); the
  COLLAPSE has screen time, the rest of the band does not.** `TreeFont` is gone: the tree drew its nodes in one font while measuring the row
  height, the search band and the header default in another — two sources of truth for the same
  measurement. `Font` is the survivor, and the four places that pinned «Segoe UI, 9» were **deleted
  rather than translated**, because that value is now the default and writing it explicitly would
  pin the font and make it deaf to `ThemeManager.ApplyBaseFont`. The new footer is the header's
  sibling (same `Color.Empty` = «from the theme» contract, same `ShouldSerialize*`/`Reset*` pairs)
  plus the one piece the header has no equivalent for: a collapse button that takes the tree down to
  `MinimumCollapsedWidth` and, while collapsed, floats the hovered row out to the right through
  `TreeNodeFlyout` — `KBotNavFlyout`'s sibling, same window tricks, same «geometry in pure functions»
  discipline. Two lessons worth carrying, both caught by the new tests on code nobody was suspecting:
  (1) **a `Reset*` that clears its pin flag before calling the base is a no-op** — `Control.ResetFont`
  writes `Font = Nothing` through the *virtual* setter, i.e. through yours, which sets the flag right
  back; clear it *after*. This was live in `ResetFont` since 0027 and nothing asked;
  (2) **hit-test geometry must never live in a field that only paint writes** — the collapse button
  was unclickable headless because its rectangle was published by `DrawFooter`. Both the drawing and
  the hit-test now read the same pure function. And a third, from the operator's first run
  (09.08.2026, «it collapses the tree then imediately it expands it again»):
  (3) **a control whose width is owned by the host layout must not write `Width` at all** — the tree
  is `Dock=Fill` inside `split.Panel1`, so every `Me.Width = 100` was handed straight back on the
  next layout pass, which is what the flicker was. Writing the host contract in a worklog is not
  implementing it: the control now goes quiet when `HostOwnsWidth`, and `MainForm` moves the
  splitter on `CollapsedChanged`. And a fourth, same day:
  (4) **reserving layout space for something nobody can see cancels the reason it is hidden** —
  the caption gave up the right-icon width on every row whenever the node had a `RightIcon`, hover-only
  or not, so `ShowRightIconOnHover` bought an empty strip instead of a wider text. The gutter is now
  one function that asks whether the icon is actually on screen; the column band deliberately stays
  hover-blind, because a full-control geometry that re-lays out under the cursor would be unusable.
  **The band itself — gradient, drawn chevron, caption alignment
  and its own colours — is still unconfirmed piece by piece, and the fixes in §4, §5 and §6 have not
  been re-checked on screen.**

- **Slice 0027 (`AdvancedTreeControl` — search bar, theming, designer surface) — DONE and, for
  once, SIGNED OFF ON SCREEN (operator, 08.08.2026: «everything had screen time and it checks
  out»).** Build 0 warnings, **884 tests**. This is the first control slice with a visual verdict:
  0010+ (`KBotDataView`), 0018/0025 (`KBotNavList`) and 0026 all remain «code green, never seen».
  Three things are worth carrying forward as *lessons*, not just as a changelog:
  (1) **a property that only the XML path acts on is a dead property in the designer** —
  `SearchShow` was written by `MainForm.Designer.vb` and consumed only by `ResolveHeaderIcons`,
  which a designer-authored form never calls; whenever a control has two authoring paths (XML/FOREXE
  and designer), every property needs a seam both of them pass through;
  (2) **`ThemeManager.Traverse` recurses into the children of anything that is not
  `IThemedControl`** — a control that owns internal child controls MUST implement the interface or
  the generic per-type rules will repaint them. This has now bitten twice (`KBotTextField`, then the
  tree's search `TextBox`). Any future control with internal children starts from `IThemedControl`;
  (3) **without `ShouldSerialize*`/`Reset*`, Visual Studio serialises the RESOLVED default** into
  `.Designer.vb`, and that frozen value then reads as an operator choice forever — which is exactly
  how the light palette ended up hard-written into five designer files. New colour/font/image
  properties get the pair from the start. **And the inherited ones need it too:** a control that
  writes `Control.BackColor`/`ForeColor` itself (constructor default, or a theme apply) makes the
  base `ShouldSerialize` answer True, so the designer freezes a colour nobody chose — override the
  pair and answer from your own "the operator pinned it" flag instead. Caught on re-read by the
  operator, not by the tests, because nothing was asking what the *designer* would serialise; the
  test now asks through `TypeDescriptor`, the same path Visual Studio uses.
  Nothing here touched the Python API and nothing ran against a real database.
  Deferred out of this slice: the **`ComboBox` theming retrofit** (CLAUDE.md listed it together with
  the tree; only the tree half is done) and the Istoric/tree **sort buttons**, still `MsgBox` stubs.

- **Slice 0024 (Adobe profiles on the DDF «Document» tab) — three passes landed (0024-01/02/03),
  code green, 0 warnings, 693 tests.** The two measured bench configurations ship as profiles behind
  a runtime setting; the harness runs on the same shared hosting code (`src\KBot.Controls\Adobe\`);
  and pass 03 fixed the two defects the operator was actually seeing — **the stray Adobe window with
  a taskbar button** (teardown restored the style and re-parented to the desktop, which *creates* a
  top-level window; `RestoreStandalone` is now deleted from the codebase) and **the window flashing
  before it is placed** (the search required `IsWindowVisible`, so it could only ever match a window
  already drawn on screen; capture is now PID-first, tolerates an invisible window, and hides it
  inside the search loop). Operator documents: `docs\SETARI_UTILIZATOR.md`.
  **Human items, none done yet — and note that pass 03's definition of done is explicitly NOT
  signed off, because every remaining item is an on-screen observation:**
  (1) **open a second DDF document and confirm no stray window and no taskbar button** — the code
  paths that produced the defect are gone and their absence is pinned by tests, which is a weaker
  claim than «observed fixed»; (2) **watch for the residual flash and state it as a measurement** —
  hiding moved from «after visible» to «on match» and polling from 150 ms to 30 ms, but by how much
  that shows on a real machine is unknown; the creation hook (off by default) may narrow it further
  and cannot be promised to close it; (3) **compare detach mode A against B on the bench** using the
  new launch→embed milliseconds label, then set `ReaderHostPreview.DetachMode` accordingly — but see
  the finding below: **mode B did not work on the operator's machine**; (4) ~~record the AcroPDF
  verdict~~ **DONE 05.08.2026 — a real XFA DDF renders, and document switching works. The one grey
  failure was the same document being open in the hosted window at the same time, not a limit of the
  control**; (5) **fill in the `bEnableAv2` table** (is the opt-out still honoured, per UI
  generation) — `AdobeUiPreference` exists but is inert behind `ENFORCE_LEGACY_UI = False`, so its
  snapshot/restore path has never executed in the app; (6) **open one real DDF PDF on the «Document»
  tab** — the measured geometry has still never been seen working inside the *app's* panel (it was
  measured in the bench's, a different size); (7) **confirm the popup watcher actually finds an
  `AVL_AVPopup`** — the class name comes from the brief and nothing in the repo corroborates it;
  (8) **re-run the two saved bench states with Adobe open**; (9) **decide whether the shipped default
  should force `/n`**: the modern profile was measured with `newInstance = false`. Pass 03 makes that
  risk *safer* — a process K-BOT did not start is now never killed, in the app or the bench — but
  does not settle it.
- **Slice 0022 (Istoric view) — both passes landed (0022-01/02), code green, 0 warnings; the
  seventh real view (the sixth was DDF).** Endpoint (`{cod, randuri[], clasificatii[]}`) +
  `IstoricView` (filter strip with three menu buttons, grid, detail pane) + the pure
  `IstoricFilter` engine. **Operator revision 2026-07-24:** value columns removed from the grid
  (now Clasificație/Tip/Data/Descriere/Observații, Observații stretched last), totals row removed,
  and the non-zero values moved into a small `Tip | Valoare` grid in the detail pane (unpivot per
  row). Human items, none done yet: (1) **the #1 host check —
  the classification-caption tables** `AVACONT_COMUN.DefaClsfF` / `DefaArticol`: the plan (§2.2)
  says `.Denumire`, but the Access export (`qFX_DDF_SA_CLSF`) says `DefaClsfF.Explicatie` and
  `DefaTitlu2`, and the real MariaDB schema for these nomenclatoare is not in the repo — if the
  names differ, one SQL string in `istoric.py` changes; (2) run `test_forexe_istoric.py` on the
  host (12 tests, skip off-host) — also answers the finding test (`Status_migrare_5 §9`: how many
  `(IdUnitate, IdClsfAcc)` map to >1 `Clsf`); (3) `IstoricView` has never been rendered on screen.
- **Slice 0020 (DDF view) — all five built passes landed (0020-01…05), code green, 0 warnings;
  the sixth real view.** Endpoint + `DdfView` (tree/horizontal sub-nav/grid/combo) + two preview
  surfaces + `KBotPaths`/file browser + PDF generation (`DdfXmlBuilder` + background-thread
  `XfaWriter.Genereaza`). **Operator revision 2026-07-24:** added a fourth sub-nav page «Document»
  hosting the **real PDF** (`ReaderHostPreview`, lazy-loaded only when that page is visible so
  Adobe isn't launched on every tree click); selecting a file in «Fișiere» now opens it there
  instead of reverting to «Vizualizare» (the XFA reconstruction). See `SLICE-0020-03` revision note. Human items, none done yet: (1) **step 05-00** — `GET`/`HEAD`
  `https://kbot.avatarsoft.ro/api/mfp/template_ddf` with `X-API-KEY` over 443 (the gate; if it
  fails it's an nginx `location`, not app code); (2) run `test_forexe_ddf.py` on the host (17
  tests, skip off-host) — also answers §3 item 5 (how many `CodAngajament` have >1 `FX_DDF`
  header); (3) **produce one real PDF and confirm Adobe opens it** (closes 0019 a/c); (4) capture
  a real Access-written DDF `.xml` and diff it against `DdfXmlBuilder` (settles the UTF-8-vs-ANSI
  base64 risk; no DDF sample exists in the repo — `ddf_demo.xml` is really ORD); (5) nothing in
  0020 has been rendered on screen. **Pass 06 (signing) deferred to slice 0021.**
- **Now:** run Slices 0008 + 0009 on the host. The endpoint (incl. 0009's orphan escape)
  and the client are written and green offline, but nothing has hit a real database:
  `PYTHON/tests/test_forexe_tree.py` skips off-host and is the fastest way to answer
  verification items 1–5 and 8, plus the two new orphan tests (`TREEO`/`TREEX`).
- **Next:** the remaining seven views are still `PlaceholderView` (Sumar landed in 0011,
  Rezervări in 0014); Slice 0004's remaining Tier 1 items and the Slice 0003 VPS config
  are still open and short.
- **Slice 0015 (Recepții) — pass 0015-01 landed, code green, 0 warnings; the third real
  view.** The plan's three forks (tree depth, LISTA driver, single endpoint) were all
  settled from the Access source, not guessed. Three human items: (1) run
  `test_forexe_receptii.py` on the host (15 tests, skip off-host) — confirms the eight
  tables/columns exist and the `FX_Indicatori` join yields a populated `Clsf` (blank on
  every row ⇒ join key, same trap as 0011-03); (2) `ReceptiiView` has never been rendered
  on screen; (3) 0015-02 tooltip + **0015-03 operator revision landed** (3-level month tree,
  aggregated LISTA at any level, Descriere=Clasificatii.Denumire, month+receptie tooltip with
  the plăți window extended to the next month's first receptie). Nothing in 0015 has been
  rendered on screen or run on a live DB.
- **Slice 0014 (Rezervări) — code green, 0 warnings; the second real view.** The plan's
  open month-total question was answered from the Access source (`qFX_REZERVARI_TREE.TOTALL
  = SUM(R_Valoare)`), not guessed. Three human items: (1) run `test_forexe_rezervari.py`
  on the host (16 tests, skip off-host) — confirms the eight tables/columns exist and the
  `FX_Indicatori` join yields a populated `Clsf` (blank on every row ⇒ join key, not the
  view — same trap as 0011-03); (2) `RezervariView` has never been rendered on screen;
  (3) the exact screenshot month totals were not reproduced numerically (no data for that
  angajament) — confirm visually.
- **Slice 0011 (Sumar) — all three passes landed, 164 .NET tests green, 0 warnings.**
  The join-key question is now ANSWERED on real data (0011-03): the key was wrong,
  `IdUnitate` was missing, and the nomenclator has duplicates — all three fixed by
  moving to scalar subqueries. Two things still need a human:
  (1) rerun `test_forexe_sumar.py` on the host (18 tests now; they skip off-host) to
  confirm the fix end-to-end and that all eleven touched tables exist;
  (2) look at `SumarView` on screen — **it has still never been rendered**.
- **Slice 0010 (`KBotDataView`) — all six passes landed, 158 tests green, 0 warnings.**
  **The one open item is a human one:** nobody has run the visual harness yet. Please run
  DevHarness (Debug start → «Nu») → Controls/UI → «KBotDataView — virtualizare + temă
  (5.000 × 20)» and give it a Pass/Fail. That probe is the only way to confirm scroll
  smoothness, keyboard/resize interaction, floating-editor placement and the actual colours —
  and pass 06 found a bug (all column headers were `Nothing` since 0010-01) that no headless
  test could have caught. After that: the **Sumar** slice, which consumes the grid read-only.

## Open threads (not yet scheduled)

- **⚠️ 11 teste ROȘII comise deliberat la 13.08.2026, ca să nu se piardă munca din arborele de
  lucru.** Niciunul nu ține de Plăți (0017-04 e 16/16 verde); soluția compilează cu 0 avertismente.
  Două grupuri, cu cauze diferite:
  - **Roșii DEJA pe `master`, în cod NEATINS de arborele de lucru** (deci regresie mai veche, nu
    din munca asta): `KBot.Domain.Tests.DdfInfoTests.EtichetaRevizie_*` (3) și
    `KBot.Api.Tests.ApiClientTests.GetDdf_FormatsRevisionLabel_WithSpacePadding_Not_Zeroes` (1).
    Toate patru se învârt în jurul aceleiași reguli: eticheta de revizie DDF se umple cu SPAȚII,
    nu cu zerouri. Cineva a schimbat regula fără să atingă testele, sau invers.
  - **Roșii din munca necomisă până acum, pe vederile trecute prin designer**:
    `IstoricViewTests` (4), `DdfViewTests` (2), `MainFormNavItemsTests.Designer_WroteLiteralDiacritics_NotEscapes` (1).
    `IstoricView.Designer.vb`, `DdfView.Designer.vb` și `MainForm.Designer.vb` au fost toate
    regenerate de designerul Visual Studio. **Verifică întâi dacă n-au pățit ce a pățit
    `PlatiView`** (vezi 0017-04): o regenerare a lui `InitializeComponent` îi ștersese cele zece
    apeluri `InitDetailPair`, lăsând panoul de detaliu gol fără niciun zgomot. Testul de
    diacritice sugerează în plus că designerul a rescris șiruri românești ca `\uXXXX`.
  - **Remăsurate la felia 0031-01 (14.08.2026): tot 11, aceleași nume.** Mulțimea numelor picate e
    IDENTICĂ înainte și după acea felie (comparată cu `diff`, ieșire goală), deci firul ăsta e încă
    deschis exact cum a fost lăsat, iar 0031-01 nu l-a atins nici într-un sens, nici în celălalt.
    Rămâne valabil și avertismentul de acolo: cine repară grupul DDF ar trebui să înceapă de la
    regula unică (umplere cu SPAȚII, nu zerouri), fiindcă un singur defect se vede în Domain, Api
    și App deodată.
- **„Fix Rezervări «+» (one leaf, not all)" a rămas fără număr.** Era planificat ca 0017-04, dar
  operatorul a realocat 04 reveniri asupra arborelui de Plăți. Elementul **nu s-a făcut** și are
  nevoie de un număr nou când se programează.

- **Cod livrat FĂRĂ felie și fără worklog (constatat la comiterea din 10.08.2026).** Arborele de
  lucru conținea, pe lângă feliile 0028…0028-04, un corp de muncă pe care NICIUN worklog și
  niciun rând din registru nu-l descrie: editorul de teme (`KBot.Controls/ThemeEditor/` —
  `ThemeEditorForm`, `ThemeScope`, `ControlStyleProxy`), magazia de suprascrieri de temă
  (`KBot.Theming/Overrides/` + `DesignerBaseline.vb`), schema **Colorful**, `CustomPopup`
  (`KBot.Controls/Popup/`), `KBotComboBox` (`KBot.Controls/Combo/`) și butonul de opțiuni al
  barei de titlu — cu teste proprii, toate verzi. A fost comis ca atare, ca să nu se piardă, dar
  **nu e documentat de nimeni**: ce a cerut operatorul, ce decizii s-au luat și ce a rămas
  neverificat nu se pot reconstitui din cod. Cine reia zona scrie întâi worklog-urile lipsă
  (numere libere de la 0029) și abia apoi le modifică. `SLICE-0028-03` se sprijină deja pe ele
  (`DesignerBaseline.Restore`, «cele patru scheme compilate»), deci golul e vizibil din felie.
- **No genuine DDF XFA sample in the repo (found in 0020-03).**
  `Surse/SURSA_XFA_WRITTER/XSD_XML/ddf_demo.xml` is BYTE-IDENTICAL to `ord_demo.xml` (same
  MD5 `9e5fe957751aca9630a71222dd974dda`) — it is a copy of the ORD machete, not a real DDF.
  Consequence: the 0020 DoD item "the XML builder output structurally matches a known-good
  Access-written `.xml`" **cannot be met from the repo**; pass 05 must capture a real DDF
  `.xml` from a live Access run to diff against. The DDF element set is currently derived from
  `mdl_FX_DDF_PDF.GenereazaXML_PentruPython` alone.
- **`Config.vb` (`KBot.Xfa`) still hardcodes `X-API-KEY`.** 0020-05 step 05-00 repointed
  `BASE_URL` to `https://kbot.avatarsoft.ro/api/` (HTTPS), but the API-key removal is still
  deferred: it means `@require_session` on the two `mfp` template routes + a bearer token into
  `TemplateDownloader` (which also touches ORD). Decide when, not whether.
- **Step 05-00 live check outstanding.** `GET`/`HEAD` `https://kbot.avatarsoft.ro/api/mfp/template_ddf`
  with `X-API-KEY` over 443 has never run. The route + HEAD exist in the Flask app
  (`routes/mfp.py:177`), but the 443 nginx `location` and a template in the server cache are
  unverified. Until this passes, DDF generation (0020-05) cannot be tested end-to-end.
- **No real DDF PDF produced or opened** (0019 open items a/c, still open after 0020-05). The
  generation flow is wired and unit-tested, but `XfaWriter.Genereaza` needs the machete + Adobe;
  the "Adobe opens the generated PDF correctly" verdict is outstanding.
- `_upload_sessions` (routes/ftp.py) is still an in-process dict → blocks multi-worker.
- The rate limiter's counters are also in-process → restart clears lockouts; also blocks
  multi-worker. Both must move to Redis before `workers > 1` is possible.
- **No MariaDB DDL for `FX_DDF`, `FX_ORD`, `FX_DDF_REV_SA`, `FX_Receptii_H`** — five of
  the nine tree flags read them and `DDL_FX_ListaAngajamente.sql` creates none of them.
  The operator confirms all four exist live (2026-07-15); the endpoint hard-fails if one
  does not. Add their DDL so the repo stops disagreeing with production.
- **The two FX_Angajamente DDLs contradict each other**: `DDL_FX_ListaAngajamente.sql:42`
  has `ASCUNS` and `IdUnitate`, `docs/FX_Angajamente.sql` has neither. Operator confirms
  `ASCUNS` exists live. Pick one file as canonical and delete the other.
- The Access export used for the tree contract is the **repo copy**
  (`FX_System_Export/QUERIES/`); nobody has confirmed it matches the live Access file, or
  `C:\AVACONT\FX_System_Export`.
- ~~`KBotNavList` has no `SetItemVisible` — 0008 gates views with `SetItemEnabled`
  (grey-out) instead of hiding.~~ RESOLVED in slice 0018: `SetItemVisible` added and
  `ApplyViewGating` now hides (not greys) a view whose `Are*` flag is FALSE.
- `GET /api/forexe/angajamente` + `IApiClient.GetAngajamenteAsync` have **no caller left**
  after 0008 (the tree replaced them on MainForm's load path). Retire or find them a use.
- Server housekeeping: kernel reboot, systemd hardening, de-root the service user (blocked
  by AvacontPush SSH dependency), close stray ufw port 5010.
- Naming ambiguity: `Unitati` exists in both `AVACONT_COMUN` and each per-unit DB with
  different columns.
- **`Clasificatii` DDL is not in the repo.** Slice 0011 was blocked on it until the
  operator pasted it by hand. It documents real contracts the code now depends on
  (`Clsf`/`Titlu`/`SS` as STORED generated columns; FKs into `AVACONT_COMUN.Defa*`).
  Add it to the repo so the next slice does not have to ask again.
- ~~**MariaDB inverts Access's classification-id naming**~~ — SETTLED in 0011-03 on
  real data: `FX_Indicatori.IdClsf` holds the **Access** id (matches `C.IdClsfAcc`).
  Promoted to Locked decisions below.
- **`Clasificatii` has real duplicates on `(IdClsfAcc, IdUnitate)`** — on `000_DEMO`:
  `(75,79)`, `(75,84)`, `(75,90)`, `(75,92)`, `(75,93)`, each twice. Sumar is safe
  (scalar subquery + `LIMIT 1` = one row per indicator), so this does not block, but
  it needs triage: **if the duplicate rows differ by `Sursa`/`Sector`, a join
  dimension is MISSING** and `LIMIT 1` is silently choosing between two genuinely
  different classifications; if they are identical, they are data to clean. Any
  future view that needs the classification's `Sursa`/`Sector` must settle this first.

## Locked decisions (do not relitigate without a note here)

- Single-worker Gunicorn stays; `on_starting` guard refuses `workers > 1`.
- Under Redis: TTL counts down to the 30-min absolute cap (no slide); the 20-min idle
  window is enforced in-app from `expires_at`; operator password is never written to Redis;
  keys namespaced `kbot:sess:` in K-BOT's own DB; never FLUSHDB/FLUSHALL (shared Redis).
- Static API key kept ONLY for the legacy FOREXE fleet on shared routes; eliminated for
  K-BOT.
- **The "drop `IdUnitate`" rule applies to `FX_` tables ONLY.** Shared nomenclatoare
  (`Clasificatii`, `Parteneri`, and others of that kind) **keep** the `IdUnitate`
  predicate: the per-unit database holds them for SEVERAL units. On `000_DEMO`,
  `Clasificatii` carries 8 (48, 75, 76, 121, 123, 135, 136, 157). Measured cost of
  getting this wrong in slice 0011 (`FX_Indicatori` = 29 rows, 25 with `IdClsf <> 0`):
  `ON I.IdClsf = C.IDClsf` → **0** rows (wrong key); `ON I.IdClsf = C.IdClsfAcc` →
  **67** (multi-unit fan-out); `+ AND I.IdUnitate = C.IdUnitate` → **50** (duplicate
  fan-out). Full detail: `SLICE-0011-03-sumar-join-clasificatii.md`.
- **Key-naming conventions, both confirmed against the live schema:**
  - `Clasificatii`: `IDClsf` = MariaDB PK, `IdClsfAcc` = retained Access id.
    **BUT `FX_Indicatori.IdClsf` holds the ACCESS id — it does NOT follow the
    convention.** The column name is not evidence.
  - `FX_ORD` family: suffix "P" = MariaDB PK (`IDORDTBLP`), without "P" = Access id
    (`IDORDTBL`).
  - **Operational rule that beats both tables: never infer the key from the column
    name — COUNT rows before and after the join.** A join returning 0, or more rows
    than its left-hand table, is a defect, not a data quirk. Slice 0011 lost three
    live runs to this; one `COUNT(*)` would have caught all three.
- **Migrarea `FX_` NU recreează schema (varianta A, nedistructivă).** Tabelele există
  deja în MariaDB cu DDL curat; `POST /api/forexe/seed/schema` rămâne în cod pentru o
  bază complet goală, dar **utilitarul de migrare nu îl apelează** — face
  `DROP TABLE IF EXISTS` înainte de un `CREATE` derivat din tipurile DAO ale Access-ului
  (`LONGTEXT` pentru orice tip necunoscut, `VARCHAR(255)` implicit), deci ar înlocui o
  schemă bună cu una mai slabă, peste date reale. Lista de coloane se citește în schimb
  din destinație, cu `GET /api/forexe/seed/columns` (slice 0012-01).
- Shared nomenclatoare are read via **scalar subqueries with `LIMIT 1`**, not joins,
  wherever the value is display-only (`Clsf`, `Partener` in Sumar). `Clasificatii` has
  real duplicates on `(IdClsfAcc, IdUnitate)`, so even a fully-predicated join fans
  out; a scalar subquery guarantees one row per indicator.

---

## How to update this file

- When a slice changes state, edit its row and (if needed) the Current focus section.
- When you assign a new slice, add a row and bump "Next free slice number".
- Do it in the same commit as the work, alongside the worklog. Never let this drift.
