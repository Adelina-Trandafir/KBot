# PLAN_MainForm_Scaffolding.md — MOVED

The canonical copy of this plan is **`docs/worklog/PLAN_MainForm_Scaffolding.md`**
(Slice 0006).

This file was an older duplicate that had drifted: the worklog copy carries the same body
plus the slice header and the mandatory "Definition of done" section. Two copies of one
plan is how this project has already lost time twice, so the content now lives in exactly
one place. Nothing is lost — the full previous text is in git history.

**Slice 0006 is DONE.** Two notes for anyone reading the plan as if it were pending:

- the real DI signature is five parameters (`forexeRunner, session, apiClient, authApi,
  loginFactory`), not the four the plan expected;
- the sidebar now lists **ten** views, not six — Slice 0008 added Indicatori, Istoric,
  Revizii and Partener so that each of the nine `Are*` tree flags gates exactly one entry.

See `docs/worklog/KBOT_STATUS.md` and `docs/worklog/SLICE-0008-tree-data-api.md`.
